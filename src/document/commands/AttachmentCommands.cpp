// Umbrella translation unit for domain-specific attachment command headers.
// All implementations moved to the three domain .cpp files (LifeCycle / Angle / Slide).
// This file is kept in the build only so any out-of-translation-unit code that
// still #includes "AttachmentCommands.h" gets a consistent TU (阶段 3 拆分).
#include "AttachmentCommands.h"
