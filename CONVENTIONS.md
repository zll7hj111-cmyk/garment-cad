# CONVENTIONS.md —— 开发规范与验证基线档案

> 本文件是 AGENTS.md「开发规范」「验证命令」两区的全文迁出（2026-09-01 文档规划第二轮）。
> **按需查阅，不注入会话**：AGENTS.md 只留压缩版规则清单；需要历史沿革/测试名/证据时查本文件。
> 改动了本档案声明的约定时，同步修订 AGENTS.md 对应规则条目。

---

## 开发规范（全文）


- **文件编码**：所有新建源文件（.h/.cpp/.qrc 等）必须 UTF-8 with BOM，否则 MSVC 解析失败（C2001/C2061）。**Write 与 Edit 工具写出的文件均无 BOM**（Edit 还会剥掉文件原有 BOM）——创建/编辑含中文的源文件后必须补 BOM（pwsh 脚本 `[System.IO.File]::WriteAllText($p, $t, [System.Text.UTF8Encoding]::new($true))`）。
- **工具模式切换统一用 W 键，禁用 Tab**（Tab 是 Qt 焦点导航键，打断交互）。W 语义按工具区分，切换后 `event->accept()` 并刷新预览/HUD。
- **交互函数必须显式接入事件流**（mouseMove/mousePress/keyPress），只实现不调用 = 功能静默失效；用单元测试直接驱动事件流验证。
- **工具元数据表驱动（2026-08-29 TOOL_SYSTEM_AUDIT P0-H3，2026-12 P3 升级）**：状态栏 9 工具操作提示唯一出处 = 各工具 `static ToolDescriptor describe()` 的 `hintText`（`src/tools/ToolRegistry.h`；P3 后原 ToolManager.cpp 的 toolHintText switch 已删，`cad::tools::toolHintText(ToolType)` 退化为查 `ToolRegistry` 的薄封装）；MainWindow 只渲染（`setToolHint` → elide 省略 + tooltip 全文兜底 + Resize 重算，M9），菜单/工具坞按钮/图标/提示全部遍历 registry 自动生成。**新增工具必须**：①ToolType 枚举加值（ToolRegistry.h）②实现 onActivate/onDeactivate + name() ③实现 `static ToolDescriptor describe()`（id/显示名/图标/快捷键/提示/工厂，缺 = test_tool_hints 红）④ToolRegistry.cpp 加 1 行 `registerTool<T>()`⑤`MainWindow.cpp` 的 `toolDockIcon()` 补 1 项 Ela 字体图标（漏补 = 兜底箭头图标 + 警告，**不崩**，见下条）。旋转提示必须描述 D15 确认门（H2 拍板文案）；提示还可在运行期被工具覆盖（M5，见下）。**运行期提示覆盖（M5）**：工具内部有静态元数据表达不了的状态时（智能笔 直线/省道线），用 `Tool::reportHintOverride(hint)` 经 `ToolHost::setHintOverride` → `ToolManager::hintOverrideChanged` → `MainWindow::onToolHintOverride` 改写状态栏文案；**切工具时 MainWindow 会先清覆盖**（不跨工具继承），传空串 = 恢复 `describe()` 的默认文案。`ToolHost::setHintOverride` 是**非纯虚**（提示是可选能力，无头单测桩不实现也能编译）。
- **工具公共能力层三件套（2026-08-29 TOOL_SYSTEM_AUDIT P1 收口）**：①**HUD 标签一律用 `HudItem`**（`src/canvas/HudItem.h`，全局命名空间；双外观 ThemeDefault=主题浅色 / DarkPill=深胶囊；`moveToPoint/placeAtScene` 自带 1/zoom 补偿 + 屏幕像素偏移）——禁止再手搭 QGraphicsRectItem+TextItem（M1 缩放失效根源），禁在场景单位里写"字体像素"尺寸；②**命中测试一律走 `HitTester.h`**（tools 层 header-only：`blockHitsAtScene` 一次 items() 出全部块命中，堆叠降序+去重+活动层过滤**唯一规则源**；`isBlankSpaceAtScene` 空白判定不分层）——新工具不得再复制 hitBlock 循环，活动层判定只认它；③**工具临时图元必须登记 `ManagedItems`**（`src/canvas/ManagedItems.h`：`own(item, &shadow)` 建点登记 + 影子绑定，`release(item)` 会话中重建型，`clear()` deactivate 统一释放）——禁止再写 `removeItem+delete+置空` 手写样板（M2 泄漏先例）。
- **工具生命周期收口（2026-12，TOOL_SYSTEM_AUDIT P2 落地）**：①**基类契约编译器保证**——Tool::activate/deactivate 非虚（src/tools/Tool.h：全量入口 activate(const ToolContext&) + 两参便捷重载 activate(scene, doc) 供测试直驱），派生工具只实现 onActivate/onDeactivate 虚钩子，**禁止再覆盖 activate/deactivate**（曾 5 个工具遮蔽 m_scene/m_paramDoc 且不调基类 = H4）；基类 protected 成员 m_scene/m_paramDoc/m_undoStack/m_hud/m_host 全工具共用，ToolRotate 的悬浮角度框已改名 m_angleHud（避免遮蔽基类 HudItem* m_hud）。②**上下文一次注入**：ToolContext{scene, paramDoc, undoStack, host} 替代旧三处散落 setter（Tool::setUndoStack 保留为 ToolManager::setUndoStack 的活更新通道；setToolSwitchRequest / ToolSelect::setEditTargetCallback 已删）；ToolHost 接口（requestToolSwitch / setEditTarget）由 ToolManager 实现——工具不认识管理器。③**实例常驻（L5）**：ToolManager 持 std::map<ToolType, std::unique_ptr<Tool>>，切换 = deactivate 旧 + activate 新；**重复点击当前工具 = no-op**（旧实现连按两次 R 清空旋转会话）；选区继承 = switchTool 内先抓 Select 选集再 deactivate（Select 实例常驻但其 deactivate 会清选集）。常驻实例下 onActivate 必须复位会话状态（Select 回单选 / SmartPen 回直线 / Rotate 回角度模式 / Intersection 走 resetState / Measure·AngleMeasure 清 snap 残留）。新增工具必须实现 onActivate/onDeactivate + 事件处理 + name()。
- **工具注册表 ToolRegistry（2026-12，TOOL_SYSTEM_AUDIT P3 落地）**：ToolType 枚举定义移入 `src/tools/ToolRegistry.h`（ToolManager.h 不再定义，仅 Tool.h 保留前置声明）；每个工具 `static ToolDescriptor describe()` 自持 { id, displayName(含 & 加速符), iconName(Phosphor SVG 名), shortcut(测量 M 让给画布长按故为空), hintText, factory }，`ToolRegistry.cpp` 构造时注册全部 9 个（含 PlacePoint；注册序 = 工具坞按钮序 = 菜单序）；MainWindow 遍历生成 QAction（`QHash<ToolType, QAction*> m_toolActions` + 注册序 `m_toolOrder`，替代旧的逐工具成员 / actionX 槽 / onToolChanged 平行 switch），工具坞 Ela 字体图标由 `MainWindow.cpp` 匿名命名空间的 `toolDockIcon(ToolType)` 提供（`QHash<ToolType, ElaIconType::IconName>` 键控查表 —— app 层视觉偏好，非工具元数据，故不进 ToolDescriptor）；**查不到 = 兜底 ArrowPointer + `Q_ASSERT_X`/`qWarning` 报警**。复核 N1：早先是 `std::array<…,8>` 配 `arr[static_cast<int>(type)]` 枚举序下标 —— 新增第 9 个工具会**越界读 UB 且编译不报错**，而且让"不再改 MainWindow"变成假承诺。ToolManager::ensureTool 改 `ToolRegistry::instance().create(type)`（保留 P2 常驻实例池）；**`~ToolManager` 先 deactivate 激活中的工具**（常驻实例下清理点已迁到 deactivate，否则退出时激活工具的 onDeactivate 从不执行）；`toolHintText(ToolType)` 退化为查 registry 的薄封装。**新增工具 = 1 对 .cpp/.h + describe() + ToolRegistry.cpp 1 行注册 + `toolDockIcon` 补 1 项图标**（漏补不崩，只报警 + 降级箭头图标）。`registerTool<T>()` 带重复注册断言（重复注册会在注册序塞重项 → 生成重复按钮）。
- **复核收尾（2026-12，TOOL_SYSTEM_AUDIT 剩余项落地）**：①**AngleHud 已随 CONTEXT_STRIP 二期删除（2026-12）**——连接手势的角度输入并入 ContextStrip（会话模式, 见「连接角度会话」条目），其 L7 教训（悬浮控件颜色不得沿父链反查、构造时解析一次 CanvasStyle）已无宿主，tools 层再无 QWidget；`ToolHost::setConnectAngleSession/setConnectAngleValidity` + `Tool` 虚钩子 `connectAngleTextChanged/ModeChanged/Committed/Cancelled`（默认 no-op，ToolSelect 转交 ConnectGesture）构成新通道。②**旋转 HUD 随视图重定位**（M8）：ToolRotate 挂 CanvasView::zoomFactorChanged + 双滚动条 valueChanged → repositionHud()；ToolRotate 非 QObject —— 必须 QObject::connect/disconnect 限定名，sender 对 CanvasView 成员信号要 static_cast<CanvasView*>；连接存成员（m_view*Conn），onDeactivate 断开防悬垂。③**非模态弹窗阻塞反馈**（M10）：智能笔 QuickAuxDialog/省道弹窗打开期间画布光标 = ForbiddenCursor + toast「请先完成…」（showDialogBlockedFeedback()，文案同值守卫防 mouseMove 每帧刷屏）；事件早退路径调用以兜底维持；弹窗 finished / deactivate 时 clearDialogBlockedCursor() 恢复。④**工具坞无禁用态**（M3 复核）：ToolDockStyle 已删 disabled 分支与注释（全仓工具按钮永不禁用）；未来若引入"禁用某工具"需重建 enabled 分支。⑤**M4 图标落地**：bezier-curve.svg（曲线）/angle.svg（角度测量）入 qrc 并接入 describe().iconName（angle.svg 渲染后重心 cy=167 偏下 39px，已上移 24px 调至 cy=143；换图前可用 build/icon_render 的 Qt 渲染器做像素校验）。⑥**L6**：ToolSelect 右键守卫冗余末项 cands.size() < 2 已删（与 overlapMenu == nullptr 等价）。
- **连接角度会话（CONTEXT_STRIP_DESIGN.md 二期，2026-12 交付）**：连接手势的角度输入并入 `ContextStrip`（`src/ui/AngleHud` 已删除）。**条带是纯输入面，连接语义全在 `ConnectGesture`**（预览/换算/收尾/整步 undo）——通道四条：①手势→条带：ConnectGesture 注入 `m_beginAngleSession` 回调（ToolSelect::onActivate 接线）→ `Tool::reportConnectAngleSession` → `ToolHost::setConnectAngleSession`（ToolManager emit `connectAngleSessionChanged`）→ MainWindow → `ContextStrip::beginConnectAngleSession(blockId, segId, attId, initialAngle)`，**全 null = 会话结束**；②合法性：`ToolHost::setConnectAngleValidity` → 条带角度框 `angleInvalid` 属性（Theme QSS `QLineEdit#angleEdit[angleInvalid="true"]` 红边）；③条带→手势：ContextStrip 四信号 `connectAngleTextChanged/ModeChanged/Committed/Cancelled` → ToolManager `forward*` → `Tool` 虚钩子（默认 no-op）→ ToolSelect override → ConnectGesture::onAngleTextChanged/onAngleModeChanged/commitAngle/cancelAngle（已移 public）。**铁律**：会话内条带**绝不 push 命令**（applyName/applyLength/applyAngle/onUnitToggled/onReverseClicked 全部 `m_connectSession` 守卫——连接在 finalizeConnection 前是飞行中附件，SegmentEditBarCommand/ReverseSegmentCommand 插进手势宏之间会乱 undo）；会话期间名称/长度只读、角度可编辑（refreshChrome 会话分支强制，普通 isBridge 分支会锁回角度框）；组件级连接 fromBlockId 为空 → `findEditAttachment` 会话分支按 `attachmentsView().byId(m_connectAttId)` 直取；**°/⌒ 切换 = 数值字段几何保持换算，公式驱动也允许切换且公式原样搬移（2026-12 用户两轮拍板：先废"公式驱动拒绝切换"，再定"公式绝不乘换算系数"）**——`followerModeSwitchValues`（FollowerAngle.h）返回 `FollowerModeSwitchResult{angle, arcMm, angleFormula, arcFormula}`，切换只改 rotationMode + 源公式字段原样赋给目标公式字段（followerAngleFormula ↔ arcLengthFormula 文本不变，`b/4` 切到弧长后仍 `b/4`、按弧长语义求值；"一个表达式只会用一种模式表达"）；数值字段仍做几何保持换算（无公式时 45°↔47.12mm 一致）；属性卡 `SegmentAngleCard::onModeToggle` / 条带锁定态 `onUnitToggled` / 连接会话 `ConnectGesture::onAngleModeChanged` 三处共用（**条带旧内联换算不读公式求值会拿到陈旧存储值，勿恢复**）；**状态栏模式显示铁律：° /⌒ 必须是原生 QPushButton + chipButtonStyle（ElaPushButton 自绘无 isChecked 分支、选中态不可见）+ QButtonGroup 互斥，徽标追加"· 角度/· 弧长"**；**输入锁定只认真桥线 `block->isBridge`（长度公式非空不算桥，输入变量不锁输入）**；会话 Esc/Enter 走 eventFilter 最前分支 emit 信号，**绝不走 clearPinned/cancelRequested**；`setPinnedTarget` 会话期间直接 return（会话独占显示）；hideBar 清会话标志（工具切换两路幂等）。跟随线段 = `blk->exitSegmentAtPoint(m_connectFromPoint)`（组件级 = 被抓成员块）。测试：test_context_strip +6 例、test_select_wkey +1（击键预览+°⌒+Enter 整步 undo）+ 会话上报断言 2 处、test_component 2 处改会话断言、test_rotate_copy 删 angleHudShortcutOverrideContainment。
- **约束类型分派点登记表**（ParamPoint.h）：新增/修改 PointConstraint 枚举必须逐层同步 13 处（数据定义、Block::resolve exhaustive switch、resolveInterpolatedPoints、曲线宿主判定、Resolver 交点 step、blockReferences 脏传播、degradeOrphanedIntersections、BlockItem 渲染、ToolSelect 交互、ToolCurveEdit 交互、BlockCommands 降级恢复、DocumentSerializer 表驱动字符串映射、Duplicate；权威清单见 `src/parametric/ParamPoint.h:39-51`）。遗漏 blockReferences 或序列化映射不会编译报错，只会静默损坏。**序列化字符串映射已表驱动（2026-08-28 收口 B4）**：`DocumentSerializer.cpp` 四组枚举映射（PointConstraint/SegmentType/SegmentRole/LineStyle）为 `constexpr std::array<std::pair<Enum,const char*>>` 表 + `enumToStr/enumFromStr` 双向查找（首行 = canonical default），str→enum 与 enum→str 同表必然同步，新增枚举只改一处表。
- **删除影响报告**：新删善后分支必须同步更新 `deleteImpactReport` 与测试（九项计数：attachmentsRemoved/bridgesReleased/intersectionsFrozen/intersectionsAimCleared/linkedFrozen/linkedVarsRemoved/measureVarsRemoved/angleVarsRemoved/formulasBroken，权威定义 `src/parametric/ParamDocumentDetail.h:8-17`；`dartLinesDegraded` 随省道线下线已删）。
- **卡片抽取范式**：子卡片持 doc 指针 + 目标 id，setTarget/refresh 双入口，模型变更经 `changed(ChangeKind)` 信号回报对话框，对话框只做联动刷新。
- **卡片基类 CardBase（2026-08 抽取）**：五张虚拟列表卡片（VariableCard/FormulaCard/LinkedCard/MeasureCard/AngleMeasureCard）继承 `src/ui/CardBase.h`，共用左侧竖线 paintEvent（`accentBarX()` 虚函数，FormulaCard 组内右移 kGroupIndent）、悬停删除按钮+同尺寸占位槽（`appendDeleteButton`，20×20 防跳动契约）、序号标签（`setIndex` + `indexText()` 虚函数：默认 "N" / 测 "测 N" / 角 "角 N"）、名称 chip、等宽值标签（`createValueLabel`）、🔒 只读图标（`setupLockIcon`）、dangling 样式翻转（`setValueLabelDangling`）与每帧值刷新守卫（`refreshValueGuard`，MeasureCard 额外带 kind 守卫）。数值格式化收口到 `geometry/Units.h`：`formatCm`/`formatLength`/`formatDegTrimmed`/`formatNumberTrimmed`/`trimTrailingZeros`（`formatCmTrimmed` 已于 2026-12 更名为 `formatCm`）。**注释回填也收口（2026-08-28 B5/A5）**：五卡注释同步统一走 `CardBase::setCommentSilently(text)`（QSignalBlocker + hasFocus 守卫，五卡共用；原来五卡各自复制 blockSignals 三行）；FormulaCard 实际值回显用 `Units::formatNumberTrimmed`（去尾零，2026-12 拍板后该处曾漏改）。**改卡片骨架先改 CardBase 再改派生**；indexLabel 固定 objectName（varIndex/cardIndex/linkedIndex/measureIndex/angleIndex）是测试查找契约，勿改。
- **角度工具收口（2026-08）**：存储域归一化 = `cad::geo::normalizeDeg360`（[0,360)，原 alphaFromSignedFold）、带符号折角显示 = `cad::geo::normalizeDeg180`（(−180,180]，原 signedFoldDeg，逐字节等价）、角度显示格式化 = `cad::geo::Units::formatDegValue`（无后缀）/`formatDegTrimmed`（带 °）——统一实现在 `src/geometry/Angle.h`/`Units.h`，工具层（ToolRotate/ConnectGesture/SegmentConnectionCard）不再本地复制。改角度约定只改这两处头文件。**收口明细（2026-08-28 CLEANUP_REFACTOR_DESIGN.md A1/A3 落地）**：全库手写 `fmod(x,360); if(<0)+=360` / `while(>180)-=180` 副本已清零（SegmentEditBar 私有 normalizeDeg 删、SegmentAnchorTab 相对切线角、SegmentAuxTab/LineFactory/RotateCopyGesture/BreakCommands/ToolRotate/ToolSmartPen 显示角等全部改调几何函数；-180° 边界统一 Angle.h 语义 `<=\-180`，SegmentAnchorTab 原 `< -180` 分叉已收敛）；弧长↔角度换算收口 `cad::geo::arcMmToDeg(arcMm, radiusMm)`/`degToArcMm(deg, radiusMm)`、直线弦长/开度换算收口 `cad::geo::chordMmToDeg(chordMm, radiusMm)`/`degToChordMm(deg, radiusMm)`（Angle.h）+ 三模切换写回收口 `cad::param::followerModeSwitchValues(att, radiusMm, targetMode, params, cond)`（FollowerAngle.h，SegmentAngleCard::onModeToggle 与 ContextStrip::onUnitSelected 共用；开度端点位置保真，公式原样搬移）。
- **数值显示格式收口（2026-12，审计 P1-6 / §3.6 落地）**：同一物理量只允许一种显示精度，换算 + 格式化一律走 `src/geometry/Units.h`，调用点禁止裸 `QString::number(x, 'f', N)` / `QString::asprintf("%.Nf")` / 内联 `/ 10.0`、`* 10.0`（唯一豁免：`Units.h` 自身格式化函数体、`src/canvas/BlockItemPainter.cpp:287-288` 的 0.1 mm 网格去重键）。**长度**：`Units::formatCm(mm)`（2 位小数去尾零、无单位后缀，用于数值框/卡片值标签）/ `Units::formatLength(mm)`（= formatCm + " cm"，用于句子/提示/量测结果）——`formatLength` 刻意无 precision 参数（同一物理量不得有两种精度）；旧名 `formatCmTrimmed` 与死代码 `formatAngle` 已删（CAN-P1-18）。**角度**：`formatDegValue`（1 位去尾零、无后缀）/ `formatDegTrimmed`（带 °）。**坐标**：`formatPoint` 固定 2 位不去尾零（状态栏读数宽度稳定，刻意例外）。
- **旋转手势角度域收口（2026-12，审计 P0-4 落地）**：旋转手势里的「角度」是两个不同的物理量，此前同一姿态在 HUD 显示两个数字（连接段 −90.0°、自由段 270.0°），且写库分支按 rotationMode 各自选域。现收口为**一写一读两个唯一入口**：①**写** = `cad::param::writeFollowerAngleForMode(att, mode, rawDeg, radiusMm)`（`src/parametric/FollowerAngle.h`，手势原始角 → 连接存储字段的唯一入口）——Angle 存 `followerAngleToStorage`（[0,360) 存储域，多圈值合法、不折叠）、ArcLength 用 360 域经 `degToArcMm` 换算（弧长带符号，可区分 θ 与 360−θ）、ChordLength **必须**用 `followerAngleToDisplay` 折角域经 `degToChordMm`（弦长 2r·sin(θ/2) 无法区分 θ 与 360−θ，270° 与 90° 弦长互为相反数）；`src/tools/RotateSession.cpp` `applyAngleDeg` 的三分支已合并为单次调用，新增手势写角度一律走本入口。②**读** = `cad::tools::formatRotationBadge(deg, RotateBadgeQuantity)`（`src/tools/RotateDragMath.h`，HUD 徽标唯一入口），**域由物理量决定而非由连接状态决定**：`Delta`（多选/框选）= 相对起始姿态的旋转量，带符号、不折叠（旋转量不是姿态角）；`Pose`（单线段，连接或自由）= 姿态角，一律显示域 (−180,180]。状态栏提示的「角度」同走折角域（`src/tools/RotateHintTexts.cpp`）。守卫 `tests/test_rotate_angle_domain.cpp`（写读一致 + 270°/−90° 回归锁）；徽标文本此前无任何测试覆盖，改角度域必须先改这两个入口。**2026-09 统一（S1-S5，用户拍板 D1/D3）**：显示侧收口升级为 `cad::geo::toDisplayDeg(deg, AngleDisplayRole)`（`src/geometry/Angle.h:67` 枚举 `{WorldDirection, FoldPose, DeltaAmount}`、`:71` 函数；World→[0,360)、Fold→(−180,180]、Delta→带符号不折叠）——**凡「世界方向」物理量一律 [0,360)**，禁止各自调 normalizeDeg180/360（Angle.h:65 注释即纪律）。状态栏提示的姿态域由 `RotateHintSnapshot::poseRole` 携带（`src/tools/ToolRotate.cpp:779-781`：复制→DeltaAmount / 连接→FoldPose / 自由→WorldDirection），`ContextStripDisplay.cpp:86/:113` 基准角度框与角度框统一走 WorldDirection（已删本地 normalizeDeg180 与 `worldDeg += 180.0`）。画布徽标仍走 `formatRotationBadge`（物理量选域不变）。
- **几何数值容差收口（2026-12，审计 P1-4 落地）**：几何/参数域比较容差一律取 `src/geometry/Epsilon.h`（命名空间 `cad::geo`）——`kGeomEpsLoose = 1e-6`（参数域 t/s、投影包含、点去重）、`kGeomEps = 1e-9`（默认长度/坐标）、`kGeomEpsUltra = 1e-10`（弦长/半径守卫）、`kGeomEpsTight = 1e-12`（平方长度/叉积行列式）、`kGeomEpsSq = 1e-8`（长度²/相对缩放细尺度）；比较平方量时该常量本身即平方阈值，勿再平方。**刻意不入表**：缩放守卫（`zoom > 1e-9`）、显示去抖、有限差分步长、以「度」为单位的容差、张力下限（`CurveMath.cpp` 的 `kMinTension`）。
- **角度常量收口（2026-12，审计 P1-5 落地）**：圆周率与角度换算一律走 `src/geometry/Angle.h` 的 `cad::geo::kPi`/`degToRad`/`radToDeg`，源码禁止 `M_PI`、`std::numbers::pi` 与手写 `* 180.0 / pi`、`* pi / 180.0`（4 处本地 `#ifndef M_PI` 宏已删）。
- **交互像素容差收口（2026-12，审计 P1-3 / P1-9 / P1-16 落地）**：所有屏幕像素半径与点击/拖动阈值统一取 `src/tools/InteractionTolerances.h`（命名空间 `cad::tools`），调用点禁止裸数字或本地 `constexpr` 副本。**线身吸附 = 画布悬停半径**：`kBodySnapRadiusPx` 是 `src/canvas/CanvasStyle.h` 的 `kHoverRadiusPx`（8px）的**别名**（canvas 层是唯一数值来源，canvas 不得反向依赖 tools）——「高亮什么就吸附什么」；`SnapEngine::findSegmentSnap` 走 `segmentSnapRadius`（= kBodySnapRadiusPx），不再借用点吸附的 `snapRadius`。**点/端点吸附** = `kPointSnapRadiusPx`（12px，端点目标小、抓取放宽）；`SnapEngine::snapRadius` 即此值。**点击 vs 拖动**唯一判定 = `isDrag(delta, zoom, selectionEstablished)`（阈值 `kDragThresholdPx` 5px / `kDragThresholdSelectedPx` 10px）；ToolRotate 框选释放的 3px 点击容差已并入 5px（同一物理量不得两值）。**框选资格 = 点选资格**：`src/tools/HitTester.h` 的 `isInteractiveBlock`（活动层 + 非影子）是唯一规则源，`MarqueeGesture`/`ToolRotate`/`MultiRotateSession` 不得再各写 `layer != activeLayer` / `!isBridge` 近似判定（TOOL-P1-12）。连接手势两半径（`kConnectSnapRadiusPx` 7.5px 落点 / `kConnectGrabRadiusPx` 10px 源端点抓取）见 DECISIONS.md。
- **缩放守卫收口（2026-12，审计 U11 落地）**：像素↔世界单位换算的除零守卫唯一出处 = `cad::canvas::safeZoomOr(double zoom)`（`src/canvas/CanvasStyle.h`：`zoom > cad::geo::kGeomEps ? zoom : 1.0`，覆盖 0/负/未初始化）与 `CanvasScene::safeZoom()`（`src/canvas/CanvasScene.h`，= `safeZoomOr(currentZoom())`）。工具层禁止再写 `m_scene ? m_scene->currentZoom() : 1.0`、`zoom > 1e-9 ? zoom : 1.0`、`if (abs(zoom) < eps) zoom = 1.0;` 这类本地守卫（约 50 处已迁）。刻意例外：`src/app/MainWindowSegmentMenu.cpp` 的 1e-4 阈值（语义不同）、`HudItem.cpp`/`OverlapBatteryHud.cpp` 读 `view->transform().m11()` 的 abs 守卫（负缩放 = 镜像视图）、`CurveAnchorDragSession.cpp`/`ToolCurveEditHandles.cpp` 的 `std::max(zoom, kGeomEps)`（除零钳制而非守卫）。
- **文档变更门面（2026-12，审计 U3 落地）**：命令类禁止手写 `touchGeometry() + resolveAll()` / `resolveAll() + emit structureChanged` 样板——改用 `ParamDocument::touchAndResolve(const QUuid& blockId)`（找块→touchGeometry→resolveAll；块不存在仍 resolveAll）与 `ParamDocument::commitRawChange()`（resolveAll + emit structureChanged），实现于 `src/parametric/ParamDocumentResolver.cpp`。条件式 touch（`SegmentPropertyCommands.cpp`）保持原样。
- **射线求交唯一入口（2026-12，审计 U6 / PAR-P0-3 落地）**：射线 × (线段|贝塞尔 span) 一律走 `cad::geo::raySegmentOrCurveIntersect(origin, dir, segStart, segEnd, spans, bidirectional)`（`src/geometry/CurveMath.h`，返回 `RaySegmentHit{hit, point, s, t}`；s = 沿射线参数、t = 直线段参数[0,1] 或曲线全局 span T）。`BlockResolve.cpp` 与 `ResolverIntersection.cpp` 的重复实现已删；跨块版统一在块局部坐标求解（`transform.toLocal` / `effectiveLocalPos`，Transform2D 是刚体正交变换）。
- **点在线段上的参数（2026-12，审计 U5 落地）**：投影参数一律 `Vec2::closestParamOnSegment(p, a, b)`（`src/geometry/Vec2.h`，退化段返回 0.0）；点线距离用 `Vec2::distanceToSegment`。手写 ax/ay/lenSq 样板已清零（CanvasView / BlockItemPick / SnapEngine / ParamDocumentAttachments / CurveMath）。
- **角度输入解析（2026-12，审计 UI-P0-7 落地）**：输入框角度文本判读一律 `cad::geo::parseAngleText(const QString&)`（`src/geometry/Units.h`，紧跟 `parseNumberOrFormula`）——数值判读前剥度数符号 `°`(U+00B0)，公式原文保留。**显示侧带 ° 的 `formatDegTrimmed` 必须与输入侧成对**：`SegmentConnectionCardConn` 曾用裸 `toDouble()` 读回 `formatDegTrimmed` 回填的 "5°"，回车被判公式并静默回滚。长度等其他量仍用 `parseNumberOrFormula`。
- **旋转读数物理量（2026-12，审计 UI-P0-1 落地）**：旋转 HUD 读数按物理量选域，唯一入口 `cad::tools::formatRotationBadge(deg, RotateBadgeQuantity)`（`src/tools/RotateDragMath.h`）：`Delta` = 本次旋转增量（|deg|≤0.01 显示空串）、`Fold` = 折角域 `normalizeDeg180`、`World` = 世界方向域 `normalizeDeg360`；选域在 `computeGizmoPose`（多选/框选与复制手势 = Delta、连接段 = Fold、自由段 = World），与 `RotateSession` 写库分支一致。**2026-09 统一（S1-S5）**：`computeGizmoPose` 的 `GizmoPose` 字段 `refBaseRad` 更名 `startPoseRad`（`src/tools/RotateDragMath.h`）——黄虚线 = 起手姿态、黄弧 = 起手→当前，跨度一律 `normalizeRad(currentPoseRad − startPoseRad)`；徽标物理量选域不变。**姿态角是唯一角度真相**：`GizmoPose` 不再有 `deltaDeg`，`RotateGizmo::update(zoom, startPoseRad, currentPoseRad, badgeText)` 为 3+1 参、`isArcEmpty()` 由姿态差判定（`src/tools/RotateGizmo.cpp`）；`GizmoPoseInput::baseAngleDeg` 一并删除（S5 清债）。文字侧显示域走 `toDisplayDeg`（见上条）。
- **数值输入判读与规格收口（2026-12，审计 UI-P1-8 / UI-P1-9 / CAN-P1-17 / N5 落地）**：①**判读**——输入框文本一律走 `cad::geo::parseNumberOrFormula`（无单位/长度）或 `parseAngleText`（角度，剥 U+00B0），禁止两段式 `bool ok; text.toDouble(&ok)` + `!text.isEmpty()` 判公式；已收口 AuxPointForm / IntersectionForm / LineGeometrySection（滑轨偏移）/ PlacedPointDialog / QuickAuxDialog / **PlacedPointStripBar**（放置点条带与线段条带同权：公式实时求值预览、提交写回 `interpOffset*Formula`，回显公式优先）；②**规格**——QDoubleSpinBox 的「精度 + 后缀 + 步长」唯一出处 = `src/ui/NumericFieldSpecs.h`（`kLengthCmSpec` 2 位 + " cm"、`kAngleDegSpec` 1 位 + "°"、`kWeightPxSpec` 1 位无后缀、`applyNumericSpec`），**范围留在调用点**（领域约束：切线长度 ≥ 0、变量值 ±99999）；③自由文本输入框**不装** `QDoubleValidator`（会挡掉公式字符）。
- **文案唯一出处（2026-12，审计 N4 落地）**：命令/动作文案分两层收口——① `src/document/CommandTexts.h`（`cad::cmd::texts`，18 个）供 document/commands 与上层 ui/tools/app 共用（ui 层不能被反向包含，共享文案只能落下层）；② `src/ui/UiStrings.h`（`cad::ui::str`，41 个）只收 ui/app/tools 内部重复文案。新增/改文案只改这两处，禁止再写裸 `QStringLiteral("…")` 副本；同文件内复用的错误文案（ExpressionEvaluator / DocumentFile / LinkedVariable）用文件内匿名命名空间常量。
- **跨层连接反馈收口（2026-08）**：`crossLayerToast`（QUuid 层/Block 双重载）+ `crossLayerBadge` 统一在 `src/ui/LayerFeedback.h`（header-only inline，**命名空间 `cad::ui`**，2026-12 P2-4 随 UI 组件从 tools/ 迁入；现进 gcad_ui 源列表），LineFactory/ConnectGesture 里改以 `cad::ui::crossLayerToast` 调用，SegmentConnectionCard（同层）直接无修饰调用（SegmentAuxTab 调用点已随「点连接」只读标签页删除——2026-08 用户拍板，属性页连接分区/端点摘要覆盖其内容）。改文案/跨层语义只改这一个头文件。
- **工具表单群统一骨架 + 圆角纪律（2026-08-28，ui-redesign §4.5/§5.5/§07 P2 落地）**：①共享骨架收口在 `src/ui/FormScaffold.h`（header-only inline，gcad_ui）——`makeFormGroupHeader`（等宽 10px 大写代号 + 中文标题 **text1 墨字 12px 600** + 底部发丝线，替代裸粗体分组——中文原按设计稿用 text2 11px，2026-08-28 用户反馈「掉色感」后加深一档，代号保持 text3 标注感）、`applyFormGrid`（QFormLayout 标签列 88px 右对齐、行距 10px，与线条属性对话框同栅格语言）、`makeFormButtonBar`（surface3 底 + 顶部分隔线，按钮右对齐、主操作最右 = 实心 accent 陶土 `#CC785C` + onAccent 字）、`makeFormTitleBar`（代号标题栏，**ElaDialog 自带 Fluent 标题栏时勿用——双重标题**，仅供无边框容器）；已接入 QuickAuxDialog（辅助点）、MeasureResultDialog（测量结果，读数组值 = FontLg 15px）、AuxPointForm + IntersectionForm（88px 栅格）；表单对话框颜色构造时烘焙（生命周期短，exec 即销毁）。②`RadiusPill` 更名 **`RadiusBadge`**（Theme.h，值恒 4px——「胶囊」命名与禁胶囊纪律自相矛盾）；全库 >4px 圆角实例清理：ConditionDialog 条件行 6→4、ComponentTab 组件卡 6→4、LayerPanel 层卡 7→4/折叠钮 9→4/计数 pill 8→2/加层钮 5→4、AngleHud 8→4（AngleHud 已随 CONTEXT_STRIP 二期删除）；**新增 chrome 圆角勿超 4px**。③**输入框与微调框纯胶囊纪律（2026-09 用户拍板）**：全项目所有输入框（`ElaLineEdit` / `PointRefEdit` / `CopyChip` / `CompoundChip` / `ElaDoubleSpinBox` / `ElaSpinBox`）统一为**无底边横杠的纯胶囊输入框**（两端半圆 `r = height / 2`）；彻底剔除 Fluent 静态底边阴影线（`BasicHemline`）与聚焦下划线展开动效（`pExpandMarkWidth`）；聚焦统一采用全包围高亮边框（`PrimaryNormal` 1.5px）；底层由 `third_party/elawidgettools_qt69_patch.cmake` Part 6 补丁收口保障，新输入框禁止再手写方形 QSS 边框。
- **卡片竖线色/字号接入 ThemeTokens（2026-08-28 方案 A 重拍板，ui-redesign-2026-08 §2.5）**：卡片左竖线 = **类型色**（变量=piece1 碳灰 `#383530` / 公式=piece2 深青 `#2E6B65` / 测量(含角度)=piece3 陶土 `#C46849` / 关联=piece4 钴蓝 `#3D5A80`；组件管理行也用 piece1，ComponentTab.cpp；值见 `src/ui/Theme.cpp:84-87`），由 `CardBase::setAccentRole(cad::ui::CardAccent)` 驱动（paintEvent 现读 token，主题切换免重绑）——**旧蓝/橙交替 `rowEven/rowOdd` 已删除**（用户 2026-08-28 拍板方案 A，替代同日早先的交替拍板；setAlternate 保留为虚拟列表 bind 契约 no-op）。暗色模式 piece 家族 = 同色相提明度（piece2 → `#4D9088` / piece4 → `#6E88A8`，`src/ui/Theme.cpp:123-126` dark()，「公式=青」跨模式记忆一致）。字号阶梯：`FontXs 10 / FontSm 11 / FontMd 12 / FontBase 13 / FontLg 15（卡片数值读数 15px Semibold+Mono，卡片第一焦点）/ FontXl 18（空状态主文案）`。悬停卡片描边转 borderStrong（§6.2）；删除钮占位槽默认画 24% 透明 ✕ 轮廓（GhostDeleteSlot，防「空白→出现」跳变）；dangling = 值 danger 字 + danger 8% 浅底 + ⚠ badge（CardBase::appendDanglingBadge）。散落的 accent 硬编码（图标/光环/fx 标签/角色色）已收口到 `Theme::tokens().accent`。画布侧 select 色仍在 `CanvasStyle.h`（canvas 层不依赖 ui，双 token 手动同步）。测试契约：test_formula_groups cardOrdinalsAndAlternate 已改为类型色断言。
- **Qt 性能**：每帧同步槽里禁止 setStyleSheet（re-polish 数毫秒级），只在离散状态翻转时调用；setText 做同值短路；批量操作禁用布局避免 O(N²)。
- **性能断言抗噪声**：Debug 构建性能波动 30%+，断言用宽松边界（如 ≤2.0x），勿用严格排序；验证算法优化要构造最坏场景（逆序深链）而非常见微差。
- **测量工作流**：测量任意两点发布 MeasureVariable（refName `M_xxx`，cm 域）；测量工具内 W 键循环 距离/水平/垂直 三模式（`MeasureKind`，ToolMeasure.cpp cycleKind；水平=|dx|、垂直=|dy|，求解刷新按 kind 计算于 MeasurementStore.cpp measureMeasureVars，序列化字段 "kind" 缺省=Distance 兼容旧档）；水平/垂直模式下两点在测量轴重合（dx/dy < 0.05mm）时第二击拒绝创建（toast 提示，停留在 SelectB）；测量卡值标签按模式加前缀（水平/垂直，普通距离无前缀）；"烘焙到操作层"=复制非移动（lengthFormula 活链接）；跨层附着单向（仅辅助层 follower、工作层 leader）；值循环预检只在 addAttachment 入口拦截；角度测量严格禁止跨图层（方案 A，点选第二条线非同层拒绝并 toast 提示）；角度测量基于相交点与用户光标点选位置推导射线方向（记录为 flipA/flipB，持久化进 AngleMeasureVariable），支持 T 型与十字相交线所见即所得测量且求解后稳定不跳变。
- **公式引擎符号集（2026-11 扩展，ExpressionEvaluator.cpp）**：支持 `+ - * / ^`、一元 ±、括号（含全角/方括号/花括号归一化）、小写函数与任意名字变量（含中文）。单参函数 `cos/sin/tan/sqrt/abs/atan/asin/acos/floor/ceil/round`（可裸参形式 `cos60`/`sqrt2`/`cos前肩角度°`），双参函数 `atan2(y,x)/pow(a,b)/min(a,b)/max(a,b)`（必须括号+逗号）；**三角函数参数为度制，反三角函数结果为度制**（制版约定）；`^` 右结合、优先于一元负号（`-2^2==-(2^2)`）；小写 ASCII 保留给函数（大写引用名 B/MA_xxx 永不冲突；`cosine`/`cos_a`/`min2`/`atan2x` 等仍按变量名处理）；无 `√` 字符，统一用 `sqrt(...)`；域错误（sqrt 负数、asin/acos 越界、除零、非有限结果）返回错误而非 NaN。测试：tests/test_expression.cpp。

---

## 验证命令（全文）


```bash
ctest -C RelWithDebInfo -R <test名>   # 【日常首选·秒级】只跑受影响的特定单测（如 ctest -C RelWithDebInfo -R test_select_wkey）
ctest -C RelWithDebInfo               # 【收尾验收专用】跑全量 64 个用例（56 功能测试 + 8 守卫，耗时 1~2 分钟，仅在交付收尾时执行）
```

- 统一构建与验证目录为 `build/out-reldeb`（RelWithDebInfo 模式，开启 /O2 优化 + 保留 PDB 符号表，1200 FPS 极速运行，消除 Debug 模式拖拽卡顿）。历史目录 `build/out`（Debug）、`build/out-rel`（Release）仅由 `tools\build.bat debug` / `release` 分支在特殊排查时写入，日常禁用。
- **【构建与测试硬红线】日常迭代严禁无脑全量构建与全量测试**：
  - 日常修改代码验证主程序：`tools\build.bat WildWindPattern`（~0.3s，跳过全部 56 个测试的重链）；
  - 日常调试单测：`tools\build.bat <test名>`，随后单跑 `ctest -C RelWithDebInfo -R <test名>`；
  - 只有在任务彻底完工、交付收尾时，才允许执行一次全量构建与全量 `ctest`。
- ctest 共 **64** 个用例（56 个功能测试 + 8 个自动化守卫；测试 exe 共 59 个——另有 test_realdoc_perf/test_realdoc_full/test_nav_smoke 不进 ctest）；原 `test_commands` 已拆分为 `test_block_commands` / `test_attachment_shadow` / `test_attachment_slide` / `test_attachment_angle` / `test_variable_layer_commands` / `test_reverse_segment_commands` 6 套；**既有基线红 = 0（2026-09-02 修复两条基线红：test_serializer::bridgeAuxPointSnappableAndAttachable = Resolver Step 5 桥跟随者重解被 205a229 删除未恢复；test_component::dragComponentLeaderCurveFollowStable 31.4798mm = followResettle 后缺组件沉降，见 TROUBLESHOOTING 第 5 组）**；**环境漂移红已消除（2026-09 复核）**：test_intersection_update 8 个用例已全部改用合成档 `CrossLayerDoc`（tests/test_intersection_update.cpp:12,38,253-260），不读 `E:/3.gcad`、无 QSKIP；test_extend 原 `savedDocFormulaStartExtendRenders` 已改名 `formulaStartExtendEvaluatesAndMovesStart`（tests/test_extend.cpp:737，同步改合成档）；**test_hold_show 已根治（2026-09-04，不再列入漂移红）**：真失败 = `renderNonWhitePixels` 隔行/隔列采样在 100% DPI 把 10px 长度标签（约 161 物理像素）测成 36，低于按 125% DPI 校准的 50px 阈值 → `forceLengthRevealsLengthLabels` 假红；「空输出 exit 1」是 Qt 6.11.1 debug 版全局现象（连纯 test_curve/通过的测试也 0 字节），非失败信号，判据与修复见 TROUBLESHOOTING 第 5 组；修复后 7 用例全绿（tests/test_hold_show.cpp:76-86）；**GUI 时序抖动**：test_dialog_tabs_switch::switchBackAfterTyping（interpPercent 断言）/ test_aux_layer / test_rotate_copy_* / **test_select_wkey** / **test_component::junctionComponentConnectGesture + componentConnectOverlapSwitchTarget** 在整批 ctest 压力下偶发失败，单测复跑即过，勿误判回归。**test_canvas_perf 的 singleCurveFrame 曾为 Debug 段错误存量问题**（疑似环境/软渲染），当前 Ninja Debug 下通过；复现时用基线对照法排查，勿误判回归（详见 `TROUBLESHOOTING.md` 第 5 组）。
- **按影响面选测试（2026-12 用户拍板，替代"默认全量"）**：`ctest` 全量是**收尾/跨模块大改**的最终验证，**不是每个任务必跑**。日常按改动影响面选相关测试，测试文件增多后全量耗时不可接受。分级约定：
  - 纯 `geometry` 数学（Vec2/Units/Angle/CurveMath）→ `test_curve` + `test_expression`；
  - `parametric` 引擎（Resolver/Block/ParamDocument/命令）→ `test_resolver_points` / `test_resolver_attachment` / `test_resolver_curve_arc` / `test_resolver_diag_misc` + `test_block_commands` + `test_attachment_shadow` / `test_attachment_slide` / `test_attachment_angle` + `test_variable_layer_commands` + `test_reverse_segment_commands` + `test_serializer` + `test_migration`；
  - 序列化/磁盘格式 → `test_serializer` + `test_migration`（见下方 P2-1 条目）；
  - 工具/UI 交互 → 对应 `test_select_wkey` / `test_rotate_copy_*`（semantics/shadow/endtarget/flow/anchor/strip/d15_gate/pivot_multi）/ `test_context_strip` / `test_dialog_tabs_*`（switch/angle_conn/aux）等；
  - 跨模块大改 / 任务收尾 → 全量 `ctest`。
  单测跑法：`ctest -C RelWithDebInfo -R <test名>`（如 `ctest -C RelWithDebInfo -R test_resolver_points`）。**判定回归时仍以全量基线为准**（见下条 2 红基线 / 环境漂移 / GUI 时序抖动，勿把已知红误判为本次改动引入）。
- 不进 ctest 需手动跑：test_realdoc_perf、test_realdoc_full（env `GCAD_DOC=<.gcad路径>`，可叠加 `GCAD_PROFILE=1`）、test_nav_smoke（ElaWindow 导航栈冒烟，无 env）。
- **静态分层检查（2026-12 P1-6）**：`python tools/check_layering.py`（无参数、零依赖、秒级）。扫描 `src/<module>/**` 的 include，凡指向更上层模块即报错并 exit 1。当前状态 = 干净；**新增跨层 include 前先跑它**（2026-09 起已进 ctest 为 `check_layering` 测试，全量 ctest 即覆盖；CI 亦接入）。
- **硬编码颜色守卫（2026-08-31）**：`python tools/check_hardcoded_colors.py`（无参数、零依赖、秒级）。样式纪律 = 所有控件颜色必须来自 `ThemeTokens`（ui 层）/`CanvasStyle`（canvas 侧镜像），写死 `#RRGGBB` 会暗色模式失效 + 主题切换静默丢色。脚本扫 `src/` 非 token 表的 hex：四张 token 表文件豁免（Theme/CanvasStyle 的 .h/.cpp）、行尾 `//` 注释跳过（注释里的文档引用色）、余者须带同行 `// color-allow: 原因` 声明（合法豁免先例：`parametric/Serial.cpp` 串号富文本——分层铁律禁止 parametric 引用 ui token；`document/DocumentSerializer.cpp` 磁盘旧档缺省色——数据契约非样式；`app/MainWindowStatusBar.cpp:90` danger 徽章白字——danger 底两模式可读）。当前状态 = 干净；**新增硬编码颜色前先想 token，写完跑它**。
- **测试档铁律（2026-12 P2-2）**：**回归基线不许放在仓库之外**。测试必须用模型 API 自建文档（合成档）；仓库内**没有** `tests/fixtures/` 目录，**禁止**引用绝对路径（盘符路径 / build 产物 / 用户目录）。历史教训：test_intersection_update 加载用户存档 `E:/3.gcad`，用户一改就红（"交点偏移 29.17mm / 缺变量 后长补正"）；test_p612_colinearity 加载 `build/out/Debug/1.gcad`（未随仓库提交）→ 常年 QSKIP 空跑。守卫：`python tools/check_test_fixtures.py`（发现即 exit 1；一次性探针可加 `// fixture-allow: 原因` 注释豁免；test_realdoc_* 因 GCAD_DOC 手动跑而豁免）。合成档范式见 `test_intersection_update.cpp` 的 `CrossLayerDoc`、`test_p612_colinearity.cpp`。
- **代码拆分实操规范（2026-09 v2.0，外部参考《文件拆分实操规则 v2.0》`E:/e/file_split_rules_v2_final.md` **未入库**；本仓以 `tools/check_file_size.py` + `redline_exceptions.json` 为准）**：
  - **核心原则**：职责优先，拆分的目的是让一个职责只在一个地方被修改，杜绝过度拆分导致的认知碎片化；动手前必须有特征测试锁住行为（第 0 步）；改动命令/头文件使用门面（Facade）保留原头文件 include 转发，避免打断编译单元。
  - **反伪拆分铁律（2026-09-06 增补）**：**严禁仅为了压低物理行数而将同一个类的实现散落到多个 `.cpp` 文件（Partial Class 陷阱）**。类的成员函数分散在多个文件并未降低内部状态耦合与认知负荷；重构必须以**提炼独立的协作者对象（Collaborator）、策略对象（Strategy）或组合式子组件（Sub-Widget/Section）**为第一原则。
  - **架构分层行数预算**：纯逻辑/几何算法 ≤500 行；撤销命令/数据模型 ≤400 行；Qt UI 布局 ≤800 行；聚合外观头（如 `ParamDocument.h`）≤800 行（硬上限 900 行）；常规源码 ≤900 行；单测试文件 ≤2500 行（警告线 1500 行）。
  - **隐式状态防爆炸**：单类内 bool 成员变量禁止 >5 个；超标必须提炼显式状态机、子会话或属性结构体。
  - **测试单 Target 拆分规范（2026-09-06 增补）**：测试代码超标拆分**严禁新建可执行文件 target**（防止 MSVC 胖重链和 PDB 膨胀）；必须在既有单 target 内部按测试业务域拆分为多个 `.cpp` 编译单元（如 `test_foo_basic.cpp` + `test_foo_advanced.cpp`）。
  - **自动化守护体系（八守卫进 ctest）**：
    1. `tools/check_file_size.py`：按架构层扫描行数（含申报豁免文件的膨胀棘轮检查，超申报行数直接 FAIL）；
    2. `tools/check_header_classification.py`：头文件 fan-in 与命令大杂烩检测；
    3. `tools/check_bool_flags.py`：检测类内 bool 状态爆炸；
    4. `tools/check_test_split.py`：检测测试文件行数硬顶与命名规范；
    5. `redline_exceptions.json`：存量超标申报清单。**铁律：清单只许缩短、绝不扩增**。CI 阻断任何未申报的新违规；
    6. `tools/check_inline_units.py`：单位/角度内联字面量检测（禁 `M_PI`、裸 `180.0 / kPi`、裸 `QString::number(x, 'f', N)`、裸 `/ 10.0`；唯一豁免 `src/geometry/Angle.h`/`Units.h`，同行 `// units-allow: <理由>` 可放行）。
- **GUI 测试等待（2026-12 P2-3）**：禁止用固定 `QTest::qWait(N)` 为"等 UI 追上"占位 —— 它是竞态，机器一忙就让健康功能假失败（整批 ctest 下每次红的集合都不同，单跑又全绿，极难归因）。`tests/TestHelpers.h` 三件套：`cad::test::waitUntil(pred, 5000)`（等"某状态成立"）、`cad::test::grabStable(widget)`（抓帧等到连续两帧一致，替代 `qWait(30); grab()`）、`cad::test::settle()`（**只在断言"什么都没发生"时用**——没有可等的状态，只能排空事件循环）。**迁移前先分清两类**：断言"某值变了"→ `waitUntil`；断言"某值没变"→ `settle`。典型根因：输入值经 debounce 定时器应用，断言却在 `mouseClick()` 返回后立刻跑（test_dialog_tabs::switchBackAfterTyping 就是这样红了很久）。全仓 `QTest::qWait(` 调用仍有大量存量（**具体计数属易腐快照，勿再写入本文档**，需要时现场 grep 统计），其中一批是 `qWaitForWindowExposed` 后的 `qWait(80)` —— 属**过度等待**（`QTest::qWait` 不会提前返回，负载下并不会更短），**不要机械换条件**；sendMouse 式 `sendEvent(view.viewport())`+`qWait(20)` 对 —— **已判定为同步链路、统一保留**（Qt 6.11 下 `QTEST_QPA_MOUSE_HANDLING` 未定义，`QTest::mouseClick(QWidget)`/`sendEvent` 均走 `qApp->notify` 同步投递，工具链路无定时器/排队连接，断言不依赖异步工作；test_rotate_copy 1~844 行批次已逐点位加判定注释，后续批次照此办理），见本文件末尾「架构决策记录」。
- **磁盘格式版本与迁移（2026-12 P2-1）**：`kFormatVersion` 的唯一定义点在 `src/document/FormatMigration.h`。**改格式 = bump 常量 + 写 `migrateVNToVN+1` + 往 `FormatMigration.cpp` 的 `registry()` 追加一行**，不要在 `DocumentSerializer` 里加字段嗅探。`FormatMigration::migrate()` 逐级前进，**链路有缺口就拒绝加载**（旧实现退回嗅探正是静默损坏的入口）。序列化器里剩下的是**模型不变量**（如"缺辅助层就补一个"——见 `Layer.h`）与**损坏兜底**，不要把两者混成"历史"。回归：跑 `test_migration`（12 例；2026-09 影子偏转删除后 `v1ShadowOffsetMigratesToAnchor` 改写为 `v1ShadowOffsetKeyIsDropped`，v1→v2 步改为纯删键占位；2026-09-06 v3→v4 纯直通步 `optional-fields` 落地——2026-09 批量可选字段 OrthoOffset/chordLength/showOrthoAxis/flipA/flipB/shadowLastHost* 记入 v4，配 `v3OptionalFieldsPassThrough`）+ `test_serializer`。
- 性能探针：src/parametric/PerfProbe.h，运行时 `GCAD_PROFILE=1` 启用，按逻辑帧统计打印到 stderr（每 120 帧一行）。源码中**没有** `gcad_click_log.txt` 落盘路径——旧文档「固定路径遥测日志」的说法已失效（2026-09 核对，全仓 grep 0 命中）。
- **新增 .cpp 只需加入其模块库的源列表**（CMakeLists.txt 中 gcad_geometry/gcad_parametric/gcad_canvas/gcad_document/gcad_ui/gcad_tools/gcad_app 七库之一），主程序与所有测试通过链接库自动获得，**不再有逐 target 源列表**。注意：测试 target 源列表只放 `tests/*.cpp`（+ 需要图标的 `resources/icons.qrc`），**切勿把项目头文件加进测试源列表**——AUTOMOC 会在 exe 里再 moc 一份与库的 moc 重复定义（LNK2005）。


---

## 构建细节（第三轮迁出）

- **PDB 膨胀治理 + 链接映像完整性（2026-08 起，2026-12 泛化）**：**全部 `test_*` 目标**在 `CMakeLists.txt` 末尾按 `BUILDSYSTEM_TARGETS` 统一强制 `/INCREMENTAL:NO`（原 25 个全链测试的硬编码名单已删除，新增测试自动生效）——Debug 全符号下这些测试的 PDB 曾因增量链接累积到 ~1GB/个（总量 12GB）；且 `.ilk` 陈旧时增量链接可能产出半更新映像（2026-12 实测 test_resolver_points.exe 的 InitializeSListHead IAT 槽为 0 → 启动即 AV，重链接即恢复，见 TROUBLESHOOTING.md）。主程序保留增量。**勿删这些标志**；其代价是测试重新链接为全量重链（当前 out-reldeb 实测 PDB 120~225MB/个）。因此日常开发必须使用 `tools\build.bat WildWindPattern` 或单个测试名，跳过测试的链接风暴（`ctest -N` 可列出全部用例）。
- 如需换回 VS 生成器：改 CMakePresets.json 后删 `build/out-reldeb/CMakeCache.txt`+`CMakeFiles` 及 `_deps/*-subbuild` 缓存（`_deps/*-src` 保留即可离线复用依赖源码）。
- clangd 安装于 `C:\Users\Administrator\AppData\Local\Programs\clangd\clangd_22.1.6\bin\clangd.exe`，opencode 的 LSP 配置指向 `build\out-reldeb`（见 opencode.json）。
- PowerShell 5.1 下 `cmd /c ""...&&..."` 嵌套引号会失败（报 'C:\Program' 不是内部或外部命令），改走临时 .bat（先 call vcvars64.bat 再 cmake）。
