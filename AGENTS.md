# AGENTS.md

## 经验库使用规则

- **修 bug 前**：先 grep `TROUBLESHOOTING.md`（踩坑经验库，按需查阅，不注入会话）相关关键词，避免重踩已知坑。改快捷键前必查第 0 组登记表。
- **修完清单外的新 bug**：根因 + 修复验证后追加到 `TROUBLESHOOTING.md` 对应主题分组。
- **任务收尾防过时**：本任务若触碰了 AGENTS.md/TROUBLESHOOTING.md 中声明的事实（常量/命令/路径/文件/用例数），收尾时必须核对源码并更新对应条目；事实性条目应带源码引用（file:line），改动源码后同步修订。
- **约定变更须同步**：改动了 AGENTS.md「架构原则/领域建模决策」声明的行为时，同步修订对应条目（决策属用户拍板，翻案前先确认）。

## 项目简介

WildWind Pattern（野风帖）——参数化服装 CAD 系统，基于 C++23 / Qt6 构建。通过参数化建模引擎驱动服装纸样的自动生成与联动更新。核心能力：参数化建模（ParamPoint/Segment/Block/Attachment/FormulaVariable）、智能约束求解（Resolver + ConditionEngine）、交互式制图工具链（智能笔/打断/交点/曲线编辑/旋转）、UI 组件化（VariablePanel/LayerPanel/SidePanel）、文档持久化（DocumentSerializer，ZIP 压缩 via miniz 3.0.2）。

## 模块边界

| 目录 | 职责 |
|------|------|
| `src/parametric` | 核心引擎：参数点、线段、Block、公式变量、条件引擎、Resolver、PerfProbe |
| `src/ui` | 面板与对话框 UI：变量面板、图层面板、公式卡片、条件对话框、IconHelper、Theme；**以及线段属性对话框群**（2026-12 P2-4 从 tools/ 迁入：LinePropertyDialog / Segment*Card / AuxPoint·IntersectionForm / PointRefEdit / QuickAuxDialog / MeasureResultDialog / LayerFeedback；AngleHud 已随 CONTEXT_STRIP 二期删除）——凡 QWidget 即归 `ui/` |
| `src/tools` | **只放手势与状态机**：选择、智能笔、曲线编辑、捕捉引擎、工具管理器、Connect/Marquee/RotateCopyGesture、LineFactory、SnapEngine、RotateGizmo、LeaderCandidatePicker。P2-4 后本层**不再含任何 QWidget**；需要弹窗时用 `cad::ui::` 前缀的前置声明（`namespace cad::ui { class X; }`） |
| `src/canvas` | 画布渲染：CanvasView/CanvasScene、BlockItem/CurveItem、图层、OpenGL 视口 |
| `src/geometry` | 基础几何：Vec2、单位定义、CurveMath |
| `src/app` | 应用入口、上下文属性条（ContextStrip，CONTEXT_STRIP_DESIGN.md 一期+二期+三期；2026-12 取代 SegmentEditBar + SmartPenPreInputBar 两条互斥 bar + 旋转/连接两处浮动 AngleHud，旧类已删；三期起 测量/角度测量/交点/打断 四工具经 `reportHoverTarget` 接入只读悬停"扫过即看"） |

## 架构原则（不可破坏）

- **分层单向依赖**：上层可依赖下层，下层绝不反向依赖上层（parametric 引擎不依赖任何 UI）。可测试性靠此保证。include 层面不设防（所有库都 PUBLIC 暴露整个 `src/`），所以**依赖方向靠 `python tools/check_layering.py` 把关**（2026-12 P1-6：扫描各模块目录的 include，报告指向上层的边；canvas/document 为同级可互访，tests 不检查）。需要"下层回调上层"时用**依赖倒置**：接口声明在下层（如 `canvas/InputDispatcher.h` 由 tools 层的 ToolManager 实现），或把逻辑提到上层、通过信号对接（CanvasView 只上报 `segmentContextMenuRequested(SegmentHit)`，菜单/对话框/命令全在 app 层）。
- **ParamDocument 是门面**：所有公共 API 与信号签名必须保持不变；业务逻辑封装在内部子域类中。容器写路径已收口：blocks/layers/variables/formulas 等只保留 const 访问器，模型变更只能走 ①带校验/信号的门面方法 ②显式命名的静默恢复 API。禁止 `const_cast`；attachment 原位编辑唯一通道是 `findAttachment(id)` 可变重载。
  - **静默恢复通道（2026-12 P1-2）**：14 个 `*Raw` API 已**私有化**，唯一入口是 `cad::param::RawModelAccess`（`src/parametric/ParamDocumentRaw.h`，ParamDocument 的唯一 friend）。调用形如 `cad::param::RawModelAccess::addAttachmentsRaw(*m_doc, atts)` —— 只有 include 了该头的**受信任管线**（反序列化 / undo 回放 / 拖拽取消快照还原）能编译通过，调用点的"信任"是可 grep 的。
  - **undo 栈有上限（2026-12 P2-5）**：`ParamDocument::kUndoStackLimit = 150`，构造时 `setUndoLimit()`。命令带**全量模型快照**（删除命令还带级联子图），所以栈无上限 = 长会话无界内存增长；超限后 QUndoStack 自动丢弃最旧的一步。写长链条测试（>150 步）时注意历史会被截断。
  - **可变裸指针（2026-12 P1-3）**：`findBlock()/blockById()` 返回指向 blocks 向量的裸指针，任何结构性变更（add/remove/clear）都可能使其失效 —— **一次一取，禁止跨变更持有**。`doc.structureEpoch()` 可观察结构代数（供缓存失效），debug 下 `doc.blockPointerInRange(p)` 可断言指针仍在当前存储区内。**中期句柄化经 C1 审计（2026-08-29）结案为不做**：全仓 0 违规 / 失效面仅 4 个 bump 点（附件与求解不失效）/ 纪律已被遵守；B 类视图 `byId()` 预留承接点，若未来出现指针类 bug 可低成本重启窄版 BlockPtr（设计见 .workbuddy/memory C1 报告）。
- **UI 与引擎并联观察者**：面板与画布都是 ParamDocument 信号的观察者，不直接调用 Resolver（被封装在文档内部）。
- **隐藏实体**：visible=false 是纯视觉属性，不影响交互（仍可悬停/选择/捕捉）。几何缓存 rebuildCache 必须包含所有实体，渲染 paint 独立按可见性控制。
- **画布缓存刷新**：只改显示/语义属性、不移动几何的命令必须显式 `block->touchGeometry()`，否则 rebuildCache 不触发、画布不刷新。（2026-12 P1-1 收口：`Block::geometryEpoch` 是**私有字段**，唯一 bump 入口 = `Block::touchGeometry()`，只读用 `geometryEpoch()`；`grep -rn "touchGeometry" src/` 即可列出全部刷新点，禁止再写 `++geometryEpoch`。）

## 构建命令

```bash
tools\build.bat               # 一键：vcvars64 + configure + build（Ninja Debug，binaryDir=build/out）
tools\build.bat release       # Release（binaryDir=build/out-rel）
```

- **Ninja 生成器需要 MSVC 环境**：普通 PowerShell 直接跑 `cmake` 找不到 cl.exe 会失败，必须走 `tools\build.bat`（内部先 call vcvars64.bat）或 Developer PowerShell。
- compile_commands.json 由 Ninja 生成器自动导出到 `build/out`（`CMAKE_EXPORT_COMPILE_COMMANDS`），clangd 直接使用并热重载——增删源文件/改 CMakeLists 后只需重跑构建脚本，**无需手动刷 db**。
- clangd 安装于 `C:\Users\Administrator\AppData\Local\Programs\clangd\clangd_22.1.6\bin\clangd.exe`，opencode 的 LSP 配置指向 `build\out`（见 opencode.json）。
- PowerShell 5.1 下 `cmd /c ""...&&..."` 嵌套引号会失败（报 'C:\Program' 不是内部或外部命令），改走临时 .bat（先 call vcvars64.bat 再 cmake）。
- **构建/跑测的宿主限制（2026-08 实测）**：Bash 工具**禁止调用 cmd.exe**（安全策略直接拦截，含 `MSYS_NO_PATHCONV=1 cmd /c`），构建/ctest 一律走 PowerShell 工具的 `& .\tools\build.bat` / `ctest`；PowerShell 工具**不回显 stdout**，日志必须 `*>` 落盘（`Out-File` 默认 UTF-16）再用 `iconv -f UTF-16LE -t UTF-8 x.log` 转码读取。
- **模块静态库（2026-08 CMake 重构）**：源文件按模块目录归入 7 个静态库（`CMakeLists.txt`），分层单向依赖 `gcad_geometry` → `gcad_parametric` → `gcad_canvas`/`gcad_document` → `gcad_ui` → `gcad_tools` → `gcad_app`；主程序与全部测试通过链接库获得源码，不再逐 target 罗列。`DeleteImpactConfirm` 属 UI 对话框归 `gcad_ui`（document 层不依赖 ui）。
- **PDB 膨胀治理（2026-08）**：11 个全链测试（test_realdoc_full/nav_smoke/aux_layer/smartpen_aux/measure/**context_strip**(原 segment_edit_bar)/tool_intersection/select_wkey/curve_edit/rotate_copy/dialog_tabs）在 CMakeLists 末尾强制 `/INCREMENTAL:NO`——Debug 全符号下这些测试的 PDB 曾因增量链接累积到 ~1GB/个（总量 12GB），关增量后封顶 ~13MB/个（全量符号 PDB 本就小，1GB 几乎全是增量历史）。主程序保留增量（日常调试迭代）。**勿删这些标志**；代价仅是这 11 个测试重链稍慢。
- 如需换回 VS 生成器：改 CMakePresets.json 后删 `build/out/CMakeCache.txt`+`CMakeFiles` 及 `_deps/*-subbuild` 缓存（`_deps/*-src` 保留即可离线复用依赖源码）。

## 验证命令

```bash
cd build/out
ctest
```

- default preset 的 binaryDir 是 `build/out`（单配置 Debug，无需 `-C`）；release 是 `build/out-rel`。
- ctest 共 **31** 个用例（test_tool_hints(H3 P0)/**mode_indicator(2026-08-29)**/expression/resolver/serializer/p612_colinearity/duplicate/break/intersection/commands/curve/triangle_unfold/perf/**migration(P2-1)**/aux_layer/smartpen_aux/measure/**context_strip(CONTEXT_STRIP 一期, 原 segment_edit_bar 迁入)**/formula_groups/tool_intersection/select_wkey/curve_edit/rotate_copy/dialog_tabs/canvas_perf/hold_show/log_probe/component/extend/intersection_update/**resolve_scale(P2-6)**）；**2 红 = 既有基线**（test_serializer::bridgeAuxPointSnappableAndAttachable + test_component::dragComponentLeaderCurveFollowStable 31.4798mm，见 TROUBLESHOOTING 第 5 组）；**环境漂移红（2026-08-28 实测）**：test_intersection_update 全部 5 用例 + test_extend::savedDocFormulaStartExtendRenders 依赖活档 `E:/3.gcad`——该文件被用户改动后与测试期望不符（交点偏移 29.17mm / 缺变量「后长补正」，tests/test_extend.cpp:858-886），非代码回归，恢复 3.gcad 对应内容或更新测试期望即可；**GUI 时序抖动**：test_dialog_tabs::switchBackAfterTyping（interpPercent 断言）/test_aux_layer/test_rotate_copy/**test_select_wkey**（2026-08-29 实测：整批 3 次挂 1 次，单跑连续通过）在整批 ctest 压力下偶发失败，单测复跑即过，勿误判回归。test_intersection_update 依赖 E:/3.gcad 存在（缺则 QSKIP 跳过）。**test_canvas_perf 的 singleCurveFrame 曾为 Debug 段错误存量问题**（疑似环境/软渲染），当前 Ninja Debug 下通过；复现时用基线对照法排查，勿误判回归（详见 `TROUBLESHOOTING.md` 第 5 组）。
- 不进 ctest 需手动跑：test_realdoc_perf、test_realdoc_full（env `GCAD_DOC=<.gcad路径>`，可叠加 `GCAD_PROFILE=1`）、test_nav_smoke（ElaWindow 导航栈冒烟，无 env）。
- **静态分层检查（2026-12 P1-6）**：`python tools/check_layering.py`（无参数、零依赖、秒级）。扫描 `src/<module>/**` 的 include，凡指向更上层模块即报错并 exit 1。当前状态 = 干净；**新增跨层 include 前先跑它**（CI 未接入，靠约定）。
- **测试档铁律（2026-12 P2-2）**：**回归基线不许放在仓库之外**。测试必须用模型 API 自建文档，或加载 `tests/fixtures/` 下随仓库提交的档；**禁止**引用绝对路径（盘符路径 / build 产物 / 用户目录）。历史教训：test_intersection_update 加载用户存档 `E:/3.gcad`，用户一改就红（"交点偏移 29.17mm / 缺变量 后长补正"）；test_p612_colinearity 加载 `build/out/Debug/1.gcad`（未随仓库提交）→ 常年 QSKIP 空跑。守卫：`python tools/check_test_fixtures.py`（发现即 exit 1；一次性探针可加 `// fixture-allow: 原因` 注释豁免；test_realdoc_* 因 GCAD_DOC 手动跑而豁免）。合成档范式见 `test_intersection_update.cpp` 的 `CrossLayerDoc`、`test_p612_colinearity.cpp`。
- **GUI 测试等待（2026-12 P2-3）**：禁止用固定 `QTest::qWait(N)` 为"等 UI 追上"占位 —— 它是竞态，机器一忙就让健康功能假失败（整批 ctest 下每次红的集合都不同，单跑又全绿，极难归因）。`tests/TestHelpers.h` 三件套：`cad::test::waitUntil(pred, 5000)`（等"某状态成立"）、`cad::test::grabStable(widget)`（抓帧等到连续两帧一致，替代 `qWait(30); grab()`）、`cad::test::settle()`（**只在断言"什么都没发生"时用**——没有可等的状态，只能排空事件循环）。**迁移前先分清两类**：断言"某值变了"→ `waitUntil`；断言"某值没变"→ `settle`。典型根因：输入值经 debounce 定时器应用，断言却在 `mouseClick()` 返回后立刻跑（test_dialog_tabs::switchBackAfterTyping 就是这样红了很久）。全仓现有 **150** 处 `QTest::qWait(` 调用（2026-08-29 P0 收尾复核：test_rotate_copy 78 / test_select_wkey 50 / 其余 22；此前记 156 为旧树状态），其中 76 处是 `qWaitForWindowExposed` 后的 `qWait(80)` —— 这批属**过度等待**（`QTest::qWait` 不会提前返回，负载下并不会更短），**不要机械换条件**；另有 53 处 sendMouse 式 `sendEvent(view.viewport())`+`qWait(20)` 对 —— **已判定为同步链路、统一保留**（Qt 6.11 下 `QTEST_QPA_MOUSE_HANDLING` 未定义，`QTest::mouseClick(QWidget)`/`sendEvent` 均走 `qApp->notify` 同步投递，工具链路无定时器/排队连接，断言不依赖异步工作；test_rotate_copy 1~844 行批次已逐点位加判定注释，后续批次照此办理），见本文件末尾「架构决策记录」。
- **磁盘格式版本与迁移（2026-12 P2-1）**：`kFormatVersion` 的唯一定义点在 `src/document/FormatMigration.h`。**改格式 = bump 常量 + 写 `migrateVNToVN+1` + 往 `FormatMigration.cpp` 的 `registry()` 追加一行**，不要在 `DocumentSerializer` 里加字段嗅探。`FormatMigration::migrate()` 逐级前进，**链路有缺口就拒绝加载**（旧实现退回嗅探正是静默损坏的入口）。序列化器里剩下的是**模型不变量**（如"缺辅助层就补一个"——见 `Layer.h`）与**损坏兜底**，不要把两者混成"历史"。回归：跑 `test_migration`（11 例）+ `test_serializer`。
- 性能探针：src/parametric/PerfProbe.h，`GCAD_PROFILE=1` 启用；GUI 点击链路遥测日志固定路径 `e:\garment-cad\gcad_click_log.txt`。
- **新增 .cpp 只需加入其模块库的源列表**（CMakeLists.txt 中 gcad_geometry/gcad_parametric/gcad_canvas/gcad_document/gcad_ui/gcad_tools/gcad_app 七库之一），主程序与所有测试通过链接库自动获得，**不再有逐 target 源列表**。注意：测试 target 源列表只放 `tests/*.cpp`（+ 需要图标的 `resources/icons.qrc`），**切勿把项目头文件加进测试源列表**——AUTOMOC 会在 exe 里再 moc 一份与库的 moc 重复定义（LNK2005）。

## 环境要求

| 依赖 | 要求 |
|------|------|
| Qt | 6.x（推荐 6.5+），组件：Widgets、Svg、Test、OpenGLWidgets |
| 编译器 | MSVC 2022（Visual Studio 17，x64，`/std:c++latest` + `/permissive-` + `/FS`） |
| CMake | ≥ 3.25 |
| C++ 标准 | C++23（`CMAKE_CXX_STANDARD_REQUIRED ON`） |

Qt 安装与 QT_DIR 配置见环境方式一（Qt 官方安装器，QT_DIR 指向 msvc2022_64）/方式二（vcpkg manifest + toolchain file），两种方式无需同时使用。

## 依赖管理

- FetchContent 内嵌于 CMakeLists.txt（无 vcpkg.json/conanfile）：miniz `https://github.com/richgel999/miniz.git` Tag `3.0.2` 浅克隆。
- **ElaWidgetTools**（`https://github.com/Liniyous/ElaWidgetTools.git`，GIT_TAG `aa1856b8f8589ba0a82e50c0ac01e289eb3ff2c4`，GIT_SHALLOW）：主项目 CMakeLists.txt 用 `PATCH_COMMAND` 执行 `third_party/elawidgettools_qt69_patch.cmake`（共 4 个 Part：Qt 6.9 编译兼容 / 弹窗按钮同步 emit 修 UAF + 隐藏空文本中间按钮 / ElaMenu 移除 400ms 动画 / ElaAppBar 默认关闭路径尊重 close() 被拒，均幂等）。库为静态库，主程序以 dllimport 语义链接产生 LNK4217 警告属正常。重建流程与链接坑详见 `TROUBLESHOOTING.md` 第 2 组。
- **spdlog**（`https://github.com/gabime/spdlog.git`，GIT_TAG `v1.17.0`，GIT_SHALLOW，MIT）：编译版静态库，自带 fmt；链接 `spdlog::spdlog`，头文件 `spdlog/spdlog.h`。Qt sink 可用（`spdlog/sinks/qt_sinks.h`）。
- **Tracy Profiler**（`https://github.com/wolfpld/tracy.git`，GIT_TAG `v0.14.0`，GIT_SHALLOW，BSD-3）：客户端库 `Tracy::TracyClient`，头文件 `tracy/Tracy.hpp`，探针 `ZoneScoped`；`TRACY_ON_DEMAND=ON`（无 profiler 连接时零收集），发布版可用 `-DTRACY_ENABLE=OFF` 整体关闭；GUI 为独立工具（GitHub releases 的 windows-x64 预编译 zip）。
- Qt6 组件：Widgets Svg OpenGLWidgets Test；AUTOMOC/AUTORCC/AUTOUIC 已启用。
- 图标：Phosphor Icons SVG（MIT），存放 resources/icons/ + icons.qrc（前缀 `:/icons/`），统一经 `src/ui/IconHelper.h`（iconByName 着色 / icon2State 双状态 / appIcon）。新图标从 phosphor-icons/core assets/regular/ 下载并在 qrc 登记。

## 开发规范

- **文件编码**：所有新建源文件（.h/.cpp/.qrc 等）必须 UTF-8 with BOM，否则 MSVC 解析失败（C2001/C2061）。**Write 与 Edit 工具写出的文件均无 BOM**（Edit 还会剥掉文件原有 BOM）——创建/编辑含中文的源文件后必须补 BOM（pwsh 脚本 `[System.IO.File]::WriteAllText($p, $t, [System.Text.UTF8Encoding]::new($true))`）。
- **工具模式切换统一用 W 键，禁用 Tab**（Tab 是 Qt 焦点导航键，打断交互）。W 语义按工具区分，切换后 `event->accept()` 并刷新预览/HUD。
- **交互函数必须显式接入事件流**（mouseMove/mousePress/keyPress），只实现不调用 = 功能静默失效；用单元测试直接驱动事件流验证。
- **工具元数据表驱动（2026-08-29 TOOL_SYSTEM_AUDIT P0-H3，2026-12 P3 升级）**：状态栏 8 工具操作提示唯一出处 = 各工具 `static ToolDescriptor describe()` 的 `hintText`（`src/tools/ToolRegistry.h`；P3 后原 ToolManager.cpp 的 toolHintText switch 已删，`cad::tools::toolHintText(ToolType)` 退化为查 `ToolRegistry` 的薄封装）；MainWindow 只渲染（`setToolHint` → elide 省略 + tooltip 全文兜底 + Resize 重算，M9），菜单/工具坞按钮/图标/提示全部遍历 registry 自动生成。**新增工具必须**：①ToolType 枚举加值（ToolRegistry.h）②实现 onActivate/onDeactivate + name() ③实现 `static ToolDescriptor describe()`（id/显示名/图标/快捷键/提示/工厂，缺 = test_tool_hints 红）④ToolRegistry.cpp 加 1 行 `registerTool<T>()`⑤`MainWindow.cpp` 的 `toolDockIcon()` 补 1 项 Ela 字体图标（漏补 = 兜底箭头图标 + 警告，**不崩**，见下条）。旋转提示必须描述 D15 确认门（H2 拍板文案）；提示还可在运行期被工具覆盖（M5，见下）。**运行期提示覆盖（M5）**：工具内部有静态元数据表达不了的状态时（智能笔 直线/省道线），用 `Tool::reportHintOverride(hint)` 经 `ToolHost::setHintOverride` → `ToolManager::hintOverrideChanged` → `MainWindow::onToolHintOverride` 改写状态栏文案；**切工具时 MainWindow 会先清覆盖**（不跨工具继承），传空串 = 恢复 `describe()` 的默认文案。`ToolHost::setHintOverride` 是**非纯虚**（提示是可选能力，无头单测桩不实现也能编译）。
- **工具公共能力层三件套（2026-08-29 TOOL_SYSTEM_AUDIT P1 收口）**：①**HUD 标签一律用 `HudItem`**（`src/canvas/HudItem.h`，全局命名空间；双外观 ThemeDefault=主题浅色 / DarkPill=深胶囊；`moveToPoint/placeAtScene` 自带 1/zoom 补偿 + 屏幕像素偏移）——禁止再手搭 QGraphicsRectItem+TextItem（M1 缩放失效根源），禁在场景单位里写"字体像素"尺寸；②**命中测试一律走 `HitTester.h`**（tools 层 header-only：`blockHitsAtScene` 一次 items() 出全部块命中，堆叠降序+去重+活动层过滤**唯一规则源**；`isBlankSpaceAtScene` 空白判定不分层）——新工具不得再复制 hitBlock 循环，活动层判定只认它；③**工具临时图元必须登记 `ManagedItems`**（`src/canvas/ManagedItems.h`：`own(item, &shadow)` 建点登记 + 影子绑定，`release(item)` 会话中重建型，`clear()` deactivate 统一释放）——禁止再写 `removeItem+delete+置空` 手写样板（M2 泄漏先例）。
- **工具生命周期收口（2026-12，TOOL_SYSTEM_AUDIT P2 落地）**：①**基类契约编译器保证**——Tool::activate/deactivate 非虚（src/tools/Tool.h：全量入口 activate(const ToolContext&) + 两参便捷重载 activate(scene, doc) 供测试直驱），派生工具只实现 onActivate/onDeactivate 虚钩子，**禁止再覆盖 activate/deactivate**（曾 5 个工具遮蔽 m_scene/m_paramDoc 且不调基类 = H4）；基类 protected 成员 m_scene/m_paramDoc/m_undoStack/m_hud/m_host 全工具共用，ToolRotate 的悬浮角度框已改名 m_angleHud（避免遮蔽基类 HudItem* m_hud）。②**上下文一次注入**：ToolContext{scene, paramDoc, undoStack, host} 替代旧三处散落 setter（Tool::setUndoStack 保留为 ToolManager::setUndoStack 的活更新通道；setToolSwitchRequest / ToolSelect::setEditTargetCallback 已删）；ToolHost 接口（requestToolSwitch / setEditTarget）由 ToolManager 实现——工具不认识管理器。③**实例常驻（L5）**：ToolManager 持 std::map<ToolType, std::unique_ptr<Tool>>，切换 = deactivate 旧 + activate 新；**重复点击当前工具 = no-op**（旧实现连按两次 R 清空旋转会话）；选区继承 = switchTool 内先抓 Select 选集再 deactivate（Select 实例常驻但其 deactivate 会清选集）。常驻实例下 onActivate 必须复位会话状态（Select 回单选 / SmartPen 回直线 / Rotate 回角度模式 / Intersection 走 resetState / Measure·AngleMeasure 清 snap 残留）。新增工具必须实现 onActivate/onDeactivate + 事件处理 + name()。
- **工具注册表 ToolRegistry（2026-12，TOOL_SYSTEM_AUDIT P3 落地）**：ToolType 枚举定义移入 `src/tools/ToolRegistry.h`（ToolManager.h 不再定义，仅 Tool.h 保留前置声明）；每个工具 `static ToolDescriptor describe()` 自持 { id, displayName(含 & 加速符), iconName(Phosphor SVG 名), shortcut(测量 M 让给画布长按故为空), hintText, factory }，`ToolRegistry.cpp` 构造时注册全部 8 个（注册序 = 工具坞按钮序 = 菜单序）；MainWindow 遍历生成 QAction（`QHash<ToolType, QAction*> m_toolActions` + 注册序 `m_toolOrder`，替代旧 8 个成员 / 8 个 actionX 槽 / onToolChanged 两个平行 switch），工具坞 Ela 字体图标由 `MainWindow.cpp` 匿名命名空间的 `toolDockIcon(ToolType)` 提供（`QHash<ToolType, ElaIconType::IconName>` 键控查表 —— app 层视觉偏好，非工具元数据，故不进 ToolDescriptor）；**查不到 = 兜底 ArrowPointer + `Q_ASSERT_X`/`qWarning` 报警**。复核 N1：早先是 `std::array<…,8>` 配 `arr[static_cast<int>(type)]` 枚举序下标 —— 新增第 9 个工具会**越界读 UB 且编译不报错**，而且让"不再改 MainWindow"变成假承诺。ToolManager::ensureTool 改 `ToolRegistry::instance().create(type)`（保留 P2 常驻实例池）；**`~ToolManager` 先 deactivate 激活中的工具**（常驻实例下清理点已迁到 deactivate，否则退出时激活工具的 onDeactivate 从不执行）；`toolHintText(ToolType)` 退化为查 registry 的薄封装。**新增工具 = 1 对 .cpp/.h + describe() + ToolRegistry.cpp 1 行注册 + `toolDockIcon` 补 1 项图标**（漏补不崩，只报警 + 降级箭头图标）。`registerTool<T>()` 带重复注册断言（重复注册会在注册序塞重项 → 生成重复按钮）。
- **复核收尾（2026-12，TOOL_SYSTEM_AUDIT 剩余项落地）**：①**AngleHud 已随 CONTEXT_STRIP 二期删除（2026-12）**——连接手势的角度输入并入 ContextStrip（会话模式, 见「连接角度会话」条目），其 L7 教训（悬浮控件颜色不得沿父链反查、构造时解析一次 CanvasStyle）已无宿主，tools 层再无 QWidget；`ToolHost::setConnectAngleSession/setConnectAngleValidity` + `Tool` 虚钩子 `connectAngleTextChanged/ModeChanged/Committed/Cancelled`（默认 no-op，ToolSelect 转交 ConnectGesture）构成新通道。②**旋转 HUD 随视图重定位**（M8）：ToolRotate 挂 CanvasView::zoomFactorChanged + 双滚动条 valueChanged → repositionHud()；ToolRotate 非 QObject —— 必须 QObject::connect/disconnect 限定名，sender 对 CanvasView 成员信号要 static_cast<CanvasView*>；连接存成员（m_view*Conn），onDeactivate 断开防悬垂。③**非模态弹窗阻塞反馈**（M10）：智能笔 QuickAuxDialog/省道弹窗打开期间画布光标 = ForbiddenCursor + toast「请先完成…」（showDialogBlockedFeedback()，文案同值守卫防 mouseMove 每帧刷屏）；事件早退路径调用以兜底维持；弹窗 finished / deactivate 时 clearDialogBlockedCursor() 恢复。④**工具坞无禁用态**（M3 复核）：ToolDockStyle 已删 disabled 分支与注释（全仓工具按钮永不禁用）；未来若引入"禁用某工具"需重建 enabled 分支。⑤**M4 图标落地**：bezier-curve.svg（曲线）/angle.svg（角度测量）入 qrc 并接入 describe().iconName（angle.svg 渲染后重心 cy=167 偏下 39px，已上移 24px 调至 cy=143；换图前可用 build/icon_render 的 Qt 渲染器做像素校验）。⑥**L6**：ToolSelect 右键守卫冗余末项 cands.size() < 2 已删（与 overlapMenu == nullptr 等价）。
- **连接角度会话（CONTEXT_STRIP_DESIGN.md 二期，2026-12 交付）**：连接手势的角度输入并入 `ContextStrip`（`src/ui/AngleHud` 已删除）。**条带是纯输入面，连接语义全在 `ConnectGesture`**（预览/换算/收尾/整步 undo）——通道四条：①手势→条带：ConnectGesture 注入 `m_beginAngleSession` 回调（ToolSelect::onActivate 接线）→ `Tool::reportConnectAngleSession` → `ToolHost::setConnectAngleSession`（ToolManager emit `connectAngleSessionChanged`）→ MainWindow → `ContextStrip::beginConnectAngleSession(blockId, segId, attId, initialAngle)`，**全 null = 会话结束**；②合法性：`ToolHost::setConnectAngleValidity` → 条带角度框 `angleInvalid` 属性（Theme QSS `QLineEdit#angleEdit[angleInvalid="true"]` 红边）；③条带→手势：ContextStrip 四信号 `connectAngleTextChanged/ModeChanged/Committed/Cancelled` → ToolManager `forward*` → `Tool` 虚钩子（默认 no-op）→ ToolSelect override → ConnectGesture::onAngleTextChanged/onAngleModeChanged/commitAngle/cancelAngle（已移 public）。**铁律**：会话内条带**绝不 push 命令**（applyName/applyLength/applyAngle/onUnitToggled/onReverseClicked 全部 `m_connectSession` 守卫——连接在 finalizeConnection 前是飞行中附件，SegmentEditBarCommand/ReverseSegmentCommand 插进手势宏之间会乱 undo）；会话期间名称/长度只读、角度可编辑（refreshChrome 会话分支强制，普通 isBridge 分支会锁回角度框）；组件级连接 fromBlockId 为空 → `findEditAttachment` 会话分支按 `attachmentsView().byId(m_connectAttId)` 直取；公式驱动拒绝 °/⌒ 时不触发 resolved → 条带 onUnitToggled emit 后**必须自 refreshChrome 弹回按钮**；会话 Esc/Enter 走 eventFilter 最前分支 emit 信号，**绝不走 clearPinned/cancelRequested**；`setPinnedTarget` 会话期间直接 return（会话独占显示）；hideBar 清会话标志（工具切换两路幂等）。跟随线段 = `blk->exitSegmentAtPoint(m_connectFromPoint)`（组件级 = 被抓成员块）。测试：test_context_strip +6 例、test_select_wkey +1（击键预览+°⌒+Enter 整步 undo）+ 会话上报断言 2 处、test_component 2 处改会话断言、test_rotate_copy 删 angleHudShortcutOverrideContainment。
- **约束类型分派点登记表**（ParamPoint.h）：新增/修改 PointConstraint 枚举必须逐层同步 12 处（数据定义、Block::resolve exhaustive switch、resolveInterpolatedPoints、曲线宿主判定、Resolver 交点 step、blockReferences 脏传播、degradeOrphanedIntersections、BlockItem 渲染、ToolSelect/ToolCurveEdit 交互、BlockCommands 降级恢复、DocumentSerializer 表驱动字符串映射、Duplicate）。遗漏 blockReferences 或序列化映射不会编译报错，只会静默损坏。**序列化字符串映射已表驱动（2026-08-28 收口 B4）**：`DocumentSerializer.cpp` 四组枚举映射（PointConstraint/SegmentType/SegmentRole/LineStyle）为 `constexpr std::array<std::pair<Enum,const char*>>` 表 + `enumToStr/enumFromStr` 双向查找（首行 = canonical default），str→enum 与 enum→str 同表必然同步，新增枚举只改一处表。
- **删除影响报告**：新删善后分支必须同步更新 `deleteImpactReport` 与测试（九项计数：attachmentsRemoved/bridgesReleased/intersectionsFrozen/linkedFrozen/linkedVarsRemoved/measureVarsRemoved/angleVarsRemoved/formulasBroken/dartLinesDegraded）。
- **卡片抽取范式**：子卡片持 doc 指针 + 目标 id，setTarget/refresh 双入口，模型变更经 `changed(ChangeKind)` 信号回报对话框，对话框只做联动刷新。
- **卡片基类 CardBase（2026-08 抽取）**：五张虚拟列表卡片（VariableCard/FormulaCard/LinkedCard/MeasureCard/AngleMeasureCard）继承 `src/ui/CardBase.h`，共用左侧竖线 paintEvent（`accentBarX()` 虚函数，FormulaCard 组内右移 kGroupIndent）、悬停删除按钮+同尺寸占位槽（`appendDeleteButton`，20×20 防跳动契约）、序号标签（`setIndex` + `indexText()` 虚函数：默认 "N" / 测 "测 N" / 角 "角 N"）、名称 chip、等宽值标签（`createValueLabel`）、🔒 只读图标（`setupLockIcon`）、dangling 样式翻转（`setValueLabelDangling`）与每帧值刷新守卫（`refreshValueGuard`，MeasureCard 额外带 kind 守卫）。数值格式化收口到 `geometry/Units.h`：`formatCmTrimmed`/`formatDegTrimmed`/`formatNumberTrimmed`/`trimTrailingZeros`。**注释回填也收口（2026-08-28 B5/A5）**：五卡注释同步统一走 `CardBase::setCommentSilently(text)`（QSignalBlocker + hasFocus 守卫，五卡共用；原来五卡各自复制 blockSignals 三行）；FormulaCard 实际值回显用 `Units::formatNumberTrimmed`（去尾零，2026-12 拍板后该处曾漏改）。**改卡片骨架先改 CardBase 再改派生**；indexLabel 固定 objectName（varIndex/cardIndex/linkedIndex/measureIndex/angleIndex）是测试查找契约，勿改。
- **角度工具收口（2026-08）**：存储域归一化 = `cad::geo::normalizeDeg360`（[0,360)，原 alphaFromSignedFold）、带符号折角显示 = `cad::geo::normalizeDeg180`（(−180,180]，原 signedFoldDeg，逐字节等价）、角度显示格式化 = `cad::geo::Units::formatDegValue`（无后缀）/`formatDegTrimmed`（带 °）——统一实现在 `src/geometry/Angle.h`/`Units.h`，工具层（ToolRotate/ConnectGesture/SegmentConnectionCard）不再本地复制。改角度约定只改这两处头文件。**收口明细（2026-08-28 CLEANUP_REFACTOR_DESIGN.md A1/A3 落地）**：全库手写 `fmod(x,360); if(<0)+=360` / `while(>180)-=180` 副本已清零（SegmentEditBar 私有 normalizeDeg 删、SegmentAnchorTab 相对切线角、SegmentAuxTab/LineFactory/RotateCopyGesture/BreakCommands/ToolRotate/ToolSmartPen 显示角等全部改调几何函数；-180° 边界统一 Angle.h 语义 `<=\-180`，SegmentAnchorTab 原 `< -180` 分叉已收敛）；弧长↔角度换算收口 `cad::geo::arcMmToDeg(arcMm, radiusMm)`/`degToArcMm(deg, radiusMm)`（Angle.h）+ 双模切换写回收口 `cad::param::followerModeSwitchValues(att, radiusMm, targetMode, params, cond)`（FollowerAngle.h，SegmentAngleCard::onModeToggle 与 ConnectGesture::onAngleModeChanged 共用；弧长回写保持 `std::fmod(curDeg,360)` 语义——公式值可能为原始多圈/负折角，normalize 会塌掉）。
- **跨层连接反馈收口（2026-08）**：`crossLayerToast`（QUuid 层/Block 双重载）+ `crossLayerBadge` 统一在 `src/ui/LayerFeedback.h`（header-only inline，**命名空间 `cad::ui`**，2026-12 P2-4 随 UI 组件从 tools/ 迁入；现进 gcad_ui 源列表），LineFactory/ConnectGesture 里改以 `cad::ui::crossLayerToast` 调用，SegmentConnectionCard/SegmentAuxTab（同层）直接无修饰调用。改文案/跨层语义只改这一个头文件。
- **工具表单群统一骨架 + 圆角纪律（2026-08-28，ui-redesign §4.5/§5.5/§07 P2 落地）**：①共享骨架收口在 `src/ui/FormScaffold.h`（header-only inline，gcad_ui）——`makeFormGroupHeader`（等宽 10px 大写代号 + 中文标题 **text1 墨字 12px 600** + 底部发丝线，替代裸粗体分组——中文原按设计稿用 text2 11px，2026-08-28 用户反馈「掉色感」后加深一档，代号保持 text3 标注感）、`applyFormGrid`（QFormLayout 标签列 88px 右对齐、行距 10px，与线条属性对话框同栅格语言）、`makeFormButtonBar`（surface3 底 + 顶部分隔线，按钮右对齐、主操作最右 = 实心 accent 黄 + onAccent 字）、`makeFormTitleBar`（代号标题栏，**ElaDialog 自带 Fluent 标题栏时勿用——双重标题**，仅供无边框容器）；已接入 QuickAuxDialog（辅助点）、MeasureResultDialog（测量结果，读数组值 = FontLg 15px）、AuxPointForm + IntersectionForm（88px 栅格）；表单对话框颜色构造时烘焙（生命周期短，exec 即销毁）。②`RadiusPill` 更名 **`RadiusBadge`**（Theme.h，值恒 4px——「胶囊」命名与禁胶囊纪律自相矛盾）；全库 >4px 圆角实例清理：ConditionDialog 条件行 6→4、ComponentTab 组件卡 6→4、LayerPanel 层卡 7→4/折叠钮 9→4/计数 pill 8→2/加层钮 5→4、AngleHud 8→4（AngleHud 已随 CONTEXT_STRIP 二期删除）；**新增 chrome 圆角勿超 4px**。
- **卡片竖线色/字号接入 ThemeTokens（2026-08-28 方案 A 重拍板，ui-redesign-2026-08 §2.5）**：卡片左竖线 = **类型色**（变量=piece1 碳灰 `#1E293B` / 公式=piece2 深青 `#0F766E` / 测量(含角度)=piece3 陶土 `#C85A3E` / 关联=piece4 钴蓝 `#2563EB`；组件管理行也用 piece1，ComponentTab.cpp），由 `CardBase::setAccentRole(cad::ui::CardAccent)` 驱动（paintEvent 现读 token，主题切换免重绑）——**旧蓝/橙交替 `rowEven/rowOdd` 已删除**（用户 2026-08-28 拍板方案 A，替代同日早先的交替拍板；setAlternate 保留为虚拟列表 bind 契约 no-op）。暗色模式 piece 家族 = 同色相提明度（piece2 #0F766E→#2DD4BF / piece4 #2563EB→#60A5FA，Theme.cpp dark()，「公式=青」跨模式记忆一致）。字号阶梯：`FontXs 10 / FontSm 11 / FontMd 12 / FontBase 13 / FontLg 15（卡片数值读数 15px Semibold+Mono，卡片第一焦点）/ FontXl 18（空状态主文案）`。悬停卡片描边转 borderStrong（§6.2）；删除钮占位槽默认画 24% 透明 ✕ 轮廓（GhostDeleteSlot，防「空白→出现」跳变）；dangling = 值 danger 字 + danger 8% 浅底 + ⚠ badge（CardBase::appendDanglingBadge）。散落的 accent 硬编码（图标/光环/fx 标签/角色色）已收口到 `Theme::tokens().accent`。画布侧 select 色仍在 `CanvasStyle.h`（canvas 层不依赖 ui，双 token 手动同步）。测试契约：test_formula_groups cardOrdinalsAndAlternate 已改为类型色断言。
- **Qt 性能**：每帧同步槽里禁止 setStyleSheet（re-polish 数毫秒级），只在离散状态翻转时调用；setText 做同值短路；批量操作禁用布局避免 O(N²)。
- **性能断言抗噪声**：Debug 构建性能波动 30%+，断言用宽松边界（如 ≤2.0x），勿用严格排序；验证算法优化要构造最坏场景（逆序深链）而非常见微差。
- **测量工作流**：测量任意两点发布 MeasureVariable（refName `M_xxx`，cm 域）；测量工具内 W 键循环 距离/水平/垂直 三模式（`MeasureKind`，ToolMeasure.cpp cycleKind；水平=|dx|、垂直=|dy|，求解刷新按 kind 计算于 MeasurementStore.cpp measureMeasureVars，序列化字段 "kind" 缺省=Distance 兼容旧档）；水平/垂直模式下两点在测量轴重合（dx/dy < 0.05mm）时第二击拒绝创建（toast 提示，停留在 SelectB）；测量卡值标签按模式加前缀（水平/垂直，普通距离无前缀）；"烘焙到操作层"=复制非移动（lengthFormula 活链接）；跨层附着单向（仅辅助层 follower、工作层 leader）；值循环预检只在 addAttachment 入口拦截。
- **公式引擎符号集（2026-11 扩展，ExpressionEvaluator.cpp）**：支持 `+ - * / ^`、一元 ±、括号（含全角/方括号/花括号归一化）、小写函数与任意名字变量（含中文）。单参函数 `cos/sin/tan/sqrt/abs/atan/asin/acos/floor/ceil/round`（可裸参形式 `cos60`/`sqrt2`/`cos前肩角度°`），双参函数 `atan2(y,x)/pow(a,b)/min(a,b)/max(a,b)`（必须括号+逗号）；**三角函数参数为度制，反三角函数结果为度制**（制版约定）；`^` 右结合、优先于一元负号（`-2^2==-(2^2)`）；小写 ASCII 保留给函数（大写引用名 B/MA_xxx 永不冲突；`cosine`/`cos_a`/`min2`/`atan2x` 等仍按变量名处理）；无 `√` 字符，统一用 `sqrt(...)`；域错误（sqrt 负数、asin/acos 越界、除零、非有限结果）返回错误而非 NaN。测试：tests/test_expression.cpp。

## 领域建模决策（用户拍板，勿翻案）

- **默认主题**：默认使用白色（亮色）模式（`Theme::apply(Light)`，src/main.cpp）；暗色仅经 视图 → 暗色主题（Ctrl+D）显式切换，不做持久化默认翻转。
- **面板悬浮窗（原变量悬浮窗，2026-08 重构；2026-08-28 ui-redesign 方案升级）**：变量/图层/组件 三个大标签统一进一个 Qt::Tool 悬浮窗口（侧边栏样式：长竖条、宽 ≤480、初始高按屏幕可用区 ≤660、贴主窗口右缘）；窗口头行 = 大标签（ElaTabBar，变量/图层/组件）+ 隐藏 ✕（`m_panelHeader`）。**图钉停靠方案已否决（2026-08-28 用户拍板）**：ui-redesign §4.1 曾提出「悬浮窗 ↔ 停靠右栏」双形态（已实现又移除）——用户反馈：右缘位置**特意设计为悬浮窗，本意就是不占用/裁剪画布**；停靠右栏（`m_dockHost` 嵌入内容行）会挤压画布，已整体删除（图钉按钮/setPanelDocked/panelDockHost 全移除，QSettings `panel/docked` 键启动时清除）。**面板恒为悬浮窗形态**：画布独占主区；保留头行 隐藏 ✕ 按钮、QSettings `panel/geo` 悬浮位置记忆 + `m_panelWindowPositioned` 归位短路。**组件大标签（2026-08-28 拍板，ui-redesign §4.2）**：组件管理从变量面板第 5 子标签升级为面板第 3 大标签（`m_componentTab` 由 MainWindow 持有，MainWindow.cpp setupPages），变量面板只剩 变量/公式/关联/测量 四个子标签。**子页签条 PanelSubTabBar**（`src/ui/PanelSubTabBar.h/.cpp`，QTabBar 子类自绘）：激活下划线 = piece 类型色（内容层信号，与大标签 accent 黄导航层分离），只读页签（关联/测量）恒 text2 不加粗。主窗口顶部标签 画布/变量/图层 中，后两个是面板开关：点击显示窗口并切到对应大标签页，再点当前分类 = 隐藏；**组件大标签无主窗口开关**——面板停在组件页时主标签高亮回画布（`syncPanelTabs` 的 cat<2 映射）。悬浮窗打开时对应按钮保持选中高亮 + 强调色圆点指示，X 关闭/隐藏后主标签回画布；悬浮窗内切换大标签 → 主窗口按钮跟随高亮。主窗口页面堆栈只剩画布页。主窗口标签条与面板大标签的同步入口：`onPageTabChanged` / `syncPanelTabs` / `eventFilter(Hide)`（panelWindow + 两根信息条 Show/Hide→`updateEditBand`）；面板大标签 `currentChanged` 必须同时切 `m_panelStack` 内容页 + 同步主窗口按钮高亮（漏接 stack 会导致内容永远停在变量页）。**状态栏改造（2026-08-28，ui-redesign §4.6/§6.5）**：SegmentEditBar + 智能笔预输入条从状态栏迁出为**独立编辑条带** `m_editBand`（状态栏上方，QSS `QWidget#editBand` = accentTint 底 + accentStrong 描边，全局 QSS 主题免刷新），SegmentEditBar 右端常驻「Esc 撤销创建 · Enter 确认」提示、串号徽章 `QLabel#serialBadge`（墨底 + accent 黄字）；诊断改 **danger 实心 badge**（`m_diagBadge`「⚠ N」，点击弹明细菜单，健康时不占位）；新增 `flashStatus` 瞬时反馈（保存成功 ✓ success 2s / 撤销重做文字 1.5s，条带编辑期跳过防刷屏，`m_lastUndoIndex` 方向判定：idx==count 为 push 不算重做）。**工具坞唯一实心黄（2026-08-28，ui-redesign §6.1）**：8 枚工具按钮装 `ToolDockStyle`（`src/app/ToolDockStyle.h`，QProxyStyle 自绘）——激活 = accent 实心黄底 + onAccent 墨图标（全屏唯一实心黄）、按压 accentStrong、悬停 surface2+text1、默认 text2；判据 = 有 `ElaIconType` 属性的按钮才接管（图层 chip 等走 Ela 默认样式）。**卡片行区分（历史沿革，2026-08-28 方案 A 终局）**：左侧竖线 = **类型色**（方案 A，见上方「卡片竖线色/字号」条目）——本条此前记录的「蓝/橙按行交替（rowEven/rowOdd，2026-08 拍板）」已于 2026-08-28 经用户重拍板**废除并删除 token**；`alternate` 构造参数与 `m_alternate` 保留为虚拟列表 bind 契约（视觉 no-op）。背景斑马纹方案已废弃删除（VirtualCardList 无 paintEvent 覆盖、无 stripe token——曾尝试 `ThemeTokens::stripe` 亮 #E3E8EF/暗 #333B45，用户反馈与底色区分度不够且不美观，两次迭代后放弃）；2026-08 曾被取消的类型色条以「方案 A 类型色竖线」形式回归（用户拍板：竖线标类型而非行奇偶）。**卡片列表底色 = 画布纸色 `ThemeTokens::canvasBg`**（2026-08 用户要求，亮 #ECEFF2 / 暗 #14181E，VariablePanel.cpp buildListPage + applyTheme、MeasureTab.cpp 同步）；**悬停删除按钮防跳动**：五张卡片（VariableCard/FormulaCard/LinkedCard/MeasureCard/AngleMeasureCard）的悬停显隐删除按钮都配同尺寸透明占位 `m_deleteBtnSlot` 互斥换显隐——布局空间恒定，按钮出现不引起行宽挤压/行高变化（否则 VirtualCardList 重测整列下移）。引用名输入框（2026-08 用户要求）：测量结果对话框「引用名」行改为可编辑输入框、初始为空（不显示自动生成的 M_xxx，留空保留自动生成名；输入即转大写）；变量/关联/测量/角度卡片上的引用名 chip 不再显示灰色占位文字「引用名」，无值时纯空框（tooltip 仍提示双击编辑）。**chip 静态态 = 输入框样式（2026-08 用户拍板，最终方案）**：所有 CopyChip（名称/引用名/公式名）**空闲时即显示输入框**——背景 `@surface`（hover `@surface2`）+ 1px 圆角描边，**由 `CopyChip::paintEvent` 自绘**（**坑：ElaText 不渲染 QSS 边框**——它自带内联表 `#ElaText{background-color:transparent}` 且文本路径不画 PE_Widget，QSS 的 background 能渲染（曾有"空白蓝色小方块"为证）但 border 完全无效，两次 QSS 尝试用户均反馈无差别）。**描边用专用中灰色**（亮 `#9AA4B2` / 暗 `#4E5866`，2026-08 三次迭代：`@border`→`@borderStrong`→专用色——前两版在画布纸色底上肉眼不可见，用户两次反馈"完全没有生效"；像素探针 chipBox/cardBorderPx/lightCardBorderPx 验证渲染），空值/占位态也是完整空输入框、不"空一块"；`m_label->setMinimumHeight(16)` 防塌成细条；编辑态覆盖层 (ElaLineEdit) 与静态框同尺寸同形制，输入结束收起后描边仍在；hover 由 chip 自绘（label 的 Enter/Leave 事件经 eventFil... (line truncated to 2000 chars)
- **智能笔预输入（tool 级机制保留，UI 已迁 ContextStrip，2026-12）**：预输入条（SmartPenPreInputBar）随 CONTEXT_STRIP_DESIGN 一期**删除**——`LinePreInput`/`ToolSmartPen::setPreInput` 机制保留（tests/test_smartpen_aux 直驱 tool 验证：长度+角度齐全 → 一击成线；仅长度/角度 → 预览锁该维度；无效表达式 toast 后忽略；值被使用后清空），但**应用内无 UI 喂值**（主窗口不再有预输入条；智能笔的条带交互 = 悬停只读预览 + 画线中实时读数 + 画完自动锁定，见 `src/app/ContextStrip.h`）。角度语义：自由起点 = 绝对世界角，吸附起点 = 跟随折角（闭合基准）。
- **桥接线范式**：自由线 + 长度公式引用测量变量 M_xxx + 可选端点跟随（起点跟随宿主 / 终点指向 endTarget，LinePropertyDialog 开关）；创建后与普通线段无异，无 isBridge/pin 标志。终点指向 = Block 的 endTargetBlockId/endTargetPointId/endTargetOffset 字段，Resolver Step 7 驱动（容差 5° 吸附，琥珀环高亮）。
- **曲线系统**：分段三次 Bézier + 过点锚点；AUTO 点 C2 曲率连续（solveC2Tangents Thomas 算法，tension 不作用于 C2 解）；MANUAL 点（autoTangent=false）存储切线为已知边界；智能笔只画直线，曲线编辑全部在 ToolCurveEdit（快捷键 C）；共线不等长切线手柄（tangentLocked=true 时跟随转向保长度）。**曲线缓存惰性重建（2026-09 用户反馈：跟随对象拖动时曲线抖动）**：`Block::resolve` 不再每帧无条件 `rebuildCurveCache()`——只在局部几何真正变化（geometryEpoch 增量 / 缓存未冷 / 曲线性变化）时重建（C2 重解 + 0.1mm flatten + 弧长积分每帧零开销）；纯刚体拖动（transform 变化）span 冻结，消费方（exitDirectionAtPoint/SnapEngine/画布/切线手柄）读的本地 span + 当前变换 = 精确刚体运动，消除每帧重算与微余量抖动。配套：**增删曲线点命令必须显式 `touchGeometry()`**（AddCurvePoint/RemoveCurvePoint redo+undo、ToolCurveEdit::placeCurvePoint 无 undo 路径——结构变了但点可能没动，漏 bump 会缓存陈旧）；`curveCacheBuilds` 计数器（Block.h）锁"拖动零重建"回归（test_resolver rigidDragKeepsCurveCacheFrozen）。**手柄视觉（2026-09 用户两轮反馈）**：手柄线细线（1px cosmetic、半透明 alpha 120）+ 控制点小圆（r1.6，**填充完全透明 Qt::NoBrush**、半透明青描边 alpha 150）——圆内不遮挡任何几何；演变史：r3.0 实心大点（不透明/太大）→ 纸色填充空心（仍是"不透明补丁"，盖住圈下线条，用户质疑"哪里有透明效果"）→ 真透明 + 缩小（ToolCurveEdit::updateHandleGraphics；命中半径 8px 语义不变）。桥接线禁止加曲线点。**打断曲线形状保持（2026-10 修复，用户报告"曲线打断不能维持形状"）**：`BreakSegmentCommand` 冻结切线必须与画布缓存**同解**——缓存 `rebuildCurveCache` 用 `AutoCurveMode::Hobby`（solveHobbyTangents），冻结必须也是 Hobby 解且**每点 in/out 分开存**（interior 点两侧手柄长度不同，压成一个值会出折角）；断点落在跨度内部（Interpolated 打断）时子跨度必须 de Casteljau 细分并按 **t0/(1−t0) 缩放切线**（前段最后跨度起点 out = 3(L.ctrl1−P_j)、断点 in/out = 3(B−L.ctrl2)/3(R.ctrl1−B)、后段首跨度终点 in = 3(P_{j+1}−R.ctrl2)）；断点为链点（CurveAnchor 打断）无子跨度，原切线即精确。**曲线结构变了点没动 → 显式 `++geometryEpoch`**（与增删曲线点同铁律，漏 bump = 打断后前段仍画旧 span 缓存）。**断点附件保留（2026-10 修复，用户报告"端点和组件连接打断位置跳变"）**：目标 = 断点的端点/组件连接**不删除**（断点仍是前段端点同 id 同段），参考方向由 interior 弦向变为端点切线/段向，按 `refDeltaRad` 补偿跟随角（角度/弧长分支、公式角度跳过）保证世界朝向零跳变；目标 = 原端点且端点被删 → 重指后段终点（原线尾部）、随线跟随不丢；曲线辅助点前后段分派按**弧长位置**（projectPointOnCurve().s）而非 interpPercent（弦向分数与弧长分数不可比）。
- **辅助计算层**：全局唯一、固定在图层列表最底部（index 0）、不可删除。层间契约：唯一出口 = 发布的测量标量；工作层对辅助层不可见、不可捕捉、不可附着。非激活时完全隐身，求解照常。
- **图层系统**：图层是纯粹"选择/显示过滤器"，求解器/测量/附着零改动。非活动层灰显（#9E9E9E+40%、不显示标注、不可选不可悬停）但可捕捉；最终可见性 = block.visible && layer.visible；新线归活动层；切换活动层自动清空选择。LayerPanel 的 refresh 必须 QueuedConnection（否则 use-after-free）；QTreeWidget 设了 stylesheet 必须显式定义 ::indicator。
- **同点堆叠捕捉与智能笔落点确认（2026-08）**：不同层点精确重合（≤1e-6mm，kTieDistSq=1e-12mm²，SnapEngine.cpp findSnap）时平局取活动层点——「先选活动层」；智能笔起点/终点 0.5mm（kSnapOverlapEps，SnapEngine.h 共享常量，ConnectGesture 同源）内多候选时：起点可点选候选线段切换落点（trySwitchStartPoint，仅池内 layerSnappable 合法目标；灰显辅助层候选点击忽略，防附着被拒静默降级）；终点多候选进入 ConfirmEnd 确认态（默认活动层点，点候选线段切换，空白接受默认，Esc 取消）。落点池只含合法目标，单向契约不变。
- **Resolver 性能**：settle 收敛 = BFS 拓扑一趟（森林不变式保证无环）；拖拽帧内增量求解（collectAffected BFS + affectedOnly + resolveForDrag 忽略跨选择集附件）；公式求值 = Kahn 拓扑序单趟；Step5 桥接仅在 bridgesMoved 时重收敛、Step7 仅在 rotated 时收敛。**每帧热路径铁律（2026-09 用户反馈性能专项，已锁定）**：凡是"每帧/每次输入"的几何变更必须走 `resolveForDrag(seeds)`（seeds = 实际变更的块；组件级用暴露端点成员 + componentClosure）+ `scene->syncBlockPositions()`（syncFromBlock 只重建 epoch/rotation 变化的块）；**禁止** `resolveAll()`（全文档重解）或 `refreshAllBlockItems()`（全画布删重建所有 BlockItem 缓存）。`blockReferences` 已覆盖终点指向/省道/曲线点 follow/polar/midpoint/交点/插值引用——跨块依赖不会被漏掉。已收敛的 4 处：ToolRotate::applyAngleDeg（旋转拖帧）、RotateCopyGesture::applyAngle/applyFormulaValue（旋转复制拖帧）、ConnectGesture::onAngleTextChanged（角度 HUD 击键，组件级种子=暴露端点成员）、ToolCurveEdit::dragCurveAnchorTo/dragHandleTo（锚点/切线拖帧）。**2026-09 性能专项第二轮（已收敛）**：①SnapEngine 每块坐标缓存（SnapEngine.h 私有 SnapBlockEntry/SnapSegBlockEntry，键 = geometryEpoch+transform+点数/段数——**增量缓存必须含 count 键**：删点/段可能不 bump epoch；mutable 成员 + prune 2×块数防长期会话膨胀；回归 test_aux_layer snapCoordCacheInvalidatesOnChange）；②`collectAffected` 引用索引（ParamDocument m_referenceIndex：target→referencers，惰性重建——失效点在 addBlock/removeBlock/addBlockRaw/clear + **resolveAll() 入口**（所有引用字段变更都经全量 resolve 落地），拖帧 resolveForDrag 不置脏；语义与 blockReferences 等价，自引用不入索引）；③组件包围盒签名缓存（CanvasScene m_componentBoxSig：成员 epoch+origin+rotation+zoom 的 FNV 哈希，未变零重算 boundingBoxOf；删盒时必须先快照 key 再 erase——erase 后迭代器指向下一项）；④VirtualCardList 值级刷新（`setValueBinder` + `refreshValues()`：setRows 同 keys 时走轻量值路径，未装则回退 rebindAll；LinkedCard/MeasureCard/AngleMeasureCard 的 refreshValue 已带同值守卫——LinkedHost 与 MeasureTab 已接线）；⑤settleComponents 收敛（2026-09 已修，原"剩余已知遗留"）：`applyComponentTransform` 返回**是否真的动了**（epsilon 门控：`std::abs(delta) < 1e-9 && translate.lengthSquared() < 1e-12` 判为无写入→false，ParamDocument.cpp:543），4 轮沉降循环**无移动即 break**（`const bool compMoved = settleComponents(...); if (!compMoved) break;`，ParamDocument.cpp:1847-1848），且 `settleComponents(atts, affectedOnly)` 在收窄求解时跳过不受影响的组件（leader 块与全部成员都不在 affected 集 → skip，:567-573；affected 集由 collectAffected 展开组件闭包 +... (line truncated to 2000 chars)
- **隐藏/灰显与交互**：隐藏实体仍可交互；灰显层可捕捉不可选。
- **连接手势半径（2026-09 拆分，用户拍板）**：目标吸附 / 光环 / 目标环由单一常量 `kConnectSnapRadius`（7.5px）统一驱动（WYSIWYG 铁律：光环 = 目标环 = 吸附范围）；**源端点抓取**单独用 `kConnectGrabRadius`（10px）——抓取必须好抓、落点必须精确，两半径分离。两常量均为 `inline constexpr`，定义于 ConnectGesture.h 顶部（namespace cad::tools），ToolSelect 悬停端点提示共享同一值（勿在别处复制）。
- **拆开保留角度（2026-08 用户拍板，最终方案）**：拆开只解除**位置吸附**、不解除**角度跟随**——跟随线 A 的旋转仍由基准线 B 驱动（A 的角度 = B 当前方向 + 拆开前的跟随角 α），B 旋转时 A 跟着转，平移 A 不动角度。实现 = Attachment 新增 `angleOnly` 字段：Resolver applyAttachment 只写 rotation、跳过位置约束（Resolver.cpp）；**拆开路径统一转 angleOnly**（**D 键快拆 ToolSelect quickDetachSelection（2026-09 新增）**、拖跟随线拆散 ToolSelect endDrag——**新建连接默认焊接（2026-08 用户拍板复旧：2026-10 曾改为默认不焊（拖动保护可选），2026-08-24 用户再拍板回滚——addAttachment/addComponentAttachment 对新建连接强制 isLocked=true，整对移动为默认语义；拆散 = D 键快拆 / 面板取消「拖动保护」；字段级默认与旧档迁移不变）；多线整体移动仍可交给组件 componentClosure）——面板上仅角度由拖拆/D 键产生；属性面板「拖动保护」是可选焊接开关（✓=isLocked 焊接拖不拆、✗=解焊仍完整连接，仅角度态勾选 = 恢复完整连接+重新焊接）），undo 宏 = `SetAttachmentAngleOnlyCommand` + MoveBlockCommand；**彻底断开是单独操作**：「位置吸附」取消勾选（2026-10 新语义，RemoveAttachmentCommand，断开后记忆最近宿主可一键恢复）/「清除」按钮 / 旋转工具锚心移离吸附点（旋转=放弃跟随，仍走 RemoveAttachmentCommand）。angleOnly 与拖动保护互斥（置位自动清 isLocked；lockedClosure 跳过 angleOnly），恢复完整连接（面板勾回「拖动保护」/ 命令 undo）位置重新吸附回宿主点并**重新焊接**（SetAttachmentAngleOnlyCommand redo(false) 置 isLocked=true，不得沿用 m_oldLocked=false）。序列化字段 "angleOnly"（Optional since v5）；旧档 isLocked=true 的连接读出来保持焊接（不迁移）。
- **滑轨模式（抽屉式滑动，2026-08 用户拍板）**：连接姿态保持（角度跟随不破坏），但位置只留一个自由度——**基准线局部系**（x=沿基准线方向、y=垂直；基准线旋转时滑轨跟着转）下的单向滑动，用于微调。模式A「沿线滑动」（AlongLeader）：连接点沿基准线方向滑、垂直偏移锁定（激活时快照 slidePerpMm，默认 0 贴线）；模式B「垂直拉出」（PerpLeader）：连接点垂直基准线拉动、沿线位置锁定（slideAlongMm）。实现 = Attachment 新增 `SlideMode` 枚举（None=0/AlongLeader=1/PerpLeader=2）+ `slideAlongMm`/`slidePerpMm`（mm，基准线局部系坐标快照）；Resolver applyAttachment 位置**只从存储坐标解算**（不做现场投影——基准线刚体移动时局部坐标不变、跟随线随滑轨刚性携带）；拖拽工具（ConnectGesture.move / ToolSelect.updateDrag）每帧调 `ParamDocument::updateSlideOffsetsFromCurrent` 回写**自由轴**坐标、锁轴坐标保持激活快照；拖动 undo = `SetSlideOffsetsCommand`（redo 只写坐标不 resolve——避免与同宏 MoveBlockCommand 的 resolve 双倍位移；undo 必须 resolve——宏撤销时 MoveBlockCommand.undo 已按新坐标钉位，最后需再结算一次）+ MoveBlockCommand。滑轨与拆开（angleOnly）互斥（进滑轨清 angleOnly+isLocked，切回 None 重新焊接）；拖动保护在滑轨态禁用（位置必须可滑动）、lockedClosure 跳过滑轨附件、ConnectGesture 快拆循环与 ToolSelect m_detachedAttachments 收集都跳过滑轨附件（端点/整线拖动保持滑轨约束）。入口：属性面板「滑轨」行 = 水平/垂直两个**数值或公式**输入（cm 域；**公式优先**：`slideAlongFormula`/`slidePerpFormula` 序列化 since v7，其余同规则；纯数字即清该轴公式；拖拽沿自由轴 = 手调→清自由轴公式；留空=0；只填一轴也合法——水平轴非空→AlongLeader、垂直轴非空→PerpLeader、两轴都 0/空 = 退回全连接，2026-12 用户拍板 v3 表单）；**数值回显去尾零**（`Units::formatCmTrimmed`/`formatNumberTrimmed`/`formatDegValue`，2026-12 用户拍板：不要 .00——数字回显 2.00→2、45.0→45、10.00cm→10cm）；激活自动快照锁轴并解锁（可滑动）；拖动跟随线只沿滑轨动；重定向指向点后 `refreshSlideOffsets` 在新基准线局部系重快照。序列化 "slideMode"(int，枚举范围校验)/"slideAlongMm"/"slidePerpMm"（Optional since v6）。

- **连接卡片（2026-12-15 用户拍板：删除「跟随/独立线段」模式下拉，卡片纯状态驱动）**：模式 = 状态投影而非用户选择——无连接线段、也无引用线段 = 已经等于独立线段，「跟随」严格 = 真的有连接（Attachment 存在），做卡片模式切换是多余设计，已整体删除（m_cmbMode/m_modeRow/refreshModeCombo/onModeChanged/detachWithCache 全删）。**统一表单 + 面板重设计（2026-12-15 用户拍板，设计体系取经 m3/HashiCorp Helios/NN-g/8pt 栅格）**：不做面板状态区分（同一张卡同样行集标题恒定「连接」），且**v3 定稿（2026-08-26 用户拍板：去卡框化单列 inspector，v2 两列四行卡片栅格/1060pt/ElaScrollPageArea 卡框/标签列 92 全部作废）**：属性页 = 五个分区（黄竖条标题 QFrame#accentBar + hairline QFrame#divider 分隔，无卡框）：基本 → 几何（长度+角度行组 SegmentAngleCard+曲线弧长/张力+延长行组并入）→ 外观（线型实/虚/点分段预览按钮 + 粗细细/中/粗分段+spin 常量可见 + 颜色色板 chip 带 hex 读数 + 显示 名称/长度/可见 toggle chips）→ 端点（起/终双列微卡 QFrame，objectName startPointCard/endPointCard，测试查找契约）→ 连接（SegmentConnectionCard+SegmentRefCard 行组堆叠，分区标题右侧状态 hint）。**子组件全部去卡框**：SegmentExtendCard/SegmentConnectionCard/SegmentRefCard 基类 ElaScrollPageArea→QWidget、卡内标题删除（分区提供标题，「连接」恒定标题由分区承担）；SegmentAngleCard 本为行组。**尺寸规范（v3）**：行内控件高 35px 统一，标签列 64（12px 短词无冒号），输入定宽（主 150/140、小 90/110、注入 70），按钮 58×35，数值读数等宽 Consolas dim（dimValueStyle+kMonospaceFamily），输入禁用 addWidget(edit,1) 自适应伸缩（基本区名称框除外，整宽）；整页 ~540pt 宽单列 + 纵向滚动兜底；**ElaTabWidget 必须 setTabSize(116,35)**——ElaTabBarStyle 默认 220×35，4 枚 = 880px 超出窄面板后 tabRect 溢出 bar 范围、点击/命中判定落空（test_dialog_tabs::probeTabHitArea 锁定）；延长行标签 = 起点延长(P12)/终点延长(P13)（2026-12 用户拍板：补全"延长"文字、去掉简写"起/终"）；端点微卡标题 = P# · 起点/终点；连接/引用卡内行结构拆两行（连接线段|连接点、基准线|基准点）。**连接卡拆分**：角度/引用/指向自 SegmentConnectionCard 抽出（SegmentAngleCard.h/.cpp + SegmentRefCard.h/.cpp 新文件，旧 Angle/RefAim .cpp 删除）；「位置吸附」**终局改名「连接线段」**（无状态区分后该行恒为连接语义；历史反复原因：此前需为自由视图保留"吸附入口"表达，现有统一表单已无此需求——记录在案不再回退）。①连接行恒显 = 「连接线段」 + 连接线段 L#·名 + 连接点 P#（输入回车 建立/重定向）+ 「清除」+「拆开」（无连接/仅角度态禁用仍显示）；②连接子状态由 「连接线段」复选框（唯一连接开关，勾选=连接默认焊接，取消=彻底断开）/ 独立角度☐ / 引用线段行（自由态预填、连入自动落库）/ 滑轨行（无连接禁用）/ 连接保护 表达——恒显但按可用性禁用；③角度行 caption 按语义切换短词（跟随角/弧长/独立角/角度，单位见占位符）且提示 = 绝对角度/世界角度。**断开记忆快照 m_modeCache**（原模式切换缓存并入复选框路径）：取消勾选/清除时快照完整 Attachment（宿主/角度/弧长/公式/角度基准/滑轨/角度独立/焊接）后拆除（RemoveAttachmentCommand），重新勾选时优先 AddAttachmentCommand 原样恢复（宿主已删/线段无法解析/校验失败 → 丢弃快照，退回记忆宿主新建，backSolve 反算零跳变）；setTarget 换线时重置。**旧代码已删**（本代+上代合计）：m_userMode（五态"用户所选模式"记忆）、“跟随·未连接”标题、freeAngleRefMode、onAngleRefPointResolved 的 combo==1/2 死分支、m_userMode==1 连接后置 angleIndependent、m_cmbMode/m_modeRow、onModeChanged、refreshModeCombo、detachWithCache。滑轨为两个数值输入（沿向/垂直，默认空）；终点指向（`SegmentAimCard`）并入连接卡片；`Attachment` 新增 `angleRefPointId`，角度基准方向优先使用所选点的出口方向，旧档缺省回退原 start→end 方向。（2026-08 用... (line truncated to 2000 chars)
- **选择工具交互（2026-09 用户拍板，取消确认基准）**：**单击 = 选中（不拖动）；按住线身移动 = 拖动**（press 后移动超 `kDragThresholdPx`=5px 即进入 Dragging，锚点 = press 位置，与旧"press 即拖"位移语义一致；release 无移动 = 单击）。多选模式（W 切换）：点击增减（已选块上单击 = 减选，未选块 press 即加入），已选块上按住 = 整体拖动。**端点 press（已选块，`kConnectGrabRadius`=10px 内）= 连接手势**（未选块端点 = 选中）。**悬停动效**：无按钮移动时——已选块端点 = 十字光标（可连接）、线身 = 抓手（可拖动）、空白 = 箭头（`updateHoverCursor`，同值短路）。**右键语义**：有选中 = 上下文菜单（取消选择）；无选中空白 = 切回智能笔。**SelectState::Confirmed 保留枚举**（ConnectGesture/CopyDragController 仍发该值），`ToolSelect::setState` 统一归一化为 Selecting（选中即就绪）。Ctrl+press 已选块 = 快捷复制。拖动阈值**分档**（ToolSelect.cpp 匿名命名空间，M6）：press 未选中的块 = `kDragThresholdPx`=5px；press **已选中**的块 = `kDragThresholdSelectedPx`=10px（`dragThresholdPxFor(pressWasSelected)`）—— 选集已建立时 press 的唯一意图就是拖，而误拖会静默改几何，代价更高，故放宽；5~10px 这段模糊带改为偏向"不动几何"。注意单选模式下 `wasSelected` 必须在覆写 `m_selection` **之前**取。端点抓取半径共享 `kConnectGrabRadius`（ConnectGesture.h）。**重叠点源点规则（2026-09 用户设计提案，回归已锁定）**：单选模式下「先点选线段、再点击重叠部位」= 连接源点恒取当前选中线段的所属点——`tryPointOperation` 候选点先按"选中块"过滤（未选线段的重叠点直接排除，选中线段的点即使不是最近点也胜出，ToolSelect.cpp:831-838），**过滤后为空 = 退回普通选中/拖动**（未选线段的端点按下 = 普通线段选中——勿对空容器取 front，MSVC Debug 断言崩溃，见 TROUBLESHOOTING.md 第 7 组），多个**已选块**端点同点重叠（`kSnapOverlapEps` 0.5mm）才进 ConfirmSource 点选候选线段确认；未选线段的端点按下 = 普通线段选中。测试：test_select_wkey 的 singleModeOverlapPrefersSelectedSegmentPoint / singleModeOverlapWinsEvenWhenOtherPointCloser / multiModeOverlapEntersSourceConfirm / unselectedEndpointPressFallsBackToSelect。
- **重叠线段消歧（2026-10 用户确认方案，共享候选集合三阶段）**：完全/近邻重合的两条线段（如旋转复制 0°、测量线+烘焙副本、链接到同一基准），选择工具按三个互补入口消歧——①**悬停提示（C）**：无按钮悬停到 ≥2 候选处，画布 HUD 显示「此处重叠 N 条 ｜ 名称、名称（点选后按 W 循环）」（同值短路、不随光标逐帧重定位）；②**点选 + W 循环（A）**：单选模式单击集群 → 选中 hitBlock 首选（堆叠自上而下 = 后建者在前），激活循环上下文（快照候选 + 常驻 HUD「重叠 N 条 ｜ 第 i/N ｜ 角色 名称（W 循环）」锚定集群处）；W 循环下一候选（逐位回绕、实时剔除已消失块）；**仅单选模式启用**（多选有加减选语义）；**W 冲突仲裁**：上下文激活时 W = 循环，否则照旧切换多选/单选；**退出上下文**：Esc / 点空白 / 点非集群线 / 右键取消 / 层切换 / 工具切换 / 拖动提交；③**右键候选菜单（B'）**：右键处 ≥2 候选 → 菜单追加「重叠候选 (N 条)」子菜单，逐项「角色 名称 · 层名 · 长度」，点选即选中并**退出**循环上下文（菜单是显式终点）——长按候选弹窗方案已否决（500ms 定时器与 kDragThresholdPx 拖动仲裁冲突 + QTest 时序 flaky 前科），改挂既有右键菜单。**候选集合规则**：`collectOverlapCandidates` = 场景 items 命中（含曲线子项上溯）+ 活动层过滤（与 hitBlock 同规，跨层重叠靠图层过滤区分）+ 距离阈值内最近线段；候选 0 = hitBlock 首选。**信息底座**：候选显示名 = 线段名 → 块名 → 线段 serial；菜单项带 角色（轮廓/内部/辅助）+ 层名 + 长度。测试：test_select_wkey 新增 overlapHoverShowsClusterHint / overlapClickCyclesWithWKey / overlapPickCandidateByIndex（事件流驱动：真实鼠标移动 + W 按键 + 命令式点名）。
- **组件（Component，2026-09 用户拍板，打包工作组 + 参数化活性铁律）**：多选（≥2 条线段）右键「创建组件」把选中的 Block 打包成组件。**组件只是打包/管理容器，绝不冻结或覆盖成员内部参数化关系**——成员间的 attachment / 变量 / 表达式继续照常求解（组件不是刚体、不产生 rigid sync；整体移动/旋转是给所有成员施加同一刚体变换，刚体变换保持内部相对几何，内部 attachment 自然继续满足）。数据模型 `src/parametric/Component.h`：`id/name/memberBlockIds/exposedPointId/exposedSegmentId/showBoundingBox/defaultAngleDeg`（无 anchor/offsets）。**外部连接 = 组件级 attachment**（`Attachment::fromComponentId`，与线级 fromBlockId 互斥）：组件整体作为 follower 跟随外部线，**借用暴露端点连接 + 借用端点线段方向形成夹角（**组件接谁谁就是基准线**：组件级连接拖到哪个对象即为 leader，重叠候选时直接连最近者，无需 ConfirmTarget 点选确认；**连接后若连接点仍有多个点重叠（kSnapOverlapEps 0.5mm），角度窗口（AngleInput）内点击重叠点的线段线身即可切换跟随基准**（用户 2026-09 拍板：点选重叠点的线段确定跟随对象；候选 = 连接点上非组件成员、非当前基准的线段端点，橙线悬停高亮同 ConfirmTarget 视觉语言；切换 = removeAttachment + addAttachment 全量校验（环/跨层拒绝回滚），followerAngle 反算保向零跳变，提交后整步 undo——ConnectGesture::collectComponentSwitchCandidates/switchComponentTarget/pressAngleTarget，测试 test_component 的 componentConnectOverlapSwitchTarget；连接点无重叠则无候选、行为不变）。`applyComponentTransform` 对所有成员施加同一刚体变换：绕暴露端点旋转 + 平移使暴露端点落 leader 点；内部关系不受影响）。**森林不变式分两维**：线级照旧（每 block 至多一条 incoming）；组件维度每组件至多一条外部跟随线（`addComponentAttachment` 校验 + 无环检查）。**暴露端点自动记录**：首个组件级连接时引擎 `recordExposedEndpoint` 记录借用端点（addAttachment/addAttachmentRaw 同步，removeAttachment 断开时清空）——无需单独命令。**一个组件一条外部连接**；断开/拖组件（`ToolSelect` 把组件级连接加入 m_detachedAttachments 彻底删除）即还原。组件可作 leader（外部线线级吸附到成员端点，组件动时跟随）。**包围盒** = 成员几何并集外接矩形（虚线 overlay + 5px 外扩，`CanvasScene::refreshComponentBoxes`，管理页开关）。**管理页** = 面板悬浮窗第 3 大标签「组件」（2026-08-28 ui-redesign §4.2 升出变量面板；`src/ui/ComponentTab.h`，由 MainWindow.cpp setupPages 持有装配）：改名/成员数/包围盒开关/回到默认角度（`ResetComponentAngleCommand` 整组绕包围盒中心旋转）/解散（拆组保留线段与内部关系）/删除（整组+成员一起删）。**组角度管理（2026-09）**：管理页每行两个角度输入——**原始角**（`defaultAngleDeg`，「回正」的目标，可编辑→`SetComponentPropertyCommand`）与**对接角**（组件级 attachment 的 `followerAngle`，有对接时可编辑→`SetFollowerAngleCommand`，无对接显示 —）；组件有对接时「回正」禁用（朝向由对接决定），并显示「断开」按钮（`RemoveAttachmentCommand`，断开后可回正/重新对接）。**整体拖动** = `ToolSelect::beginDrag` 经 `componentClosure` 展开到全组件；**脏传播** = `collectAffected` 入口展开组件闭包 + `m_followersOf` 组件边（leader → 所有成... (line truncated to 2000 chars)

- **组件整组旋转（模态已删除，2026-08-29 用户拍板；原 2026-12 W 键模态，COMPONENT_ROTATE_DESIGN.md 留作历史档案）**：旋转工具的「W 键组件整组旋转」模态连同 `RotateComponentCommand`（ComponentCommands.h）一并移除——执行 ROTATE_REDESIGN_DESIGN.md D1 既有拍板（整组模式被多选旋转覆盖）。**选集旋转路径 2026-08-29 同批删除（用户拍板：整组旋转的交互没设计好，属历史包袱，先删、日后重新设计）**：多选 → R 的选区继承（D9，`ToolManager::switchTool` 快照 `ToolSelect::selection()` → `ToolRotate::adoptSelection`）连同 `RotateBlocksCommand`（BlockCommands.h）、`AimRelease`/`DartRelease`（ComponentCommands.h）一并移除；`ToolRotate` 内 Sel 系方法（`beginSelPivot`/`beginSelRotation`/`applySelDelta`/`commitSelRotation`/`collectShadowAttachments`/`snapAnyPoint`/选集虚线包围盒）与选集状态字段全删。**当前多选后按 R = 普通单线旋转，产品内无整组旋转入口**。`baselineOffsetDeg` 字段保留（连接卡「基准偏转 / 归零」是其唯一写入方）。测试 tests/test_rotate_copy.cpp 的 selRotate* 4 例 + 影子 4 例已删（`wTogglesGroupModeOnlyForComponent` / `groupRotateReleasesExternalConstraints` 早前已随 W 模态删除）。原两段式语义（锚点任意 + 跨界连接以影子偏转保姿态而非删除、锚点不存储、一步 undo）记录在 ROTATE_REDESIGN_DESIGN.md，供重新设计时取用。
- **端点延长线（用户拍板 2026-?，设计文档 EXTEND_LINE_DESIGN.md 为权威）**：`Segment` 新增 `extendStartMm/Formula`、`extendEndMm/Formula`（**内部存储 mm、公式 cm 域**；**属性面板延长卡界面数值与公式统一 cm——2026-11 用户拍板，卡内 cm↔mm 换算，SegmentExtendCard.cpp**，≥0，默认 0）。**核心不变式：`resolvedPos` 恒为"本体"（参数化真值，比例类定义零改动）；`Block::resolve` 末尾 `applyEffectivePositions()` 把端点"实际位置"（本体 + 延长量 × 出方向，出方向 = exitDirectionAtPoint 语义）写入 `m_effectiveLocal` 缓存，resolve 开头 `evaluateExtendValues()` 求值延长量入 `m_extendEval`（求解 pass 内即见）；`worldPos()`/`effectiveLocalPos()` 读实际位置**。消费方分轨：按点位置定义（绘制/SnapEngine/附着/测量/交点宿主段/长度显示）→ 实际；按本体长度/比例定义（辅助点 percent/constant/fromEnd/refPoint、曲线锚点、方向基准、打断分派）→ 本体。**只改尾巴不动本体 → 可视变化必须显式 `touchGeometry()`**（applyEffectivePositions 比较 + SetSegmentExtendCommand redo/undo；SnapEngine 缓存键含 epoch 自动失效）。拍板规则：卡片起/终各一栏；只往外（负值钳 0）；曲线/桥线/省道线整卡灰；粘死端/同块角点/组件暴露端点单端灰；跨线共点跟随线自动跟实际端点；尾巴可捕捉/连接/测量但**禁建辅助点**（`segmentSnapWithinBase`，ToolSmartPen/ToolBreak 拦截 toast），打断仅本体范围；打断延长量随端点归属（原终点延长量快照进 BreakState，后段 buildBackBlock 继承；前段断点=0）；显示/测量 = 实际长；`extend*Formula` 进 formulaRefs `consumedBy`/deleteImpactReport linkedFrozen/MeasurementStore 消费扫描；命令 = `SetSegmentExtendCommand`；Duplicate 深拷贝自动携带。测试 tests/test_extend.cpp（15 用例，含画布像素级刷新回归 `canvasRefreshOnEndExtend` 与真实文档渲染回归 `realDocExtensionMovesEffectiveEnd`）。

- **线段换向（角度基准视角切换，用户拍板 2026-08；v2 放开 + 自动补偿）**：让线段不局限于"起点先建、终点被驱动"——属性面板角度行组下「角度基准 P1 → P2」说明行 + 「换向」按钮（SegmentAngleCard 第二行），点击 = **物理换身**（`ReverseSegmentCommand`，BlockCommands）：交换 `startPointId/endPointId` 身份 + 驱动端 Polar 约束搬到另一物理端（角度 `normalizeDeg360(+180)` 视角补偿、距离/距离公式原样转移）+ 新起点落为 Free 锚点。**换向后几何零跳变、修改长度/角度驱动另一端**（链式 A—B—C 中间换 AB = 重定根）。**延长量与换向无关（用户拍板）**：物理尾巴不动，`extendStart↔extendEnd` 随端点角色互换；宿主辅助点仅翻转 `interpFromEnd`（位置零漂移）。**采用物理换身而非持久 reversed 标志**：换向后模型回到标准形态，序列化/Duplicate/旧档零改动。**v2 放开 + 补偿（世界姿态零跳变）**：①基准段消费者（Polar `refSegmentId`=本段）角度 +180 / 公式包 `(%1)+180`；②相对交点宿主（非世界角/双向/指向点）`interAngle` 同式补偿；③跟随连接 followerAngle +180·k（k = 方向参照翻转次数：跟随侧 localDir=`directionAtPoint`=段弦向翻转、角度基准侧 exitFlips 仅辅助点/曲线锚点翻转——端点出方向换向不变；两次翻转偶数抵消不补偿；angleIndependent 不驱动旋转不补偿）；④被连接 + 旧档空角度基准点 → **回填 `angleRefPointId`=旧终点**（其出方向=原 start→end 基准，精确吸收翻转；undo 恢复空=旧档语义）；⑤曲线保形：**Hobby 自动切线求解不保证换序对称（实测换向弧长漂移 118.9→251.6mm）**——与打断冻结同范式，换向前从曲线缓存 spans 还原每点"同解"有效切线（`out=3(ctrl1−P0)`/`in=3(P3−ctrl2)`，内部锚点 k=1..n−1 全量捕获——**循环上界曾漏最后一个拼接点致半边漂移**），镜像后**冻结 autoTangent=false**，锚点 `interpPercent→1−p`/`interpOffsetDist` 取反；**坑：Hobby 求解器手动点方向按 `atan2(tangentOut)` 统一取、长度按 `|tangentIn|`——曲线端点 out 必须与 in 同向非零（写零向量方向塌成 +x）**；undo 恢复原 autoTangent/存储切线。**v1 遗留坑：applyState 内曲线冻结之后不得再写端点切线**（曾有一段旧镜像代码用原始存储切线覆盖冻结值→静默清零）。**v2 仍拒绝（canReverse 逐条 + 中文原因）**：桥/省道/终点指向、角度测量引用（start→end 是测量基准）、端点被块内其他线段共享、非"锚 Free+驱动 Polar(ref=另一端,无 refSegmentId)"结构、滑轨模式连接（基准线局部系快照会镜像）、需补偿的弧长模式连接（πr 不可参数化表达）。UI 资格在 `refreshBasisRow` 预判（不合格隐藏按钮+tooltip 原因），命令内再验；对话框经 `SegmentAngleCard::reversed()` 信号全量重填。**UI 联动三坑（2026-08 用户报告后修复，详见 TROUBLESHOOTING 第 4 组）**：①属性对话框 `populateFromModel` 回填必须全字段 QSignalBlocker——端点角色互换后，回填回波会把另一输入框的旧名写进新角色端点（名称永久覆盖，数据损坏级）；②角度卡 `onDocResolved` 自由线分支必须重算刷新世界角标签（曾直接 setVisible(false)，换向 push 内的 resolveAll 广播把标签藏掉）；③延长卡曾不监听 resolved，换向互换延长量后输入框滞留旧角色值。测试 tests/test_commands.cpp 10 用例：保形+驱动端互换+undo/redo / 延长尾巴 / 辅助点 / 基准消费者补偿 / 相对交点补偿 / 跟随角补偿 / leader 出方向稳定 / 旧档角度基准回填 / 曲线保形（逐 span 控制点世界镜像 1e-6 + 弧长相等）/ v2 拒绝（滑轨/弧长）。v3 候选：弧长模式补偿（冻结弧长常数+换算）、世界角交点宿主段曲线分支。**2026-12 三个 UI 收尾（用户报告「换向部分恒显 P1/P2、点击不调转；旋转选锚心条带无变化」）**：①**条带基准读数改真实串号 tag**（`ContextStrip::refreshChrome`，与属性对话框 `SegmentAngleCard::refreshBasisRow` 同口径 `Serial::tag`）——旧实现 `name.isEmpty()? "P1" : name` 硬编码兜底，普通画线端点无名 → 恒显 P1→P2、与端点真实身份无关、换向后也不翻转；现在换向后随 start/end 身份互换翻转。②**画布方向指示**（`src/canvas/DirectionMarker.h` `drawDirectionChevron`，BlockItem 直线中点 + CurveItem 弧长中点 labelAngle 切线）：起点→终点小箭头，换向后缓存重算自动翻转——换向几何零跳变，这是画布上唯一的换向可见反馈；灰显层不画。③**旋转会话内条带「换向」= 切换锚心**（用户拍板：旋转工具的换向意思是换选择的锚点）：通道 = `ToolRotate::reportRotateAnchorState` → `ToolHost::setRotateAnchorState` → ToolManager emit `rotateAnchorStateChanged` → MainWindow → `ContextStrip::setRotateAnchorState`（基准读数锚心端在前 + 换向按钮资格=Ready 且端点无连接）；点击 = ContextStrip emit `reverseRequested` → `ToolManager::forwardReverseRequest` → `Tool::onReverseRequested`（默认 no-op）→ ToolRotate override = `toggleAnchor()`（gizmo pivot 环移动），**不 push ReverseSegmentCommand**；普通（非旋转）会话换向仍由条带直接 push。测试：test_context_strip +2（basisShowsSerialTagsAndReverseFlips / rotateAnchorStateFlipsBasisAndRoutesReverse）、test_rotate_copy +1（stripReverseTogglesAnchor）。**二轮补洞（2026-12 用户复核「旋转工具换向逻辑不对」）**：①`selectTarget` 的 `reportRotateAnchorState` 必须在 `m_state=Ready` **之后**（active 判定 = m_state != Idle，提前上报 active=false → 条带不进旋转会话 → 换向错走命令路径、gizmo 不动）；`onDeactivate` 相反——必须**先清 m_blockId/m_state 再上报**（否则陈旧 active=true 残留条带）。②条带角度字段（自由线）旋转会话内显示锚心基准角（锚=终点 +180°），输入回写减回偏移（ContextStrip.cpp refreshFields/applyAngle）；`setRotateAnchorState` 追加 refreshFields。③`updateStatusHint` Ready 分支附带锚心串号 tag（确定提示），toggleAnchor/点端点切换补调用。回归：test_rotate_copy `anchorSwitchSyncsStrip`（真实接线全链路）、test_context_strip 补角度断言。

- **影子偏转角 baselineOffsetDeg（用户拍板 2026-08-27，设计文档 ROTATE_REDESIGN_DESIGN.md §2.6 为权威；选集旋转部分已于 2026-08-29 删除）**：`Attachment` 新增 `baselineOffsetDeg`（**基准影子偏转角**，度，默认 0；序列化 Optional since v11）：有效角度基准方向 = 真基准出方向 + 本值。动机——组内连接引用组外角度基准（B 连 C、基准外指 A）时，整组刚体旋转后解算器把 B 朝向拽回原样（关系活着、姿态守不住）；影子机制 = "给真基准配一根随组转动的隐形影子 A2"，其唯一状态就是本字段。规则：平时恒 0 行为逐位不变（旧档零迁移）；**批量写入方已删（2026-08-29）**：原「凡『被驱朝向属于旋转集 S、而角度基准方向在 S 外』的活跃连接（angleRef 显式外指 / 跨界降级残留跟随），随组旋转 δ 时 offset = base + δ 同帧写入」（`applySelDelta` 内 `updateBaselineOffsets`，非逐帧累加防漂移）随选集旋转一并移除；目前仅连接卡「归零」按钮经 `SetAttachmentBaselineOffsetCommand` 单点写入；不动 followerAngle/公式（影子位于公式求值链之外，公式原样存活）；滑轨局部系不吃本值；连接删除即字段陪葬（断开=彻底自由，不做转接）。Resolver 注入点 = applyAttachment 的 refWorld（RotationMode 两分支共用）。**命令与选区继承均已删除（2026-08-29）**：`RotateBlocksCommand`（任意 S 集 old/new 位姿 + ShadowAtt{demoted 全字段快照,old/newOffset} + AimRelease/DartRelease + releasedAtts，restore-then-replay、undo verbatim 还原）与 `ToolRotate::adoptSelection` 选区继承接线同步移除；`AimRelease`/`DartRelease` 快照结构（ComponentCommands.h）因无第二用户一并删除（原组件入口 `RotateComponentCommand` 已随 W 模态删除）。**当前整组旋转无入口** —— 重新设计时 ROTATE_REDESIGN_DESIGN.md §2.5/§2.6 的判定表与影子机制可直接取用。UI：连接卡「引用线段」行后新增「基准偏转」读数行（连接存在且角度被基准驱动时恒显——2026-08-28 拍板"常显可控"，0 时归零按钮禁用；角度独立态隐藏；行内控件高 kFieldH=35 遵 v3 规范——漏设会导致 test_dialog_tabs::connectCardUniformHeights 红）+「归零」按钮 → SetAttachmentBaselineOffsetCommand（一步 undo，物理转向基准现向）。测试：原 tests/test_rotate_copy.cpp 影子四用例（shadowHoldsPoseWithExternalRef：转后姿态守恒+A 动 B 仍跟+undo 全还原 / shadowSurvivesFormula / shadowSelectionAdoptRotate / shadowUndoRestoresBase 跨轮累计）已随选集旋转删除，字段的批量写入回归待新入口落地后补。**单线确认流（D15，用户拍板 2026-08-27 已落地）**：旋转工具单选 = 选中态（点空白取消/点端点或 X 切锚向/不可拖动）→ 右键或无焦点回车确认 → 确定态（一切按压=拖动起手，Ctrl 旋转复制保留即兴入口）→ 拖动提交后**回落选中态**（再拖需再确认）；回车按 HUD 焦点仲裁应用角度/确认；Esc 分层反悔；锚向切换为选中态专属（连接线锚点被占用仍禁切）；旋转会话期（`hasSessionTarget`）右键上下文菜单屏蔽（`CanvasView::contextMenuEvent` 早退，发布菜单与确认手势互斥）；HUD 数值提交不占确认门且提交后回显（`ToolRotate::onHudCommit`）。**确认门必须有可视（2026-08-29 TOOL_SYSTEM_AUDIT H2 补齐）**：确认态翻转统一走 `ToolRotate::applySelectionConfirmed(bool)` 入口（写标志 + `RotateGizmo::setConfirmed`：未确认 = 虚线弧 + 空心锚环 + 减淡配色 / 确认 = 实线弧 + 实心锚环；HUD caption 追加「（右键/回车确认）」，经 `AngleHud::captionText()` 可测）——此前翻转点散写标志位、gizmo 两态同图、HUD 无提示，正确实现被用户感知为"工具卡死"；状态栏旋转提示（toolHintText 表）同步改述确认门。**翻转点禁止直接写 `m_selectionConfirmed`**（selectTarget/clearTarget 的重置除外，二者随后必走 buildGizmo/updateGizmo/showHud）。测试 d15GateRequiresConfirmBeforeDrag / d15DragCommitDropsToSelected / d15BlankClickClearsSelectedTarget / d15AnchorFollowsClickedEnd / **d15ConfirmStateHasVisualAndCaptionHints** / **hudShortcutOverrideContainment**，既有 16 用例补 `sendConfirm` 确认步。**锚心跟随点击端**：selectTarget 接收点击位置 —— 自由线锚 = 距点击更近的端（无距离阈值，点哪半段锚就在哪半段的端点，平局取起点）；连接线锚恒取挂连接的一端（点空闲端也绝不落入"旋转即放弃跟随"路径）。

## 关键约束

- **单位体系**：内部计算使用厘米（cm），界面显示使用毫米（mm）。
- **Block 刚体模型**：Block 是刚体变换单元，内部点相对位置固定，整体支持平移/旋转。
- **画布缩放**：ZOOM_MIN=0.2（20%）/ ZOOM_MAX=10.0（1000%）/ SCENE_BOUND=±10,000mm（浮点精度安全）——常量定义于 src/canvas/CanvasView.h:74-77。
- **Git 远程**：origin = https://github.com/zll7hj111-cmyk/garment-cad.git，主分支 main；本地身份 林林 <2274789227@qq.com>。

## 架构决策记录（2026-08 全量评审档案）

> 本节是 2026-08 那次全量架构评审的决策档案。原文 `ARCHITECTURE_REVIEW.md` 在完成使命后
> 已删除（2026-08-29），**结论迁移至此**——下面这些是「为什么不做」的不可再生知识，删掉后
> 未来会有人重新提议、重新评估、重复付出同样的工作量。
> 源码注释里出现的 `P0-x / P1-x / P2-x` 编号与 `(ARCHITECTURE_REVIEW)` 字样均指本节。

### 一、评估后主动决定「不做」的三项（别再重新提议）

- **① worker 线程求解（原 P2-6 风险项）—— 已量化，不需要**
  实测数据（`tests/test_resolve_scale.cpp`，60fps 预算 16.67ms，每档中位数）：

  | 块数 | 拖拽帧 | 全量 resolve | 拖拽占预算 |
  |------|--------|--------------|-----------|
  | 20   | 105 µs | 167 µs       | 0.6%      |
  | 100  | 486 µs | 1091 µs      | 2.9%      |
  | 400  | 2035 µs| 9124 µs      | 12%       |
  | 800  | 4039 µs| 31495 µs     | 24%       |

  结论：①**交互路径（拖拽）近似线性**（实测指数 ≈1.0），真实衣片远小于 100 块，800 块时仍
  只占预算 24% —— 拖拽永远不会需要 worker；②**全量 resolve 才是超线性的那条**（指数
  ≈1.5~1.8，800 块 31ms ≈ 掉 2 帧），但它只由离散动作（改变量）触发，不是每帧；③真要优化，
  目标是 `r.settle`（占 `r.total` 50~60%）与 `resolve.aux`（大档位主导），**不是换线程**——
  模型是一个可变 QObject 图，上线程要么整体加锁要么整份快照，代价远大于省下的 30ms。
  **注意**：该 bench 只做正确性断言、绝不断言耗时（同一份代码空闲 vs 机器忙实测差 63%）。

- **② 指针句柄化（原 P1-3 中期项）—— 审计后结案为不做**
  全仓 361+619 处 `findBlock`/`blockById` 审计结果：失效面仅 4 个 bump 点（附件/求解/图层/
  变量操作均不失效）、成员持有 0、违规 0（唯一跨变更位点 `RotateCopyGesture::convert` 已
  手工重查+注释）、range-for/lambda/延迟变异均 0。唯一推理密集位点 =
  `SegmentConnectionCardConn:240`（依赖"命令 redo 不结构变异"的隐式不变量）。
  结论：纪律已被普遍遵守，迁移 500+ 调用点的成本 >> 收益（0 现症 bug）。
  **generation 设计被修正**：本仓 id = QUuid 全局唯一且永不回收，无 ABA 问题，句柄 = id 本身
  + 哈希校验即可，不需要 generation（那只在"句柄=整数下标"或"id 可回收"时才有意义）。
  **若未来出现指针类 bug**：B 类视图的 `byId()` 已预留承接点，可低成本重启窄版句柄。

- **③ undo 增量快照 —— 不做**
  命令带全量模型快照，但 `kUndoStackLimit = 150` 已封顶，长会话内存有界。真正的治本方案
  （增量快照）成本远高于收益，150 步上限对服装打版场景够用。

### 二、两条可复用的判据

- **「历史」vs「模型不变量」**（来自 P2-1 版本迁移框架）
  迁移历史时不要把模型不变量一起搬走。判据：**这条规则对「今天刚写出来的 vN 文件」还
  成立吗？** 成立 = 不变量/损坏兜底（留在 `DocumentSerializer`）；不成立 = 历史（进
  `FormatMigration` 的具名步骤）。
  实例：「文件缺辅助层就补一个」曾误当历史搬走，导致 `test_serializer::
  degradedValuesProduceWarnings` 立刻红 —— 它是 `Layer.h` 声明的模型约束（每文档恰好一个
  辅助层、index 0、不可删除），必须留在序列化器；真正属于历史的只有「v0 整数下标 +1 位移」。

- **B 类视图「零删除」的原因**
  视图委托门面公共方法实现 → 门面方法永远是视图的实现基底，**不存在可删的"零调用者旧
  方法"**。不要为了"收敛 API"强删门面方法。已知审计盲区：类内部无点号调用（如
  `componentClosure` 在 `ParamDocumentResolver:194`）会被统计正则漏掉，迁移前必须 grep
  实际调用形态。

### 三、唯一剩余项：Rust 重构试点

前置条件（P0 + P1-3 短期护栏）早已满足。**候选按性价比排序**：

| 模块 | 规模 | 适配度 | 理由 |
|------|------|--------|------|
| **geometry**（CurveMath/TriangleUnfold/Vec2/Units/Angle） | ~1.8k 行 | ★★★★★ | 纯数学、值语义、零 Qt 依赖；有 `test_curve` 全量回归兜底，最安全 |
| ExpressionEvaluator | ~795 行 | ★★★★★ | 边界是 `QString in → double out`；**但其静态缓存隐患已由 P1-5 在 C++ 内解决**，Rust 化收益降为纯语言偏好 |
| 序列化内核 | ~960 行 | ★★★☆ | serde 表达力强，但需先解决模型类型跨语言重复定义的成本 |
| 求解数值内核 | 需抽取 | ★★★ | 与 Block/Resolver 状态纠缠深，**先做"纯函数抽取"再谈换语言** |

**不适合**：ui / tools / canvas / app（~60% 代码量，Qt 信号槽深绑定）、ParamDocument 门面
（QObject 信号是架构的血液循环）、命令系统（QUndoStack 与 Qt 生态绑定，问题在架构不在语言）。

**若决定做，推荐路径**：①从 geometry 开刀（零 Qt 依赖、测试兜底最全），用
[corrosion](https://github.com/corrosion-rs/corrosion) 接入现有 CMake、cxx 做类型安全桥接；
②ExpressionEvaluator 第二步；③**止损线：若试点模块的 FFI 胶水代码超过模块本体 30%，说明
边界不成熟，停下来继续用 C++。**
