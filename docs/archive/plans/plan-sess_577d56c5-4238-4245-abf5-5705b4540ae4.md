# 删除「影子偏转」功能（shadowAnchorRotDeg + noFollowRotate + 冻结机制）实施计划

## 用户拍板的两个边界决策

1. **拆开（angleOnly）语义**：彻底删除 `angleRefFrozen` 冻结机制（恢复 2026-08 首版语义——拆开后 L1 角度继续由活的基准线 L2 驱动）。
2. **磁盘格式**：保持 `kFormatVersion=2`，`migrateV1ToV2` 改为纯删键占位（丢弃 `baselineOffsetDeg`），删 v1→v2 换算断言用例。

## 一、引擎层（parametric）

- **Attachment.h:179-202**：删 `shadowAnchorRotDeg`、`noFollowRotate` 两字段及其注释。
- **Attachment.h:204-219**：删 `angleRefFrozen`/`angleRefFrozenDeg`/`angleRefFrozenHostId` 三字段及其注释（含 209-213 引用影子机制的注释行）。
- **Resolver.cpp:751-753 + 791-809**：删冻结快照分支（753-755 的 `refWorld = att.angleRefFrozenDeg`）与影子叠加块（791-809 含 shadowActive 计算）；`refWorld` 恢复为纯 `leaderRefWorld` + 活基准现算，两处 `angleRef` 条件从 `else if (!att.angleRefBlockId.isNull() && angleRef)` 简化。
- **ParamDocumentAttachments.cpp**：
  - 105-107 / 155-156：删两处 `att.shadowAnchorRotDeg = toBlock->transform.rotation` 钉锚。
  - 656-659 / 695-706：`effectiveAngleRefWorld` 删冻结分支与影子叠加段。
  - 744-768：删 `repinShadowAnchorOnReattach` 函数（含上方块注释）。
- **FollowerAngle.h**：删 `repinShadowAnchorOnReattach` 声明（73-74）与第③条影子叠加文档注释（44-45）；`effectiveAngleRefWorld` 其余说明同步简化。

## 二、序列化与迁移层（document）

- **DocumentSerializer.cpp**：
  - 449-450：删 `shadowAnchorRotDeg`/`noFollowRotate` 两个写键。
  - 452-453 + 509-511：删 `angleRefFrozenDeg`/`angleRefFrozenHostId` 读写（冻结字段——顺带清出 v15 Optional 字段）。
  - 498-505：删影子锚点/不跟随旋转读段（旧档未知键被天然忽略，行为零变化）。
- **FormatMigration.h**：`kFormatVersion` 保持 2；删 `migrateV1ToV2` 声明的换算注释（80-86），改为"仅清理 v11 baselineOffsetDeg 残留键"占位语义。
- **FormatMigration.cpp**：`migrateV1ToV2`（186-229）整体重写为：`attachments[i]` 逐项 `a.remove("baselineOffsetDeg")`，不再写锚点、不再需要 blockRot 表，删除换算警告文案（224-226 行 "影子偏转账本迁移为锚点"）。

## 三、命令层（document/commands）

- **AttachmentCommands.h**：
  - 105 / 154 / 388：删三处 `m_oldShadowAnchor` 快照成员（AngleIndependent / AngleRef / NoFollowRotate 命令）。
  - 350-365：删 `SetAttachmentShadowAnchorCommand` 类。
  - 367-389：删 `SetAttachmentNoFollowRotateCommand` 类。
  - AngleOnly 命令的 `m_oldFrozen`/`m_oldFrozenDeg`/`m_oldFrozenHostId` 快照成员（109-111 附近）一并删。
- **AttachmentCommands.cpp**：
  - 184 / 204-215 / 248：AngleIndependent 命令删锚点快照、退出独立角的 shadowWillActivate 重钉块、undo 锚点恢复。
  - 288 / 302-315 / 365：AngleRef 命令删锚点快照、激活态过渡重钉块、undo 锚点恢复；327-328 行冻结字段清理行删（306 注释同步）。
  - AngleOnly 命令 124-141：删"冻结方向快照"分支（redo(true) 不再 set angleRefFrozen）；undo 相应恢复段（153-155 附近）删。
  - 415-416：`ReattachAttachmentCommand::redo` 删 `repinShadowAnchorOnReattach` 调用。
  - 432-433 / 216-217 / 330-331：反算注释提及影子的措辞同步简化为"两点基准"。
  - 735-823：删 `SetAttachmentShadowAnchorCommand` 与 `SetAttachmentNoFollowRotateCommand` 两个命令类的实现（含 mergeWith）。
- **CommandIds.h:28-29**：删 `SetNoFollowRotate = 1005` 枚举（保留 1001-1004 号值不变）。
- **BreakCommands.cpp:1189-1208**：删"重钉影子锚点"块（`wasActive`/`visibleDeg`/锚点写入），重指分支只保留 toBlockId/toPointId/toSegmentId 改写。

## 四、工具层（tools）

- **ToolRotate.cpp**：删影子基准虚线全组——484-489 块注释、`buildShadowRefLines`(491-534)、`updateShadowRefLines`(536-561)、`clearShadowRefLines`(563-569)、`shadowRefLineDirRad`(571-580)；调用点 629-634(selectTarget)、641、652(clearTarget)、690-693(beginRotation)、757-762(updateRotation) 与 onDeactivate 清理处全部移除。
- **ToolRotate.h**：删 89-92（`shadowRefLineCount`/`shadowRefLineDirRad` 测试契约）、147-158（三函数声明+设计注释）、270-274（`m_shadowRefLines` 成员）。**保留 287 行 ManagedItems "影子置空"（不同含义，勿动）**。
- **ConnectGesture.cpp**：
  - 620-621：`reattachAngleOnly` 删 `repinShadowAnchorOnReattach` 调用。
  - 785-801：`switchComponentTarget` 删重钉影子锚点块（oldHost/newHost/wasActive/visibleDeg 计算与 792-795 冻结判定）。
  - 615-619：删"冻结置位时跳过 preserveAngleRefOnReattach"的条件分支（恢复无条件 preserve）。

## 五、UI 层

- **LinePropertyDialog.cpp**：
  - 322-325：删 `m_editShadow`/`m_btnNoFollowRotate` 信号连接（仅保留滑轨三连）。
  - 856-893：删"影子偏转行"构建（m_shadowRow/m_editShadow/m_btnNoFollowRotate 含 noFollowRotateBtn objectName）。
  - 1644-1691：删 `onShadowEdited()` 槽（含 refIsHost 判定、curVisible、命令 push）。
  - 1693-1716：删 `onNoFollowRotateClicked()` 槽。
  - 1749：`onStartConnectResolved` 删 `repinShadowAnchorOnReattach` 调用；1746 `!mut->angleRefFrozen` 条件恢复为无条件 preserve。
  - 1868-1966：`refreshSlideShadow()` 改名为 `refreshSlideRow()`（或保留名但删影子段 1922-1965 与第 1877-1882 省道分支的 m_shadowRow 引用）；8 处调用点（1180/1577/1582/1586/1595/1641/2492/2504）同步。
  - 849-851 布局注释删"③影子偏转"。
- **LinePropertyDialog.h**：删 112-114 槽声明、109 注释、120-121 改名、249-257 控件声明（m_shadowRow/m_editShadow/m_btnNoFollowRotate）；include 区 SetAttachmentShadowAnchorCommand 注释清理。
- **SegmentConnectionCardConn.cpp:80-81**：删 `repinShadowAnchorOnReattach` 调用。
- **SegmentAngleCard.cpp:440-443**：`updateWorldAngleLabel` 的 effectiveAngleRefWorld 调用保留（函数不删，影子叠加已内部移除）。
- **SegmentConnectionCardRefresh.cpp:25 / SegmentConnectionCardBuild.cpp:120 / SegmentConnectionCard.h:36**：删"滑轨/影子"措辞里影子的字眼（滑轨保留）。

## 六、测试层

- **test_commands.cpp**：
  - 删 `angleOnlyFreezeShadowFollowsNewHost`（103 声明 + 1462-1556 实现）、`angleOnlyFreezeNoJumpOnDetach`（106 声明 + 1563-1594 实现，纯冻结快照回归）。
  - 1383 附近"拆开=复制隐形 L2.1"注释段修正为旧语义；1598/1622/1625/1642 的 angleRefFrozen 断言删行。
- **test_rotate_copy.cpp**：删 4 例（235-243 声明 + 实现 2810-2945 rotateHostShadowFollowsFollower / 2947-3042 noFollowRotateKeepsFollowerPulledBack / 3045-3135 shadowInactiveWhenRefIsHost / 3140-3221 shadowFollowsIndirectHostRotation），含 `shadowRefLineCount`/`shadowRefLineDirRad` API 断言；3127-3220 普通跟随断言段如残留一并清理。
- **test_select_wkey.cpp**：删 `shadowBaselineOffsetDrivesRotation`（82-83 声明 + 2345-2450 实现，含 SetAttachmentShadowAnchorCommand 直接 new 的 undo/redo 段）。
- **test_dialog_tabs.cpp**：删 `noFollowRotateButtonToggles`（63 声明 + 1339-1437 实现）；reattachPreservesAngleRef（1054-1143）中 angleRefFrozen 断言/注释清理（用例主体保留）。
- **test_serializer.cpp**：`attachmentsRoundTrip`（707-774）删 4 行——732 设值 + 766-767 断言、740 设值 + 773 断言；其余字段往返保留。
- **test_migration.cpp**：删 `v1ShadowOffsetMigratesToAnchor`（390-478 实现 + 文件声明显式列出处若有）；新增/改写一个轻量断言：v1 档迁移后 `baselineOffsetDeg` 键消失且无 shadowAnchorRotDeg 键（保住"迁移链完整性"用例对 v1 的覆盖）；文件其余 10 例保留。
- **test_migration.cpp 声明区**：确认 11 例函数声明清单随删除同步。

## 七、文档同步（AGENTS 收尾防过时铁律）

- **DECISIONS.md:33**：整条目改写为"影子偏转·锚点推导已删除（2026-09 用户拍板删除）"——删除记录（字段/命令/虚线/UI/迁移换算），冻结机制条目并入：同条目内删除冻结三段与测试登记，声明拆开语义回退 2026-08 首版（L2 旋转带动 L1）。
- **DECISIONS.md:19**：末尾"2026-xx 影子偏转 bug 修复（拆开 = 复制隐形 L2.1 线段）"段落整段删除（冻结机制随删），恢复首版语义描述。
- **TROUBLESHOOTING.md:181**：整段改写为"影子偏转 + 冻结机制已删除（拆开恢复跟随活基准）"；**:314** 末尾迁移回归计数改（"12 例"→"11 例"+ 影子用例已删）；:278 提及影子偏转的措辞顺带清理。
- **CONVENTIONS.md:58**："test_migration（12 例）"改为当前实际例数（删 v1ShadowOffsetMigratesToAnchor 后 10 例，若新增占位断言则 11 例——以最终文件为准）。
- **AGENTS.md**：无"影子"字样，无需改。

## 八、验证

1. `tools\build.bat` 全量构建（Ninja Debug，PDB 治理标志勿动）。
2. 全量 `ctest`（按 AGENTS 规则收尾跨模块大改走全量）——回归基线必须仍为 0。
3. 重点单跑：test_migration、test_serializer、test_commands、test_rotate_copy、test_select_wkey、test_dialog_tabs。
4. 收尾核对：源码 grep 确认 `shadowAnchorRotDeg` / `noFollowRotate` / `repinShadowAnchorOnReattach` / `SetAttachmentShadowAnchorCommand` / `SetAttachmentNoFollowRotateCommand` / `angleRefFrozen` 在 src+tests 全部清零（除文档档案中的历史记录文本）。