# TROUBLESHOOTING —— 踩坑经验库

按需查阅（不注入会话）：修改 bug 前先 grep 本文件相关关键词；修复了本清单之外的 bug 后，把根因 + 修复追加到对应主题分组。

## 0. 快捷键登记表（改快捷键前必查，改完必更新）

### 菜单/QAction 层（src/app/MainWindow.cpp，主窗口级，画布聚焦时也拦截）
| 键 | 功能 | 位置 |
|----|------|------|
| Ctrl+N/O/S / Ctrl+Shift+S / Alt+F4 | 新建/打开/保存/另存/退出 | 160-184 |
| Ctrl+Z / Ctrl+Y | 撤销/重做 | 190/194 |
| V | 选择工具 | 205 |
| L | 智能笔工具 | 212 |
| C | 曲线编辑工具 | 219 |
| R | 旋转工具 | 226 |
| B | 打断工具 | 233 |
| I | 交点工具 | 240 |
| A | 角度测量工具 | 254 |
| H | 切换辅助层 | 263 |
| Ctrl+D | 切换主题 | 276 |
| ~~M~~ | ~~测量工具~~ —— **已移除（2026-08），M 让给画布长按显示长度** | 原 247 |

### 画布长按（src/canvas/CanvasView.cpp:228-263，在工具分发前拦截）
| 键 | 功能 |
|----|------|
| N | 按住显示所有名称（线段名/点名称/曲线名），松开快照恢复 |
| M | 按住显示所有长度（线段/曲线弧长），松开快照恢复 |

### 工具内部按键
| 键 | 功能 | 位置 |
|----|------|------|
| W | **模式切换 leader key**（Tab 是焦点导航键，禁用）。四个工具各有模式：选择=单选↔多选；测量=距离→水平→垂直；智能笔=直线↔省道线（**仅 Idle**）；交点=跟随角↔绝对角（**仅瞄准态**）。**W 的语义随工具状态变化**——选择工具在重叠候选激活时、智能笔在 Drawing 态时，W = 循环候选而**不是**切模式 | 见下方「W 键模式指示」 |
| D | 选择工具：**快拆**（拆开保留角度——解除选中线连接的位置吸附、角度仍跟随基准线，经 undo 栈；2026-09 用户拍板） | ToolSelect.cpp quickDetachSelection |
| X | 旋转工具：锚心切换（起点 ↔ 终点） | ToolRotate.cpp keyPress |
| ~~W~~ | 旋转工具：组件整组旋转模式切换（2026-12 拍板）——**已删除**（2026-08-29 用户拍板执行 ROTATE_REDESIGN_DESIGN.md D1：模态被多选选区继承覆盖）；旋转工具现无 W 键绑定 | （原 ToolRotate.cpp keyPress） |
| Alt(拖拽时) | 曲线编辑工具：拖切线手柄断开锁定（尖角模式，持久；回锁走属性面板复选框；P3-1 2026-12 落地） | ToolCurveEdit.cpp beginHandleDrag |
| Escape / Delete / Backspace | 取消/删除 | 各工具 |
| Shift | 修饰键（多选/正交等） | 各工具 |

> 注意：QAction 快捷键（WindowShortcut）在 ShortcutOverride 阶段先于 widget keyPressEvent 生效——**画布内工具按键如果与 QAction 快捷键同名，QAction 先抢**（本次 L 冲突教训：MainWindow.cpp:212 智能笔 L 曾吞掉画布长按 L）。新增画布按键前先 grep `setShortcut` 确认 QAction 层无同名键；新增 QAction 快捷键前先查本表。

### W 键模式指示（2026-08-29 落地：显示标识统一走 ModeIndicator）

**背景**：W 切完模式后画布上没有任何常驻标识，旧实现只有一句 1.4 秒就消失的 toast —— 用户按完就再也看不出自己在哪个模式（选择工具尤其致命：多选下点空白是加选、单选下是清选，模式错了直接就是误操作）。

**三层显示，各管一段、不可互相替代**：
| 层 | 载体 | 存活 | 回答什么 |
|----|------|------|---------|
| L1 | 状态栏 `工具[模式]：…` | 工具激活期内常驻 | 现在是什么 + 此刻按 W 会发生什么 |
| L2 | 画布角标（P2，未落地） | 常驻，仅非默认模式 | 下一次点击的语义 |
| L3 | toast | 1.4 秒 | 刚刚变成什么了 |

持久层答不了"三态循环跳到第几个"，瞬时层答不了"现在是哪个模式"——所以都要。

**改模式文案/加模式时必守三条**：
1. **单一出口**：工具只实现 `modeIndicator() const`（或静态 `modeIndicatorFor(...)`），
   切换时调 `Tool::announceModeChange()`（发 toast + 刷状态栏），状态变了但模式没变时调
   `Tool::refreshModeIndicator()`。禁止各自手拼 toast + `reportHintOverride` 两条路。
2. **静态与运行期同源**：`describe().hintText` 必须调同一个 `modeIndicatorFor(默认态).hint(u8"工具名")`——
   `test_mode_indicator` 的 `staticHintMatchesRuntimeHintOnActivate` 逐字比对两者，一红就是漂移。
   注意 `test_tool_hints` 另锁"提示以自己工具名开头"，`hint()` 的 `工具名[模式]：` 格式天然满足。
3. **W 语义随状态变化**：`modeIndicatorFor` 必须由 **{mode, state}** 共同决定。选择工具重叠候选
   激活、智能笔 Drawing 态时 W 是循环候选，此时写「W 切多选」就是**错误指引**。为让这条结构性
   成立，`ToolIntersection` / `ToolSmartPen` 都把状态迁移收进了 `setState()`（`m_state` 已无直接
   赋值点），刷新挂在迁移上而不是靠 10 个赋值点各记一次。

**L2 画布角标（同日落地）**：`CanvasScene::setModeBadge(text)` 拥有常驻 `HudItem`（DarkPill，
zValue 9999 低于 toast），形态 = **视口左上角深色小胶囊**，只在非默认模式出现。三条实现约束：
①**必须显式重定位** —— 它是场景图元却要"钉在视口左上角"，视口一滚动/缩放，同一屏幕位置对应的
场景坐标就变了；`connectModeBadgeViewSignals()` 监听 scrollbar `valueChanged` +
`CanvasView::zoomFactorChanged`（缩放时滚动条 value 不一定变，两条都要）。②**不要**用 QWidget
覆盖层省这件事：`CanvasView` 的 viewport 是 `QOpenGLWidget`（CanvasView.cpp:50），上面叠普通
QWidget 有合成刷新风险。③**切工具必须撤下**：角标归场景而非工具所有，`Tool::deactivate()` 显式
`setModeBadge(QString())`，否则上一个工具的「多选」会一直挂着（m_scene 随后即空）。
回归：`test_mode_indicator::canvasBadgeShowsOnlyForNonDefaultMode` /
`canvasBadgeFollowsViewportScroll`。

## 1. 构建与工具链

- **clangd 索引陈旧（历史）**：曾需手动刷新 Ninja 专用目录 build/clangd，否则报大量"不可能的错误"（Expected unqualified-id、构造函数不匹配、No member named xxx、QWidget is not a direct base...）——构建其实通过，属索引噪音，勿误判回归。**已切换 Ninja 生成器（2026-08），compile_commands.json 由构建自动导出到 build/out，clangd 直接热重载，此坑机制性消除**；若仍出现陈旧现象，重启 clangd/opencode 会话即可。
- **生成器切换迁移坑（VS→Ninja 2026-08）**：复用旧 build/out 换生成器会报 `Error: generator : Ninja Does not match the generator used previously: Visual Studio 17 2022`（来自 _deps/*-subbuild 的旧缓存）。处理：删除 `build/out/_deps/*-subbuild` 下的 `CMakeCache.txt`+`CMakeFiles`（连同顶层 CMakeCache.txt/CMakeFiles/*.sln/*.vcxproj/x64），**保留 `_deps/*-src` 克隆与 `*-subbuild` 的 stamp**，即可离线完成迁移（github.com:443 不可达）。
- **PowerShell 5.1 嵌套引号**：`cmd /c ""...&&..."` 报 `'C:\Program' 不是内部或外部命令`——改走临时 .bat（先 call vcvars64.bat 再 cmake）。
- **LNK2005 stale object**：头文件大改后增量构建报 already defined → touch 源文件强制重编译。
- **陈旧 AUTOMOC**：MSVC 出现"不可能的错误"（单文件 cl 编译过、磁盘内容正确但构建报 C2653/C2027）→ 删该目标 `.dir` 与 `*_autogen` 目录重建。预防：IDE 打开状态下不要用脚本交替改写同一文件。
- **新增 .cpp 漏加库源列表**：CMake 无自动扫描，LNK2019 且找不到原因时检查该文件是否已加入 `CMakeLists.txt` 对应模块库（gcad_geometry/gcad_parametric/gcad_canvas/gcad_document/gcad_ui/gcad_tools/gcad_app）的源列表——主程序与测试均通过链接库获得，**不再有逐 target 源列表**（2026-08 重构后）。
- **行尾损坏 `\r\r\n` / `\r\r\r\n`（2026-08-29 P1 迁移实测）**：部分源文件（ToolBreak.cpp、ToolRotate.cpp 等）混入双重/三重 CR 的行尾，是构建日志里 `warning C4335 检测到 Mac 文件格式` 的根因，且**静默破坏任何按 `\n` 或 `\r\n` 假设做多行文本替换的脚本**（pattern 永不命中或半命中）。修复/预防：脚本读入后先 `re.sub(r'\r+\n', '\r\n', d)` 归一化再替换，或按 `\r?\r?\n` 弹性匹配行间；跨行 pattern 用正则而非字面量 replace。
- **GUI 测试 exe 段错误零输出（2026-08-29 P1 实测，formula_groups 案例）**：改了大量工具头文件后，ctest 里 test_formula_groups 无输出段错误（单跑复现、任意 slot 均崩、`-functions` 都不出文本）——**不是代码回归**，是该 exe 的陈旧 AUTOMOC/stale object 在重链后混链接所致（同本组"陈旧 AUTOMOC""LNK2005 stale object"两条）。修复：删该 target 的 `CMakeFiles/<t>.dir` + `<t>_autogen` + exe，走 build.bat 干净重建即愈。判据：崩溃测试不触碰本次改动代码面 + 全量干净重建消失 + 其余 28 例全绿。**注意**：删 target 目录后直接 `ninja` 会因缺 MSVC 环境（INCLUDE/LIB）报 `C1083 type_traits`——必须走 build.bat。
  - **2026-08-31 补充（本次踩到，务必看）**：①**不只 GUI 测试会中招** —— 本次改 `parametric/Segment.h`（被广泛包含）后，test_duplicate / test_perf / test_resolve_scale / test_p612_colinearity 四个**纯逻辑**测试一并零输出段错误（各 ~2.8s，极整齐），它们只链接 `gcad_parametric gcad_tools`，根本不碰 UI。②**只删 `CMakeFiles/<t>.dir` 无效** —— 实测先删 7 个库 `.dir` + 35 个 test `.dir` 全量重编后**仍崩**；**必须同时删 `<t>_autogen.dir` 和 exe**（三件套缺一不可），删干净后重建即全愈（2 红基线）。③判回归的**廉价归因法**：把模型层改动逐块回退（本次回退了 `Segment::annotation` 字段 + 序列化 + 两条命令的 Props），构建成功后测试若**仍是同一组红**，即可排除自身改动；再叠加"崩溃测试链接的库里没有本次改动的库"这条（test_duplicate 与通过的 test_resolver 链接库完全相同），即可定性为 stale object，不必再逐行查代码。④**构建失败时 ctest 跑的是旧 exe** —— 本次回退过程中漏删一行导致 `build.bat` EXIT=1，但 ctest 照跑并给出"仍然 6 红"的假结论，差点据此误判；**每次 ctest 前先确认上一次 build 的 EXIT=0**。
- **变量面板性能**：折叠组点击延迟 = O(N²) 布局冻结 + 双击判定 timer，已根治；测量页提取踩过 AUTOMOC 陷阱。

## 2. ElaWidgetTools（第三方库，PATCH_COMMAND 维护）

- **库被 patch 过**：`third_party/elawidgettools_qt69_patch.cmake` 共 5 个 Part（均幂等）——改库行为一律走该脚本，不要直接改 build 产物或绕开 ElaMsgBox.h 里的依赖假设。
  - Part 1：QChar 单 token 包装 char16_t（Qt 6.9+ 编译兼容）。
  - Part 2：ElaContentDialog 三按钮信号改同步 emit（修 use-after-free）；`setMiddleButtonText` 空文本自动隐藏按钮（消除默认英文 "minimum"）。
  - Part 3：ElaMenu 移除 400ms 滑入动画。
  - Part 4：ElaAppBar 默认关闭路径尊重 close() 被拒（修「点取消弹两次」）。
  - Part 5：SpinBox 支持 QAbstractSpinBox::NoButtons（ElaSpinBoxStyle 检查 buttonSymbols，NoButtons 时不绘制 ⬆️⬇️ 图标与按钮底色，编辑域占满全部宽度；ElaDoubleSpinBox/ElaSpinBox 构造函数默认启用 NoButtons）。
- **弹窗「是/取消」闪退**（已修复，2026-08）：根因 = ElaContentDialog 按钮回调先同步 accept/reject（exec 返回 + 调用方 delete dlg）再 `QTimer::singleShot(0)` 延迟 emit → 信号落在已释放 this 上（UAF）。中间按钮安全（无关闭调用）。修复 = 同步 emit。`src/ui/ElaMsgBox.h` 依赖此语义。
- **关闭窗口点「取消」弹窗弹两次**（已修复，2026-08）：根因 = ElaAppBar 默认关闭路径（`ElaAppBarPrivate::onCloseButtonClicked`，`_pIsDefaultClosed=true`）无条件执行 `window->close(); processEvents(); windowHandle->close();`——点「取消」时 MainWindow::closeEvent ignore 了事件、`close()` 返回 false，但代码不检查返回值，继续对原生句柄 `windowHandle->close()` → QWidgetWindow 把第二次 QCloseEvent 转发回 widget → closeEvent 重入 → 第二个「是否保存」弹窗。修复 = Part 4：`window->close()` 返回 false 直接 return（已接受关闭时 widget 已隐藏，后续 `windowHandle->close()` 本就是 no-op）。改库行为 + 改 `build/out/_deps/elawidgettools-src/.../ElaAppBarPrivate.cpp` 两处同步；仅 ElaAppBar 关闭按钮（X）路径受影响，菜单退出/Alt+F4 走单次 close() 无此问题。
- **ElaMsgBox 文字贴边框**（已修复，2026-08）：根因 = ElaContentDialog 主布局 `setContentsMargins(0,0,0,0)`，默认中央部件自带 (15,25,15,10) 边距，但 `setCentralWidget` 换成自定义 body（ElaText 自身无内边距）后边距全丢 → 文字紧贴弹窗边框。修复 = `src/ui/ElaMsgBox.h makeDialog` 里包一层带 (15,25,15,10) 边距的 QWidget+QVBoxLayout 再 setCentralWidget。坑：ElaMsgBox.h 需显式 `#include <QVBoxLayout>`（ElaContentDialog.h 不传递提供）；改该头文件后必须补 UTF-8 BOM（Edit 工具会丢 BOM，无 BOM 中文注释可致 MSVC C2061/C3536 误报）。
- **菜单"悬停看不见文字"**（已修复，2026-08）：根因 = ElaMenu showEvent 抓静态图 + 400ms 滑入动画，动画期间 paintEvent 画滑动的抓图（实测 20ms 帧内容 0 像素、100ms 才 55%、250ms 后完整），且 hover 高亮被动画拦截。修复 = Part 3 移除动画。实证方法：临时探针程序（grab 各时间点帧统计非背景像素数）见 `C:\Users\Administrator\AppData\Local\Temp\opencode\menu_probe\`。
- **重建流程**（github.com:443 不可达，无代理）：删 `build/out/_deps/elawidgettools-{build,subbuild}` → configure 加 `-DFETCHCONTENT_SOURCE_DIR_ELAWIDGETOOLS=E:\garment-cad\build\out\_deps\elawidgettools-src`（SOURCE_DIR 模式下 PATCH_COMMAND 仍执行，幂等可重复跑）。
- **静态库 + dllimport**：库以 `add_definitions(-DELAWIDGETTOOLS_LIBRARY)` 目录级编译（dllexport），静态库中 dllexport 不生成 `__imp_` 别名；主程序以 dllimport 语义链接产生 LNK4217 警告属正常；**probe 程序 LNK2019 缺 __imp_ 符号时，编译加 `-DELAWIDGETTOOLS_LIBRARY` 即可**。
- **patch 脚本自测假阴性**：patchtest 目录里的 patch.cmake 副本若从 third_party 复制后源文件再被改，测的是旧脚本——重测前必须重新复制；`cmake -P` 的 file(GLOB) 相对当前工作目录，workdir 必须设对，否则 glob 匹配不到、静默不替换。

## 3. Qt / UI 渲染

- **2026-08-28 清理收口（CLEANUP_REFACTOR_DESIGN.md，重构非 bug；最终 ctest 27 用例 = 2 既有基线红 + 2 组 3.gcad 环境漂移红 + GUI 时序抖动偶发，未新增红）**：
  - **A1 角度归一化**：全库手写 `fmod(x,360); if(<0)+=360` / `while(>180)-=180` 副本清零（SegmentEditBar 私有 normalizeDeg 删除、SegmentAnchorTab 相对切线角、SegmentAuxTab/LineFactory/RotateCopyGesture/BreakCommands/ToolRotate/ToolSmartPen 显示角）。**-180° 语义分叉收敛**：SegmentAnchorTab 原 `while (< -180)` 边界改为 Angle.h 公共版 `<= -180`（输入恰 -180° 时公共版映射 +180；该路径无 -180° 测试覆盖，纯显示符号约定，无功能影响）。
  - **A2 公式求值收口**：`ConditionEngine::evaluateLengthMm(formulaCm, baseValues, condByName, outMm, ctx)`——空串→false、求值 ok 才写 outMm（mm），替掉 20+ 处 `if(!f.isEmpty()){evaluate;if(ok)x=cmToMm}`（Block/Resolver/ToolRotate/ConnectGesture/SegmentAngleCard/SegmentAuxTab/BreakCommands/ToolSmartPen）。**ConvertGesture:1066 / ToolRotate:1795 双模分支保留原 evaluate**（同一 r.value 按 rotationMode 分流角度/长度，语义不同不强改）。
  - **A3 弧长↔角度**：`cad::geo::arcMmToDeg/degToArcMm`（Angle.h）+ `cad::param::followerModeSwitchValues(att, radiusMm, targetMode, params, cond)`（FollowerAngle.h，SegmentAngleCard::onModeToggle 与 ConnectGesture::onAngleModeChanged 共用）。**坑：弧长回写必须保持 `std::fmod(curDeg,360)` 语义**（公式值可为原始多圈/负折角，normalizeDeg360 会塌掉）——A3 重构时若顺手换成 normalizeDeg360 会静默改几何，followerModeSwitchValues 注释已点明。
  - **A4 currentZoom**：`CanvasScene::currentZoom()`（views 空返 1.0），43 处 `views().first()->transform().m11()` 三行惯用法一行替换（含 BlockItem/CurveItem 的 qobject_cast 局部化、ToolRotate 私有 currentZoom 委托）。
  - **A5 setCommentSilently**：`CardBase::setCommentSilently(text)`（QSignalBlocker + hasFocus 守卫统一五卡），五卡各删 3 行 blockSignals。
  - **A6 parseNumberOrFormula**：`cad::geo::parseNumberOrFormula(text)`（Units.h，`{isNumber, value, formula}`）收口「toDouble 成功走数值/失败走公式」两段式；**各调用点差异化处理（空串/负数/公式存储）保留，只收共同主干**。
  - **B1 QSignalBlocker**：SegmentAnchorTab 6 控件手工 12 次 blockSignals（跨 90 行、只存第一个控件 wasBlocked）改 6 个 RAII 栈对象——**顺手修掉隐性 bug：旧代码统一恢复为第一个控件的状态，现各自恢复构造时状态**；ConnectGesture/ToolRotate/MeasureResultDialog/CopyChip 同步改。
  - **B2/B3/B5**：滑轨 cm→mm 用 `Units::cmToMm`；「数值(°)或公式」/「数值(cm)或公式」提 `FormScaffold.h` 常量（kPlaceholderAngleOrFormula/kPlaceholderCmOrFormula）；FormulaCard 实际值回显 `Units::formatNumberTrimmed`（去尾零，2026-12 拍板后漏改处）。
  - **B4 序列化表驱动**：DocumentSerializer 四组枚举映射改 `constexpr std::array<std::pair<Enum,const char*>>` + enumToStr/enumFromStr 双向查找（首行 = canonical default），str→enum 与 enum→str 同表必然同步；**test_serializer 全过（除既有基线红 bridgeAuxPointSnappableAndAttachable）**。
  - 验证：tools\build.bat 通过；受影响 ctest 子集（serializer/segment_edit_bar/rotate_copy/commands/break/dialog_tabs/hold_show/smartpen_aux）单测复跑全绿；全量 ctest 27 用例仅剩文档化基线 + 3.gcad 环境漂移 + GUI 时序抖动（curve_edit::plainDragKeepsLock 批压下偶发、单测 6/6 过）。

- **MSVC `/permissive-` 下 `auto* f = [&]{ return new T(...); }` 编译失败 C3535（2026-08-31）**：lambda 返回指针时 `auto*` 推导不合法——报「无法推导 auto* 的类型（依据 lambda）」，错误行指向 lambda 声明处、与其它报错行隔离几十行，极易误判为「代码被改坏」。修复 = 显式返回类型：`auto f = [...](...) -> T* { ... }`。SegmentRefCard::mkSentence 与 LinePropertyDialog::buildEndpoint 两处踩实；改完顺手验证文件头 BOM 仍在（python utf-8-sig 读 + 默认写会剥 BOM，必须 binary 模式回写）。

- **ElaTabWidget 标签默认 220×35 定宽（ElaTabBarStyle.cpp `_pTabSize`），窄容器必调 `setTabSize`**：3 枚标签 = 660px（2026-08「点连接」只读标签页删除后；曾为 4 枚 880px），容器比这个窄时 QTabBar 的 tabRect 会**溢出 bar 自身的 width**——视觉上标签被裁，且 tabRect 内的点在 bar 几何之外，点击/childAt 命中落空（test_dialog_tabs::probeTabHitArea 曾因此红：540 宽对话框 tab 命中 null）。LinePropertyDialog 用 `setTabSize(116,35)`（ElaTabWidget 公开 API，透传到 ElaTabBarStyle）。凡在 <900px 宽的容器里放 ElaTabWidget 都必须显式设标签宽。
- **QTreeWidget stylesheet 需 ::indicator**：连接图标/勾选框不渲染时检查；列宽 0 需容纳分支箭头。
- **LayerPanel refresh 必须 QueuedConnection**（否则 use-after-free）。
- **每帧同步槽禁止 setStyleSheet**（re-polish 数毫秒级），只在离散状态翻转时调用；setText 做同值短路；批量操作禁用布局避免 O(N²)。
- **Debug 构建性能波动 30%+**：断言用宽松边界（如 ≤2.0x），勿用严格排序。
- **`QGraphicsScene::render` 裁剪到 item boundingRect**：画在 boundingRect 外的 label（如 `mid + (4,-4)`）会被剪掉，像素级渲染断言误报「label 没画」；必须用 QGraphicsView::grab()（真实 view 路径不裁剪）做画布像素验证（tests/test_hold_show.cpp 的教训）。
- **cache 里的预格式化文本与开关绑定**：BlockItem rebuildCache 的 lengthText 曾只在 showLength=true 时格式化 → hold-to-show 强制显示（L 键）拿不到文本；需要强制显示的内容必须无条件预格式化（BlockItem.cpp 直线/曲线两处）。
- **AngleHud 输入文字灰得看不清（2026-08 修复）**：根因 = setValid() 的 `qobject_cast<QGraphicsView*>(parentWidget())` 必然失败——AngleHud 的 parent 就是 viewport（纯 QWidget），必须 `parentWidget()->parentWidget()` 才到 CanvasView；cast 失败静默走 fallback 白底浅灰字（(255,255,255,245)/(232,234,237)），亮暗主题都看不清。同文件构造函数里用的是 viewport->parentWidget() 而 setValid 里用的是 parentWidget()——两处 cast 不一致的教训：HUD/悬浮控件取主题一律从构造时解析一次 CanvasScene 存成员。
- **悬停显隐按钮导致布局跳动（2026-08 修复）**：卡片头部悬停才显示删除按钮（setVisible 切换），按钮 20px 出现/消失 → 行内宽度挤压 + 行高变化 → VirtualCardList 收到 LayoutRequest 触发 remeasureRow → 整列下移，光标划过时"跳动一下"。修复：同尺寸透明占位 `m_deleteBtnSlot` 与按钮互斥显隐（enter/leave 互换），布局空间恒定、不触发重测。凡虚拟化列表里的行内 hover 显隐元素都必须占位恒定。
- **chip 本体 paintEvent 被 Qt 合成器以空 clip 调用，自绘静默丢弃（2026-08，CopyChip 描边消失根因）**：`CopyChip`（QWidget，覆写 paintEvent 自绘背景+1px 描边）的绘制在面板悬浮窗里完全不可见（编辑态覆盖层 ElaLineEdit 隐藏后"框消失"）。逐项探针排除了所有常规嫌疑：paintEvent 确实执行（计数+1）、尺寸正常（278x20）、`ev->region()` 全尺寸、`visibleRegion()` 全链非空、`p.device()==this`、`paintEngine()` 非空、`deviceTransform()` 正确（1.25 缩放+偏移）、`isActive()` 为真、绘制代码画到独立 QImage 正常（584px）——**唯独 `QPainter::clipRegion()` 是 0x0**，且 `setClipping(false)` 也无法绕过；grab/render/面板抓图/真实屏幕全无输出。**唯一正常上屏的是 chip 的子控件 ElaText label（文本可见）**。绕行方案（最终修复）：新增 `ChipLabel : ElaText`，把背景+描边画进 label 的 `paintEvent`（先画框再调 `ElaText::paintEvent` 画文本，与文本同一绘制上下文），chip 本体不再绘制；hover 由 `CopyChip::setHovered` 转发给 label。验证：面板抓图+真实屏幕抓图两个主题均 OK（暗 882px / 亮 1506px）。排查教训：①探针测试里的面板翻页定时器（200ms 循环切 QStackedWidget）会污染一切抓图时机，必须先禁用；②隐藏窗口上 repaint()/grab() 全部无效，唯一可信的是 `QScreen::grabWindow` 真实屏幕抓图；③面板内有多个 VirtualCardList（每页一个），findChild 只返回第一个（可能是 100x0 退化列表），必须枚举；④像素探针的"边框色匹配"可能误报——曾有 198px 集中在一行的分割线被当成 chip 框，须按区域/形状分布核对。
- **面板展开/滑动不跟手 + 卡片复用三坑（2026-08 修复，用户报告"展开和滑动时有点不跟手"）**：VirtualCardList 滚动时行离开窗口即 `deleteLater` 销毁、回程重建——Ela 卡片（CopyChip/ElaLineEdit/按钮）构造昂贵，快速滚动/折叠展开逐行重建 = 卡顿。修复：**行 widget 池（park 缓存）**——`destroyWidget` 不再销毁，清焦点后 hide 入 `m_cache`（按 key，`m_cacheOrder` FIFO，上限 `kCacheCap=64` 超出逐出 deleteLater）；`createRow` 先 `takeCached(key)` 原实例复用 + binder 全量刷新 + show。**坑三连带**：①**只能同 key 复用**——卡片 ctor 捕获 m_id（/groupId），错 key 复用会删/改错误对象；②**alternate 奇偶必须重设**——复用卡片在新行号下奇偶可能翻转，五张卡片（Variable/Linked/Formula/Measure/AngleMeasure）新增 `setAlternate(bool)`（变更才 update），binder 每次按 row%2（公式页 localIndex%2）重调，否则左侧竖线颜色停留旧行；③**序号必须重设**——变量/关联卡补 `setIndex`（公式=组内序号、变量/关联=row+1、测量="测 N"/"角 N"），binder 每绑定重调（文本同值短路）。**额外坑**：消失行的高度缓存会被 setRows 剪枝——复用路径在 `m_heights` 缺失时必须重测（否则新行高落回 kDefaultRowHeight 错位）；高度在缓存中（滚动回程）则跳过重测。测试：test_formula_groups 加缓存复用断言（折叠消失→展开取回同一实例）+ cardOrdinalsAndAlternate（序号文本 + 竖线像素色 蓝#2F6FED/橙#F59E0B，`card.grab()` 触发 paintEvent 取 `(1, h/2)` 像素）。
- **公式页分组四坑（2026-08 修复，用户报告"数量不更新/箭头错/无分组提示"）**：①**VirtualCardList::setRows 结构变化后存活行不重绑定**——同 key 幸存 widget（如分组头）在 keys 改变（拖入公式/折叠/重排）时既不销毁也不重新绑定，分组头成员数量 `(N)`、折叠箭头、卡片序号全停留在旧值（拖公式进组后数量不变；点折叠后箭头不翻转 = "打开小箭头经常显示错误"）。修复：setRows 结构分支末尾统一 `rebindAll()`（幂等、O(可见行)），binder 里 setCount/setCollapsed/setIndex/setGrouped 全部实时取自 doc。**坑**：keys 相同走 rebindAll、keys 不同只重建消失行——两个分支都曾以为对方覆盖了存活行，实际谁都没管。②**分组头/组卡片 [dropping] 高亮无 QSS 规则 = 拖放提示完全不可见**——FormulaGroupHeader/GroupCard 的 setDropHighlight 只置属性 + unpolish/polish，但 Theme::buildStylesheet 从未定义 `[dropping="true"]` 规则，属性匹配空样式（"拖进分组也没有分组被选中的提示"）。修复：Theme.cpp 补 `QWidget#FormulaGroupHeader` / `QFrame#GroupCard` 的 hover(@surface2) + dropping(@accentTint + 1px @accent 描边) 三条规则。**坑**：此类"属性驱动高亮"必须同时在主题表登记样式，只写 setDropHighlight 等于没写。③**组内公式与未分组无视觉区分**：FormulaCard 新增 `setGrouped(bool)`（kGroupIndent=16px）——组内卡片内容 + 左侧竖线整体右移，paintEvent 竖线 x 同步偏移；VariablePanel factory 与 binder 两处都要按 `fr.groupId` 调用（漏 binder → rebindAll 后缩进回退）。④**数量刷新依赖 formulasChanged 信号**：VariableStore::moveFormula 已 emit formulasChanged（面板 syncFormulaCards 监听 formulas+formulaGroups 两信号），勿再补发；测试：test_formula_groups（rebindSurvivorsOnStructureChange/moveIntoGroupUpdatesRowsAndCount/moveIntoGroupUndoRestores/collapseToggleHidesMemberRows/groupedCardIndents/cardOrdinalsAndAlternate）。
- **状态栏输入框（预输入条/SegmentEditBar）输入包含（输入快捷键字母工具被切出）**（2026-08 修复）：
  - **根因**：主窗口工具切换动作（选择 V、智能笔 L、曲线 C、旋转 R、打断 B、交点 I、角度测量 A、辅助层 H）注册在主窗口 QAction（`Qt::WindowShortcut`）。当用户在状态栏快速输入框（`SmartPenPreInputBar` / `SegmentEditBar`）中键入线段名（如 `V1`、`L_collar`、`C_neck`、`R_pocket`）或公式变量（如 `A+10`、`H-2`、`W/2`）时，输入框没有安装拦截 `ShortcutOverride` 的事件过滤器，导致单字母快捷键优先被 QAction 抢占并触发工具切换，输入框立刻丢失焦点并切出当前工具。
  - **修复**：`SmartPenPreInputBar` 与 `SegmentEditBar` 对其子输入框安装事件过滤器，在 `QEvent::ShortcutOverride` 中无条件 `ke->accept(); return true;` 阻断全局 Action 抢键；在 `QEvent::KeyPress` 中补全输入流导航：`Tab`/`Shift+Tab` 在名称/长度/角度三框内平滑轮转，`Return`/`Enter` 依次推进并最终交回焦点至画布，`Escape` 清除当前框/全部预输入并还焦画布。
  - **同类漏网（2026-08-29 TOOL_SYSTEM_AUDIT H1 修复）**：`src/ui/AngleHud.cpp`（旋转工具 / ConnectGesture 共用的角度 HUD）漏装同款保护——它创建后立刻 `setFocus()` 到输入框并高亮选中数值，照着占位符推荐输公式 `b/4+5` 的第一个字符 `b` 即触发打断工具，`ToolRotate` 被 ToolManager 销毁重建，旋转会话静默丢失。修复 = `AngleHud::eventFilter` 加同一分支（过滤器本就装在 `m_edit` + `this` 两处，无需改安装点）。回归：test_rotate_copy `hudShortcutOverrideContainment`（8 个工具键 + Ctrl 修饰 + HUD 主体均须 accept，工具类型不变）。**教训：凡"创建后立即接管键盘焦点的 QWidget"，eventFilter 必须处理 ShortcutOverride，与输入框叫什么名字、装在哪层无关。**


- **辅助点/交点的显示名称勾选对空名称点无效（2026-08-24 修复）**：BlockItem 点标签原先只用 pt.name，辅助点/交点默认名称为空，勾选显示名称后无可见变化。修复：点标签按列表/连接卡同规则回退到 serial（有名称显示名称，无名称显示编号）。现有 test_intersection_update 继续覆盖交点随胸围/肩褶E变量更新。
- **关闭程序弹「Microsoft Visual C++ Runtime Library - Debug Error!」断言（2026-12 修复，用户报告"关闭时弹 debug 报错"）**：关窗时 `QtPrivate::assertObjectType<MainWindow>` 断言失败（`"Called object is not of the correct type (class destructor may have already run)"`，qobjectdefs_impl.h:107）。**栈**：`~MainWindow → ~ElaWindow → ~QObject 子对象析构 → ~ToolManager → m_activeTool->deactivate()（ToolManager.cpp:34，N5 设计）→ ToolSelect::onDeactivate → deactivateOverlapContext → Tool::refreshModeIndicator → reportHintOverride → ToolManager::setHintOverride → emit hintOverrideChanged → 命中 MainWindow::onToolHintOverride（半销毁窗口）`。**根因**：`ToolManager` 是 MainWindow 的子对象，子对象在 `~MainWindow` 函数体**之后**才析构；`~ToolManager` 按 N5 约定 deactivate 当前工具，其链路 emit `hintOverrideChanged`，而接收者 MainWindow 已过析构函数体 → Qt 6.11 Debug 构建断言（Release 是 UB）。`~MainWindow` 原来只点名断开 m_undoStack/m_paramDoc，漏了 m_toolManager。**修复（双保险）**：①根因——`~ToolManager` 开头 `disconnect(this, nullptr, nullptr, nullptr)` 断掉全部出向连接（关闭期所有 emit 变 no-op，未来任何子对象析构期发信号都不再踩坑）；②`~MainWindow` 通扫 `children()` 逐个 `disconnect(child, nullptr, this, nullptr)`（落实注释里「Drop every child→window connection」的承诺，m_undoStack 非直属子对象仍需显式断开）。**教训：析构期不得向外界发信号；凡是"析构时会 deactivate/清栈/清列表"的子对象，其出向连接必须在自身析构或父窗口析构体里先断开。**验证：连续 8 次关窗零弹窗（修复前约半数触发）；ctest 31 例仅剩 2 条文档化基线红（serializer bridgeAuxPoint / component dragComponentLeaderCurveFollowStable 31.4798mm），无新增回归。
- **智能笔预输入条消失（2026-12 修复，用户报告"切智能笔时状态栏底部输入窗口没有了"）**：**不是删除，是位置迁移 + 显隐死锁**。2026-08-28 重设计把预输入条（SmartPenPreInputBar）从状态栏迁到**状态栏上方的独立编辑条带 `m_editBand`**（accentTint 底 + accentStrong 描边，§4.6）。但 editBand 初始 `hide()` 后，**父链隐藏时 `setVisible(true)`/`show()` 不会真正显示子控件、也不发 Show 事件** → `updateEditBand()`（只靠 eventFilter 的 Show/Hide 事件驱动）永远不跑 → editBand 保持隐藏 → 预输入条静默消失（探针实测：`preVisible=0 bandVisible=0 bandGeo=(0,0 100x30)`，updateEditBand 零调用）。**修复（双保险）**：①`updateEditBand()` 判**显隐意图**（`!isHidden()`）而非实际可见（`isVisible()`）——isVisible 受父链影响，editBand 隐藏时子条 isVisible 恒 false，是死锁第二环；②所有会改变两根信息条（preInputBar/segmentEditBar）可见性的路径主动调 `updateEditBand()`：onToolChanged（切工具）、onLineCreated、onLinePreview、onEditTargetChanged（选择工具点线段）、onEditStripCancel——不依赖事件。**教训：凡"父控件初始隐藏 + 子控件显隐靠事件驱动父控件显隐"的组合必然是死锁；父链隐藏时 setVisible/show/hide 都不发事件，必须主动同步或改用显隐意图判断。**验证：探针确认修复后 `bandVisible=1 bandIsHidden=0 bandGeo=(8,725 1259x32)`（画布下方、状态栏上方）；7 个 GUI 套件全绿；3 次关窗零弹窗（上一条修复无回归）。
- **数值显示统一去尾零 + 旋转 HUD 公式原文（2026-12 修复，用户报告"预输入默认加 .00 / 旋转 HUD 显示换算值而非变量"）**：①**SegmentEditBar（预输入/编辑条带）数值回显去尾零**——showPreview 与 refreshFields 原来用 `QString::number(x, 'f', 2)`/`'f', 1` 硬编码，显示 `12.00`/`45.0`，违反 2026-12 已拍板「数值回显不要 .00」约定（AGENTS.md「数值回显去尾零」条目只收口了卡片，编辑条带漏网）。修复 = 改用 `Units::formatNumberTrimmed`/`formatCmTrimmed`/`formatDegValue`（SegmentEditBar.cpp showPreview/refreshFields 全部数值回显点；公式原文仍优先显示，与卡片同规则）。②**旋转 HUD 公式驱动时显示公式原文**——ToolRotate::refreshHudText 原来恒显示换算数值（`formatDegValue(currentAngleDeg())`），用户输入变量/公式（followerAngleFormula/arcLengthFormula）后 HUD 回显的是算出来的度数/cm 而非表达式，误导（像在说"值是 22.5"但其实由 b/4+5 驱动）。修复 = 连接线角度/弧长分支先查 `editableAttachment()` 的公式字段，非空则 `setText(公式原文)`，否则才回显换算值（与 SegmentAngleCard「公式优先显示原文」同约定；复制/选集旋转无公式语义保持数值——**注：选集旋转已于 2026-09-04 回归（MarqueeGesture 框选→RotateBlocksCommand 刚体变换），无公式语义性质不变**）。**教训：公式驱动字段的显示端必须"公式原文优先，数值兜底"，否则用户看不到自己输入的表达式。**验证：新增 test_rotate_copy 两例（hudShowsFormulaForFormulaDrivenAngle/Arc，断言 HUD 文本 == "45"/"10"），rotate_copy 37/37 过；smartpen_aux/segment_edit_bar/commands/duplicate/aux_layer/dialog_tabs/measure 全绿；全量 ctest 31 例仅剩 2 条文档化基线红，无新增回归。
- **上下文属性条 ContextStrip 一期收尾（CONTEXT_STRIP_DESIGN.md，2026-12 交付）**：旋转/选择/智能笔三工具共用一条常驻条带（悬停只读预览 / 点击锁定可编辑），替代原 SegmentEditBar + SmartPenPreInputBar 两条互斥 bar + 旋转 AngleHud 三套输入。旧类已删（src/app/SegmentEditBar.*、SmartPenPreInputBar.*），测试迁入 `tests/test_context_strip.cpp`（15 例，含输入包含/键流转/桥线只读/Esc 语义）。本期踩的三个坑（迁移 test_rotate_copy 时暴露）：
  - **条带弧长模式显示必须走"带符号折角弧长"**（ContextStrip::foldedArcDisplay）——多圈弧长（3 圈 ≡ 0° 折叠）直显原值会爆表（113.1cm 应显示 0）。换算 = `mmToCm(degToArcMm(normalizeDeg180(arcMmToDeg(arcMm, radius)), radius))`，radius = `block->segmentLengthAtPoint(att->fromPointId)`；角度模式显示 = `normalizeDeg180(followerAngle)`（带符号折角，同旧 HUD currentAngleDeg），自由线仍世界角 normalizeDeg360。
  - **单位切换（°/⌒）必须几何保持换算，不能只翻 rotationMode**——旧 HUD onHudModeChanged 同解：切换时按存储域 α（normalizeDeg360 当前折角）换算另一单位，否则弧长 3 圈切角度后显示错值；公式驱动仍拒绝切换（绝不烘焙公式）。
  - **测试点击坐标必须落在目标线段上**——迁移前 modeSwitchKeepsFormula 点 vp(270,0) 实际落在基准线 B 线身（B 跨 x∈[200,300]），旧测试靠"HUD 存在即过"侥幸全绿；迁移后断言条带锁定到 follower A，必须从模型解析 A 中段再点击（与 stripShowsFormulaForFormulaDrivenAngle 同范式）。教训：**迁移 UI 测试时，旧用例的"软断言"（只验证组件存在）可能在错误几何上侥幸通过，迁移必须把断言升级到真实状态（锁定了哪条线）并让点击坐标可溯源**。
  - 回归：test_context_strip 15/15、test_rotate_copy 37/37、test_smartpen_aux 26/26（preInputBar* 4 例迁入 test_context_strip）；全量 ctest 31 例仅剩 2 条文档化基线红（serializer bridgeAuxPoint / component dragComponentLeaderCurveFollowStable 31.4798mm），无新增回归。
- **上下文属性条二期：连接手势角度输入并入条带，AngleHud 整体退场（CONTEXT_STRIP_DESIGN.md 二期，2026-12 交付）**：`src/ui/AngleHud.h/.cpp` 已删除（CMakeLists gcad_ui 源列表同步移除），tools 层再无 QWidget。通道 = 依赖倒置四条：①手势→条带：`ConnectGesture` 注入 `m_beginAngleSession` 回调（ToolSelect::onActivate 接线）→ `Tool::reportConnectAngleSession` → `ToolHost::setConnectAngleSession`（ToolManager emit `connectAngleSessionChanged`）→ MainWindow → `ContextStrip::beginConnectAngleSession(blockId, segId, attId, initialAngle)`；全 null = 会话结束。②合法性：`ToolHost::setConnectAngleValidity` → 条带角度框 `angleInvalid` 属性 + Theme QSS 红边。③条带→手势：`ContextStrip` 四个信号（connectAngleTextChanged/ModeChanged/Committed/Cancelled）→ ToolManager `forward*` → `Tool` 虚钩子（connectAngleTextChanged/ModeChanged/Committed/Cancelled，默认 no-op）→ ToolSelect override → ConnectGesture::onAngleTextChanged/onAngleModeChanged/commitAngle/cancelAngle（已移为 public）。**踩的坑**：
  - **会话内条带绝不 push 命令**：连接在 finalizeConnection 前是"飞行中"附件（不入 undo 栈），条带的 applyName/applyLength/applyAngle/onUnitToggled/onReverseClicked 全部要 `m_connectSession` 守卫（否则 SegmentEditBarCommand/ReverseSegmentCommand 会插进手势整步 undo 宏之间，undo 顺序错乱）；refreshChrome 会话分支强制 名称/长度只读 + 角度可编辑（普通 Pinned 分支按 isBridge 设只读会把手动置为可编辑的角度框又锁回去）。
  - **组件级连接的附件匹配**：组件级连接 fromBlockId 为空（借暴露端点），条带 `findEditAttachment` 的"端点匹配"找不到 —— 会话内改按 `attachmentsView().byId(m_connectAttId)` 直取（findEditAttachment 顶部会话分支）。
  - **公式驱动拒绝 °/⌒ 切换的按钮弹回**：手势 onAngleModeChanged 拒绝时**不触发 resolved**（早退无 resolveAll），条带 onUnitToggled 会话分支在 emit connectAngleModeChanged 后**必须自己 refreshChrome() 兜底**把按钮弹回原模式（接受路径由 resolved 再刷，幂等）。
  - **会话 Esc/Enter 不得走普通锁定路径**：eventFilter 会话分支在最前 —— Esc → emit connectAngleCancelled、Enter → emit connectAngleCommitted，绝不 clearPinned/cancelRequested（否则会误删刚建的连接 / 误撤销创建）。条带保持会话态直到宿主 endConnectAngleSession（测试断言会话不自行退出）。
  - **会话上报的跟随线段 = 源端点所在线段**：`ConnectGesture::beginAngleSession` 用 `blk->exitSegmentAtPoint(m_connectFromPoint)`（m_connectFromBlock = 被抓的成员块，组件级同解）；segId 为空（端点不属于任何线段）直接上报全 null 结束会话。
  - **会话期间普通锁定上报必须忽略**：setPinnedTarget 顶部 `if (m_connectSession) return;` —— 工具在 AngleInput 态不发 pinned，但防御性隔离防未来回归。
  - **hideBar 也要清会话标志**：工具切换路径 ToolSelect::onDeactivate → cancel() → endAngleSession（经宿主）与 MainWindow::onToolChanged → hideBar 谁先到都幂等。
  - 回归：test_context_strip 新增 6 例（会话形态/信号出口/Enter-Esc/单位切换/合法性属性/结束恢复）、test_select_wkey 新增 angleSessionStripInputDrivesConnection（击键预览+°⌒+Enter 收尾整步 undo）+ 两处 HUD 断言改会话上报断言、test_component 两处同改、test_rotate_copy 删除 angleHudShortcutOverrideContainment（输入包含由 test_context_strip::shortcutOverrideContainment 覆盖）。
- **上下文属性条三期：测量/角度测量/交点/打断的只读悬停接入（CONTEXT_STRIP_DESIGN.md 三期，2026-12 交付）**：四工具统一"扫过即看"——各自 hover 更新处上报 `reportHoverTarget`，条带 Hover 态（一期已有）只读显示。挂接点：ToolMeasure/ToolAngleMeasure 的 `updateHover`、ToolIntersection 的 `updateLineHover`/`updatePointHover`（AimAngle/BorrowAim 报空）、ToolBreak::updateHover（断点优先、线身兜底、移出报空）。**坑**：①点吸附的 SnapResult 没有 segmentId——悬停点所在线段必须 `block->exitSegmentAtPoint(pointId)` 现查（游离点 → 报空，条带收起）；线身吸附（SegmentSnapResult）才有 segmentId 直取。②**hideBar() 必须把 Hover 焦点归一化为 Empty**——原实现只清 Pinned 焦点，悬停隐藏后 focusState() 残留 Hover（"Hover" 语义 = 条带可见的只读预览，隐藏后不得残留；三期 hoverShowsReadOnlyPreview 测试暴露）。③工具直驱测试的宿主桩必须实现纯虚 `requestToolSwitch`（ToolHost 唯一纯虚，漏实现 = C2259 抽象类）。回归：test_measure +2、test_tool_intersection +1、test_break +1、test_context_strip +1；全量 ctest 31 例仅剩 2 条文档化基线红。
- **任务中断遗留的三类「半成品」损伤（2026-08-31 面板重设计收尾时发现，逐一修复）**：接手中断任务先跑 build+ctest 拿基线，再对设计文档逐项 grep 核实，**不要信文档状态表**（上次会话标 ✅ 的项实际有缺口）：
  - **①行注释吞噬代码**：上次会话合并语句行时把 `m_snapshot.showName = seg->showName;` 整句黏进了上一行 `///<` 注释尾部 → 该快照字段**从未赋值**，撤销全部时读未初始化 bool（UB）。根因 = 大段重排时把「注释 + 语句」错误拼成一行，编译器不报错（合法注释）。**核实手法**：对改动文件 grep 注释符后跟 `;` 的行（如 `///<.*m_`），凡注释尾部出现完整语句即中招。修复必须把语句拆回独立行。
  - **②有父无布局的「隐藏辅助对象」被 populate 置真**：`m_connCard`（SegmentConnectionCard，保留做测试/逻辑兼容）有父 widget 但不参与布局，buildPage1 里 `setVisible(false)`；上次会话在 populateFromModel 留了 `m_connCard->setVisible(!block->isDart())` → 非省道线全部触发 **(0,0) 孤儿控件叠在本线分区上**。规则：离屏辅助对象可见性必须恒隐藏，任何 populate/refresh 不得翻转；父链上有 widget 的孤儿同样会渲染，`hide()` 必须在每次可能翻面的写入点之后重申或干脆删掉翻面点。
  - **③可见性判定两处写、后者覆盖前者**：SegmentRefCard::refresh 为桥接线 `setVisible(!hasEnd)` 隐藏后，populateFromModel 的 `setVisible(!isDart)` 又把它复显（测试用 waitUntil 轮询掩盖了瞬态）。同类条件（互斥/省道/指向）的可见性判定要**收敛到一处表达式**（现为 `!isDart && !hasEnd`），或保证 populate 后段不再写同一属性。

- **全局悬浮提示（Tooltip）黑底黑字与纯文本绘制缺陷彻底清网（2026-09-03）**：
  - **现象与根因**：变量面板变量页的复合标签（CompoundChip）、线条属性对话框（LinePropertyDialog）、辅助点/交点表单等处鼠标悬停时，出现纯黑背景且文字也是深黑色，完全不可读；即便富文本容器加了背景色，也会在文字外圈留下一圈大黑边/大黑底板（如用户截图中的「端点连接关系」提示）。
    1. **Qt Windows DWM 透明合成机制缺陷**：Qt 的 QTipLabel 在收到纯文本字符串（无 HTML 标签）时直接走原生 QPainter 绘制；当全局 QSS 中存在 border-radius: 4px 等圆角属性时，Qt 自动为 Tooltip 窗口开启 WA_TranslucentBackground；在 Windows DWM 多层窗口合成或特定 GPU 驱动下，未受管制的原生半透明底色混合失败，直接透出 Windows 窗口底层的默认 RGB(0,0,0) 纯黑；配合浅色主题的深碳黑字体（#1A202C），形成「黑底黑字」。
    2. **控件级裸样式级联污染与覆盖机制**：`Theme::dimValueStyle()`（src/ui/Theme.cpp:304）及多个控件直接使用了无选择器的裸属性 `setStyleSheet("... background: transparent; ...")`。在 Qt QSS 层叠机制中，由于局部 Widget Stylesheet 默认优先级高于 Application Stylesheet，弹出的 `QTipLabel` 会无条件继承该透明规则，致使 `QTipLabel` 自身的整个窗口背景（包括 padding 留白与文本未填充区域）全部变成透明，在 Windows DWM 下形成纯黑大外框；富文本容器即使有内部背景色也会被外层大黑框包裹。
  - **治理措施（全仓三层终极加固）**：
    1. 全局 ToolTipGuard 事件过滤器拦截（src/ui/Theme.cpp:16/435）：在 QApplication 级安装 `ToolTipGuard`，拦截全部 `QEvent::ToolTip`。通过将关联上下文转为全局屏幕坐标并将父级设为 `nullptr`（`QToolTip::showText(he->globalPos(), w->toolTip(), nullptr, globalRect)`），彻底在架构层面斩断任何控件子树裸样式向 `QTipLabel` 级联污染的链条，确保任何 ToolTip 100% 纯净遵循全局纸黄色技术样式；
    2. 局部控件作用域收紧与污染源清除：
       - `LinePropertyDialog.cpp:741`: 将 `ptBadge`（点1/点2徽标）样式限定为 `#endpointBadge { font-weight:600; }`，清除裸样式；`makeSectionHeader` 限定 `#sectionTitleLabel`；
       - `Theme.cpp:304`: 从 `Theme::dimValueStyle()` 中彻底删除裸 `background:transparent;`，并清理全仓多余的裸透明设置；
       - `CompoundChip.cpp` / `CardBase.cpp`: 所有复合控件样式严格限定在 `#objectName` ID 选择器内；
    3. 富文本容器兜底（src/ui/TooltipFormatter.h:35）：所有外层 `<div>` 容器显式写入 `background-color: @tooltipBg`；tests/test_tool_hints.cpp 补全 `tooltipFormatterProducesBackgroundColor` 和 `toolTipGuardInterceptsWidgetToolTip` 回归测试，全量守卫与测试全绿。
- **刚进入程序时长按光标中断（2026-09 修复）**：
  - **现象与根因**：刚启动进入程序时，长按光标（按住鼠标左键长按/拖拽图元或操作）极易出现中断。
    1. **画布初始焦点缺失**：`MainWindow` 构造完成并显示后，未显式调用 `m_canvasView->setFocus()`。窗口在刚启动时焦点仍在顶层非客户区或菜单控件上，用户初次在画布上长按时，系统产生初次聚焦和激活交接，容易引发局部的 `FocusOut` 或捕获丢包，导致首击长按中断。
    2. **悬停条带（ContextStrip）延迟 80ms 弹出挤压画布**：鼠标悬停线段 80ms 后会触发 `flushHover()`。如果用户在 80ms 内按住鼠标长按，定时器到期仍会强制 `show()` 条带，通过 `updateEditBand()` 将底部的 `m_editBand` 设为可见。画布容器瞬间被向上挤压 30~38px 并触发 `QResizeEvent`，导致画布坐标跳变、打断正在执行的拖拽或长按手势。
    3. **CanvasView 鼠标事件分发后透传基类冲突**：`CanvasView::mousePressEvent` / `mouseReleaseEvent` / `mouseDoubleClickEvent` 在通过 `m_inputDispatcher` 派发给工具层后，未调用 `event->accept()`，且无条件继续调用 `QGraphicsView` 的基类实现，导致 `QGraphicsView` 内部的 `mouseGrabberItem` 和选择状态与业务工具层发生双重竞争。
  - **修复**：
    1. `src/app/MainWindow.cpp:188`：构造函数末尾添加 `if (m_canvasView) m_canvasView->setFocus();`，确保启动即具备稳定焦点。
    2. `src/app/ContextStrip.cpp:336`：`flushHover()` 增加 `if (QApplication::mouseButtons() != Qt::NoButton) return;` 守卫，按键按下期间禁止弹出悬停条带；`clearHover()` 补齐 `m_hoverTimer->stop();`。
    3. `src/canvas/CanvasView.cpp:142/200/215`：`mousePressEvent`、`mouseReleaseEvent`、`mouseDoubleClickEvent` 在工具分发后显式 `event->accept(); return;`，阻断基类重复竞争。

## 4. 数据 / 引擎

- **addAttachment 拒绝跨层**（isAuxBlock 不一致）：测试构造桥 pin 前必须先设工作层。
- **新增 undo 命令**：记得 geometryEpoch 递增与快照字段完整性；删除宿主时关联变量自动删除、引用副本固化回数值。
- **旋转工具黄圈（updateGizmo）全模式大一统几何一致性（2026-09 用户报告「黄圈显示不一致」，已彻底修复）**：
  - **根因分析**：①自由线分支遗留旧代码 `arcStart = 0.0; arcEnd = deg`，永远从水平 0° 线性插值，未旋转时凭空挂大圆弧，终点锚心时甚至在空中画 180°/240° 怪异半圆；②终点锚心时线段朝向反向 180°，但旧实现未补偿锚心翻转，导致基准虚线与黄弧画在相反半球空地上，与实体脱节；③连接线分支灰虚线 `dashRad` 漏更新，与黄弧端点反向 180° 脱开。
  - **大一统原则**：①灰虚线 `dashRad` 恒为旋转参考基准（复制=原线朝向，普通旋转=起手世界朝向，连接线=闭合折叠基准）；②黄弧 `m_arc` 恒为两线之间的内夹角弧（跨度归一化到 $(-\pi, \pi]$，永远 $\le 180°$）；③黄弧基准端永远精准贴合灰虚线，活动端永远精准指向当前线段；④未拖动旋转时黄弧跨度为 0（空路径不画单点），彻底消除多余怪圆。
  - 回归测试：`test_rotate_copy` 新增 `gizmoDisplayConsistencyAcrossModes`（44/44 全绿）。
- **旋转复制锚心语义（方案 B，2026-08 用户拍板）**：副本相对角 0° 恒 = 与原线**重叠**（AutoCAD 风格），HUD 显示"旋转角度" = 绕锚心角，起点/终点锚心一致（2026-08 修复：旧实现"锚心偏移 0/180°"只对水平自由线碰巧正确，斜线/连接线差 180°−α —— 用户报告"复制以 180° 创建"）。实现：RotateCopyGesture 存 `m_baseOffsetDeg = 原线世界朝向 − 挂接点出口世界方向`（原线世界朝向 = ToolRotate::originalWorldRotDeg()：连接线 = refWorld+π−α、自由线 = 首段世界方向+终点锚心 180；挂接点出口 = `rotation + exitDirectionAtPoint(锚心, leaderSegment)` = 克隆 Resolver 的 refWorld，存 `m_attachExitRad`）+ 相对角；**存储翻转 followerAngle = 180 − (baseOffset + relDeg)**，commit 同翻；显示层 2026-08 v3 起逆时针为正（显示 = 相对角本身，无镜像）：currentRelativeAngle = fmod(180 − baseOffset − followerAngle, 360)，convert/applyAngle 直接换算，不再 fmod(360−deg,360)；relToWorldRad/worldRadToRel 公式基准 = **m_attachExitRad**（2026-08 修复，旧 m_copyRefWorldRad−π 基准已废，begin 时 refLineDeg 翻转已删；begin 用 live 姿态、convert 用 m_baseTf 姿态，两处出口方向分别计算）。几何推论：起点锚心拖 +90° → 副本绝对 +90°（原线朝向 + 相对角）。测试：test_rotate_copy 六处复制角度断言按此更新（ctrlDrag 90 / diagonal 45→135 / consecutive 90+180 / dropsAim 135 / idleKeepsAim 90 / endAnchor 135）。
- **已连接线段禁止切换锚心（2026-08 用户拍板）**：任一端点有 attachment 时 X 键/点击端点切换一律拒绝（ToolRotate.cpp toggleAnchor + mousePress 锚心点击分支），保持 Connected 模式 = 编辑跟随角，跟随绝不断开；要换锚心必须先断开连接（旋转 = 放弃跟随的语义只对自由线开放）。测试：connectedLineXAnchorSwitchBlocked。
- **拆开保留角度（2026-08 用户拍板，勿回退为"拆开=彻底断开"）**：拆开只解除**位置吸附**、保留**角度跟随**（A 的角度 = B 当前方向 + 拆开前跟随角 α；B 旋转 A 跟着转、平移 A 不动角度）。实现：Attachment 新增 `angleOnly`（Resolver.cpp applyAttachment 只写 rotation、跳过位置约束；序列化 "angleOnly" Optional since v5）。**所有拆开路径统一转 angleOnly**：ToolSelect quickDetachSelection（**D 键快拆，2026-09 新增**，选中线一键拆开，经 undo 栈 SetAttachmentAngleOnlyCommand；非 pin 转 angleOnly、pin 跳过）、ToolSelect endDrag（拖跟随线拆散，非 pin 转 angleOnly、pin 仍 Remove）、**属性面板「拖动保护」取消勾选（2026-10 用户拍板新语义：面板「位置吸附」✗ = 彻底断开、=仅角度的入口改到「拖动保护」✗，2026-08 旧入口"取消位置吸附=仅角度"已废止）**。**彻底断开是单独操作**：「位置吸附」取消勾选（2026-10 新语义，RemoveAttachmentCommand，断开后记忆最近宿主一键恢复）/「清除」按钮 / ToolRotate 旋转=放弃跟随（仍 RemoveAttachmentCommand，勿改）。坑：①angleOnly 自动清 isLocked（位置自由 ↔ 焊接互斥），lockedClosure 跳过 angleOnly；**面板「拖动保护」在仅角度态可用**（勾选 = 恢复完整连接）；②恢复完整连接（面板勾回「拖动保护」/ undo）= SetAttachmentAngleOnlyCommand redo(false) 位置重新吸附回宿主点并重新焊接——**redo(false) 必须置 isLocked=true，不得沿用 m_oldLocked=false**（仅角度态 isLocked 恒 false，沿用会画出"✓ 拖动保护但可拖拆"）；③**beginConnect 快拆已清理（2026-09）**：tryPointOperation 的 isFollower 过滤保证 beginConnect 只对非 follower 块发起（每块至多一条跟随连接；**例外 = 仅角度 angleOnly 跟随线**：位置自由，2026-12 起放行其拖端点重挂，见下条），原快拆循环是死代码——`m_connectOldAtt` 及其在 beginConnect/cancel/commitConnectMove/finalizeConnection 的快照-还原-入栈分支一并删除（旧描述"拖端点快拆 remove+re-add"作废）；拆开统一走 D 键 / 面板 / 拖跟随线三条路；④角度/弧长公式、弧长模式、旋转工具 Connected 模式（编辑跟随角）对 angleOnly 全部照常（驱动方不变）；⑤SetAttachmentAngleOnlyCommand 构造时快照当前 angleOnly/isLocked（恢复-重放模式），undo 原样还原。
- **连接语义定稿（2026-10 用户拍板两轮，起点=用户报告"拖动保护勾选无效，取消位置吸附变成仅角度"，终点=用户提案"拖动保护与组件重叠，默认焊接干扰灵活性、快拆步骤多，把整体移动交给组件"）**：
  - **最终语义（三态 + 可选焊接）**：**新建连接默认不焊接**（addAttachment 不再强制 isLocked=true，ParamDocument.cpp:771；isLocked 字段级默认 false）——连接默认可拖拆（拖跟随线 = 快拆保留角度，endDrag 既有行为 SetAttachmentAngleOnlyCommand(true)，与 D 键快拆一致，零步骤）；「拖动保护」变为**可选焊接开关**（面板 checked = isLocked：✓ 焊接拖不拆 / ✗ 解焊仍完整连接；仅角度态勾选 = 恢复完整连接+重新焊接）。**多线整体移动交给组件**（componentClosure 已承担，无需改引擎；创建组件 toast + 组件管理页空态加提示）。**「位置吸附」✗ = 彻底断开**（RemoveAttachmentCommand）；仅角度（angleOnly）由 拖拆 / D 键产生；断开后**记忆最近宿主**（m_hostMemBlockId/m_hostMemPointId：setTarget 按当前连接播种、断开/清除时记录、重连后刷新；自由态 P 输入框回显记忆 P，勾选一键恢复、角度反算零跳变）；「清除」保留（同语义显式入口）；终点指向（endTarget）独立、断开不清空。
  - **遗留中间态**：isLocked=false && !angleOnly（旧版"位置吸附、可拖拆"）在新语义下是**新默认**（不再是中间态）；旧档 isLocked=true 读出来保持焊接（不迁移，用户可自行取消）。
  - **连带修复**：①`SetAttachmentAngleOnlyCommand::redo(false)`（恢复完整连接）原置 isLocked=m_oldLocked（仅角度态恒 false）→ 恢复后"✓ 拖动保护但可拖拆"——改为 **isLocked=true 重新焊接**（与 doc API setAttachmentAngleOnly(false) 一致）；②`setAttachmentSlideMode(None)`（滑轨切回全连接）原自动 isLocked=true（重新焊接）→ 改为 **isLocked=false 不自动焊**（拖动保护可选；undo 路径经 SetAttachmentSlideModeCommand 快照恢复原态）。
  - 回归测试：test_select_wkey `connectionCardNewSemantics`（初始未焊 → 勾=焊接 → 取消=解焊仍完整连接 → 仅角度态勾=恢复+焊 → 位置吸附✗=断干净 → 勾回=宿主记忆一键恢复零跳变且默认不焊 → undo 恢复连接）；配套更新 test_commands/test_aux_layer/test_smartpen_aux 中"新建默认锁定"断言（~8 处翻转为默认不焊，lockedClosure 用例改显式焊接）。
  - **2026-08-24 用户再拍板：新建连接默认勾选「拖动保护」复旧（回滚"默认不焊"）**。改动三处：①`ParamDocument::addAttachment` 校验后强制 `att.isLocked = true`（ParamDocument.cpp ~800 附近）；②`addComponentAttachment` 同样置位（组件级连接默认焊接）；③`Attachment::isLocked` 字段默认仍为 false（供反序列化/undo 回放/解焊态使用，**勿改字段默认**——改字段会波及 addAttachmentRaw 的快照还原）。语义 = 整对移动默认、拆散走 D 键快拆 / 面板取消「拖动保护」；解焊（✗）仍完整连接可拖拆；仅角度态勾选 = 恢复完整连接+重新焊接（不变）。配套翻转的测试断言：test_aux_layer（auxConnectionDefaultUnwelded→Welded / workingConnectionDefaultsUnwelded→Welded，manual toggle 顺序适配）、test_commands（:1359/:1401/:1430/:1585 默认焊）、test_select_wkey（quickDetachKeyD :1084/:1140 默认焊 + connectionCardNewSemantics 初始态 ✓✓ 且"勾选"步骤改为取消/再勾选 + 重建默认焊）、test_smartpen_aux :472（默认焊）。工具提示/注释同步（SegmentConnectionCard.cpp 拖动保护/位置吸附 tooltip）。
- **仅角度线拖端点无法重挂（用户报告 2026-12：「使用了引用线段但没有连接线段时，拖动端点建立连接没有吸附反应」）**：拆开（D 键快拆/拖拆，angleOnly = 位置自由、角度随基准线）后的线段在属性面板正是「引用了引用线段但没有位置连接」的状态；此时拖动端点期望重新建立位置连接，但没有任何吸附反应。根因 = 2026-09 清理 beginConnect 快拆时，`ToolSelect::tryPointOperation` 的 isFollower 过滤把**所有** follower 块拦下（森林不变式：每块至多一条线级跟随连接），angleOnly 块位置本是自由的，却连抓取都进不去 → 拖折叠成整线移动（无磁铁/无吸附环）。修复：①tryPointOperation 放行 angleOnly 跟随线（仅角度 = 位置自由，专为重新挂接）；②ConnectGesture 新增 `lineConnectValid`（磁铁判定/候选池共用）：angleOnly 源把将被重挂的自身附件**排除在校验外**（重挂 = 原附件换代，不占新 follower 名额）+ 跨层单向契约显式补齐（重挂绕过 addAttachment 校验）；③release 到目标 → `reattachAngleOnly`：原附件原地重挂（旧角度基准保留为独立角度基准 → 双基准、恢复完整连接并重新焊接 isLocked=true），与卡片 onTargetResolved 语义一致；④`cmd::ReconnectAttachmentCommand`（新命令）：新态（含 HUD 角度调整）+ 旧仅角度态 + 拖前 transform 全部固化，**单次 undo 即回到拖前仅角度态**；ReattachAttachmentCommand 同步补「仅角度旧态重挂 = 重新焊接」（isLocked=true，与 SetAttachmentAngleOnlyCommand redo(false) 同规）。坑：**连接手势的 undo 栈 = ParamDocument 内部栈**（ToolSelect::activate 用 m_paramDoc->undoStack() 构造 ConnectGesture；MainWindow 的 Ctrl+Z 是另一只栈——测试里用 `doc.undoStack()->undo()` 验证，如 connectionCardNewSemantics）。回归：test_select_wkey `angleOnlyEndpointDragReconnects`（预置 B→A 完整连接 → 拆为仅角度 → 拖 B 端点到 C 终端 → 断言原附件同 id 重挂 / angleOnly=false / isLocked=true / toPoint=新端点 / angleRefBlockId=旧 A（双基准）/ 端点吸附 / HUD Esc 收尾 / doc.undoStack 单步 undo 回仅角度态）。ctest 22/26（2 红 = 既有基线 + 2 例依赖 E:\3.gcad 活档内容：test_intersection_update/test_extend::savedDocFormulaStartExtendRenders，与本次改动无关）。
- **有对齐点方向 + 变量时接入新线段覆盖变量（用户报告 2026-xx：「线段有对齐点方向且使用变量时，接入新线段会把变量直接转换成数值」）**：仅角度（angleOnly）线段使用自定义对齐点方向（`angleRefBlockId` 非空）+ 变量（`followerAngleFormula`，如 LL=90）时，拖端点接入新线段走 `ConnectGesture::reattachAngleOnly`，旧实现无条件 `followerAngleFormula.clear()` 并把变量烘成数值、还进入角度输入会话需回车。根因 = 重挂逻辑把「方向基准 = 新宿主」当唯一前提，忽略了有对齐点方向时方向基准 = 对齐点、与新宿主无关。修复（用户拍板：有对齐点方向时接入新线段 = 仅连接，原对齐点方向与变量不变，回车 = 确定连接而非确定角度）：①在 `preserveAngleRefOnReattach` 之前记录 `hadUserAngleRef = !mut->angleRefBlockId.isNull()`（**必须在固化前记录**——自动态固化也会置 angleRefBlockId，但那是引擎为保持方向基准不漂移的自动行为，不算用户自定义对齐点，无自定义对齐点的普通重挂仍走旧反算+清公式路径）；②`hadUserAngleRef` 为真时跳过反算覆盖与清公式（对齐点方向与变量原样保留）；③`hadUserAngleRef` 为真时不进入角度输入会话，直接 `finalizeConnection()` 收尾（回车 = 确定连接）。坑：**判断"用户自定义对齐点"必须用重挂前的 angleRefBlockId 状态**，不能用 preserveAngleRefOnReattach 之后的（自动态固化会污染判断）。回归：test_select_wkey 新增 `angleRefWithFormulaReattachKeepsFormula`（预置 B→A 完整连接 → 拆为仅角度 → 设自定义对齐点 A + 变量 LL=90 → 拖 B 端点到 C 终端 → 断言 angleRefBlockId=旧 A / followerAngleFormula=="LL" 原样保留不烘数值 / 状态直接回 Idle 不进入角度会话 / doc.undoStack 单步 undo 回仅角度态且变量保留）。
- **仅角度重挂反算分支用错基准 + 清公式（2026-12 用户报告「L1 拆开后重挂到 L3，变量 GG 被强制换算成 90° 并消失，且 L1 立刻翻转方向」）**：仅角度（angleOnly）线段拖端点重挂到新线段走 `ConnectGesture::reattachAngleOnly`，旧实现按 `hadUserAngleRef = !mut->angleRefBlockId.isNull()` 分叉——拆开态 angleRefBlockId 为空（角度基准 = 位置宿主 L2 隐式），判为 false 走反算分支：`followerAngle = backSolveFollowerAngle(...)` 用**新宿主 L3** 的 refWorld 反算成数值 90°、`followerAngleFormula.clear()` 清掉变量 GG。两个 bug 同一根因：①`preserveAngleRefOnReattach` 已把角度基准固化为旧宿主 L2（`angleRefBlockId = L2`），Resolver 实际用 L2 的 refWorld 驱动旋转，但反算分支却用 L3 的 refWorld → 基准不一致 → L1 方向翻转；②`followerAngleFormula.clear()` 清掉 GG → 变量消失、角度输入框变 90。修复（用户拍板：仅角度重挂 = 仅连接，无论角度是数值还是公式都保留旧基准 L2 + 原角度值，直接完成连接不进入角度输入会话）：删除 `reattachAngleOnly` 的反算分支与 `hadUserAngleRef` 判断，重挂**始终**保留 `followerAngle`/`followerAngleFormula`（不反算、不清公式、不改 rotationMode/arcLength），统一走 `finalizeConnection()` 收尾（回车 = 确定连接而非确定角度）。`preserveAngleRefOnReattach` + `repinShadowAnchorOnReattach` 调用保留（负责把 L2 固化为角度基准、重钉影子锚点）。坑：**「仅连接」语义下角度基准与公式是持久状态，重挂只改位置宿主（toBlockId/toPointId/toSegmentId），任何反算/清公式都会破坏「拆开保留角度」的既有约定**。回归：test_select_wkey `angleOnlyEndpointDragReconnects` 补断言 followerAngle==90.0 原样保留 + 重挂后直接回 Idle（原断言「进入角度输入 + Esc 收尾」已随语义删除）。
- **影子偏转 + 冻结角度基准机制已删除（2026-09 用户拍板删除）**：拆开（angleOnly）保留角度回归 2026-08 首版语义 —— 拆开只解除位置吸附，角度仍由活的基准线驱动（B 旋转时 A 跟着转，平移 A 不动角度）。删除内容：`Attachment` 的 shadowAnchorRotDeg/noFollowRotate/angleRefFrozen 三族字段、Resolver 影子叠加与冻结快照分支、`repinShadowAnchorOnReattach` 与各重连入口重钉调用、旋转工具影子基准虚线、面板「影子偏转」行与「不跟随旋转」chip、两个影子命令类。序列化不再写出对应键（旧档多余键天然忽略，零迁移）；磁盘格式保持 v2，`migrateV1ToV2` 改为仅丢弃 baselineOffsetDeg 残留键。回归：test_commands `setAttachmentAngleOnly_keepsFollowAngle` 断言改回「拆开后 A 旋转 B 跟转」；test_dialog_tabs `reattachPreservesAngleRef` 改断言「自动态拆开重连固化为两点基准」。
- **滑轨模式（抽屉式滑动，2026-08 用户拍板）**：连接姿态保持（角度跟随不破坏）但位置只留一个自由度——基准线局部系（x=沿基准线、y=垂直，基准线旋转时滑轨跟着转）。实现 = Attachment::SlideMode（None/AlongLeader/PerpLeader）+ slideAlongMm/slidePerpMm（mm 锁轴坐标快照）。坑：①**Resolver 位置必须只从存储坐标解算、禁止现场投影**——基准线旋转后跟随线的旧世界位置是"过期"的，现场投影会得到新的自由轴坐标，导致跟随线顺滑轨意外滑走；②拖动回写（ParamDocument::updateSlideOffsetsFromCurrent）**只写自由轴**（AlongLeader→slideAlongMm、PerpLeader→slidePerpMm），锁轴保持激活时快照；③SetSlideOffsetsCommand **redo 只写坐标不 resolve**（它与 MoveBlockCommand 同宏，若自己 resolve 会先按新坐标钉位、MoveBlock 再叠 delta 造成双倍位移）；**undo 必须 resolve**（宏撤销时 MoveBlockCommand.undo 已按新坐标结算一次，旧坐标恢复后需再结算才回到拖前滑轨位）；④滑轨与 angleOnly 互斥（进滑轨清 angleOnly+isLocked；切回 None 恢复全连接但**不自动焊接**——拖动保护可选，2026-10 拍板）；⑤快拆（ConnectGesture beginConnect）与拖拆收集（ToolSelect beginDrag m_detachedAttachments）必须跳过滑轨附件，否则端点拖/整线拖会失去滑轨约束；⑥激活/重定向时用 computeSlideOffsets 快照双坐标（全连接时 =0,0 不跳动），重定向指向点后必须 refreshSlideOffsets 在新基准线局部系重快照；⑦滑轨附件 isLocked 恒 false → lockedClosure 不焊接、面板拖动保护复选框禁用；⑧SlideMode 是命名空间级枚举（非 Attachment 嵌套类型），引用一律 cad::param::SlideMode::X，写成 Attachment::SlideMode::X 会报 C2039。
- **线条属性卡「= 绝对角度」不实时刷新（2026-08 修复）**：用户报告跟随角度卡片里绝对角度的计算不是实时刷新的。根因有二：①`SegmentConnectionCard::applyAngle()`（写跟随角度/弧长后）只 emit `changed(AngleApplied)` 不刷新读数——`m_lblWorldAngle`（= 绝对角度 xx°，公式 refWorld+180°−线夹角）停在旧值；②对话框是**非模态**（`dlg->show()`，ToolSelect.cpp/ToolRotate.cpp），卡片不监听 doc 信号——拖动/旋转基准线时读数不更新。修复：applyAngle 末尾调 `refreshCard()`（只刷标签/行状态，**不调 populateAngleField**——那会覆盖用户正在输入的角度文本）；抽取 `updateWorldAngleLabel(att)`（角度/弧长两分支共用，闭合基准世界角，文本同值短路）并在卡片构造时 `connect(doc, &ParamDocument::resolved, ...onDocResolved)` 轻量刷新。坑：每帧槽里禁止 setStyleSheet / populateAngleField（重排版 + 覆盖输入）；刷新路径只动 m_lblWorldAngle 一个标签。
- **线条属性面板角度输入框不随旋转工具实时更新（2026-09 修复，用户报告「旋转线段时面板只有世界角度更新、输入框不更新」）**：旋转拖动每帧走 `resolveForDrag`，其末尾照样 `emit resolved()`（ParamDocumentResolver.cpp:698）——`ContextStrip` 订阅 resolved 每帧回填角度格（ContextStrip.cpp:61-70，含 hasFocus 焦点保护），但 `SegmentAngleCard::onDocResolved` 只刷「= 世界角度/绝对角度」标签、从不回填输入框 `m_editAngle` → 面板输入框滞留旧值。修复 = onDocResolved 开头加「输入框未聚焦 → QSignalBlocker + populateAngleField() + refresh() 回填」分支（与 ContextStrip 同款焦点保护：聚焦中回填会打断敲击、覆盖未提交输入）；**桥接线除外**——其输入框内容由 setBridgeReadOnly 维护（测出的世界角），回填会覆盖成存储的跟随角。坑：回填必须 QSignalBlocker 包住 setText，否则 textChanged → onAngleDirty 把 fx 指示/样式刷脏；沿用「公式原文优先、数值兜底」约定（populateAngleField 已实现）。验证：test_dialog_tabs / test_context_strip / test_rotate_copy 全绿。
- **曲线点连接不跟随（2026-08 修复）**：用户报告"连接曲线点的线不可靠、不会跟随连接的点"。排查结论：普通 Attachment 连曲线锚点/曲线插值点（CurveAnchor / Interpolated）的引擎行为**本来就是对的**（刚性移动/旋转/变形/拖拽路径全部 delta=0，勿往 Resolver 乱改）；真正根因 = **ParamDocument 的曲线锚点 follow 后处理**（CurveAnchor 点带 `followBlockId/followPointId` 跟随另一目标点，ParamDocument.cpp 曲线锚点 follow post-pass）在**全部 Resolver 沉降之后**才移动锚点——挂在锚点上的 Attachment 跟随线本帧留在旧锚点位置：拖动最后帧滞后一拍；若锚点所在块不在 affected 集则整帧不沉降。修复：后处理记录 `followMoved`，之后按本次 ran 的 scope（WorkingOnly / AuxOnly，带 effAffected 收窄）重跑一次纯 `Resolver::resolveAll`（顺带重建曲线 span 缓存 + 6b 插值点重算；无递归——follow 后处理在 re-settle 之后不会再跑）。回归测试：test_commands `curvePointAttach_followsLeader`（①直线锚点/Bezier 插值点刚性+变形跟随 ②follow 目标拖动 → 锚点与挂线同帧到位 delta=0，修复前 delta=30mm）。
- **曲线跟随对象拖动时抖来抖去（2026-09 用户报告）**：用户观察"拖动不改形状，为什么每帧重算、还抖"。根因 = `Block::resolve` **每帧无条件 `rebuildCurveCache()`**——拖拽帧内受影响集的每个曲线块都重做 C2 三对角求解 + 0.1mm flatten + 弧长积分（纯浪费），且画布每帧拿重建结果重画，任何沉降微余量都会以"画出来的折线逐帧差异"形式放大成可见抖动。修复（用户方向：拖动不改形状就不重算）：**曲线缓存惰性重建**——只有局部几何真正变化（geometryEpoch 增量 / 缓存未冷 / 曲线性变化）才重建；纯刚体拖动 span 冻结，消费方（exitDirectionAtPoint/SnapEngine/画布/切线手柄）读本地 span + 当前变换 = 精确刚体运动。**配套铁律（漏了就地失效）**：凡是"曲线结构变了但点没动"的路径必须显式 `++block->geometryEpoch`——AddCurvePoint/RemoveCurvePoint 的 redo+undo、ToolCurveEdit::placeCurvePoint（无 undo 路径）；`Block::curveCacheBuilds` 计数器锁"拖动零重建"回归（test_resolver `rigidDragKeepsCurveCacheFrozen`：平移+旋转后 epoch/builds 不变、flatLocal 逐字节相同；percent 变化 → 恰好重建一次）。经验：帧级缓存的重建条件必须与"几何真实变化"等价（epoch delta），不能按"每次 resolve 都重建"图省事——它把"重算"与"形状变化"强行绑定。测试：test_resolver 新增用例序号见上；既有 curveAnchorResolvesOnChord/curveAnchorFollowsEndpoints 不受影响。
- **曲线跟随组件拖动时微抖（2026-09 修复，真实文档复现）**：曲线锚点宿主段端点是 Polar(公式=测量变量 M_xxx)，且**测量源点之一在组件成员块上**（如 M_ 测量 = 组件成员点 ↔ 链路块点）时，拖动 leader 每帧微抖：逐帧位移平滑但**帧末锚点欠收敛 ~0.2mm，停顿帧（重新 measure）才修正** = 视觉"拖完松手弹一下"。根因 = **外层收敛循环里 `settleComponents`（组件沉降）移动组件成员后，只调用 `Resolver::resolveAll` 重解线级森林，没有重新 measure**——基于组件成员点的测量值（M_xxx）保持旧值，引用该测量的曲线 Polar 端（及曲线锚点）用旧值欠收敛一帧；停顿帧 Phase 2 重新 measure → 修正 → 帧间差异 = 抖动。复现链（1.gcad 存档）：拖 leader → 链 → 省道块 d7f1acdc（dartRef=链路尾）→ 组件级连接 COMP→d7f1acdc → 组件成员 959c58ea → M_THKTW（= 959c58ea.e65de669 ↔ cd8d6d67.56dcf308）→ 1e3eecb9 的 Polar 端 → 曲线锚点。修复（ParamDocument.cpp 外层收敛循环 inner loop）：`settleComponents` 返回 true（组件动了）后、`Resolver::resolveAll` 前，**先跑 `measureLinkedVars() || measureMeasureVars() || measureAngleMeasureVars()`，任一变化则 `effAffected = nullptr` 升级全量**——组件沉降对测量的影响与 Phase 2/3 的"measure 变 → 全量"同规。验证：test_realdoc_full `curveFollowerDragStable`（GCAD_DOC=1.gcad，拖自由根 leader 20 帧，断言锚点帧间位移平滑 + 停顿多帧无漂移；修复前 pause drift 0.224，修复后 0）。经验：**凡"移动几何的相位"（组件沉降/附件 settle/省道）改变了测量源点，都必须先重测再重解**，否则消费者用旧 M_ 值欠收敛；测量缓存（skipAuxSource）只豁免"源全在 aux 层且不动"的测量，组件成员是 work 层源 → 必然要刷。
- **弧长/角度即时切换三坑（2026-08）**：①切换后必须 `refreshCard()+populateAngleField()` 全量刷新，只刷输入框 → caption/∠⌒按钮/世界角提示不更新（用户报告"切换到弧长没有任何文字变化"）；②切换前先把未应用输入 applyAngle() 落盘、非纯数值输入拒绝切换，否则公式信息被清成数值；③AngleHud/ConnectGesture/ToolRotate 三处模式换算必须与 Resolver 恒等映射一致（漏一处 = 静默反向）。
- **「跟随角度切换不了弧长模式」（2026-12 用户报告，三轮修复）**：第一轮排查 = **数值驱动切换全部正常**（属性卡 onModeToggle / 条带锁定态 onUnitToggled / 连接会话 onAngleModeChanged 三处 45°↔47.12mm 双向均通过），唯一静默死路 = 公式驱动（旧设计"公式不参与换算烘焙"拒绝切换且无反馈）。**第二轮用户拍板翻案：公式驱动不再拒绝切换**（"不是使用了变量就默认为弧长的"），并纠正"状态栏看不出模式"。**第三轮用户再拍板：公式切换时原样搬移、绝不乘换算系数**（"不会存在表达式在弧长和角度同时表达的情况；用户选择哪个模式，公式就按哪个模式求值"——切到弧长模式 `b/4` 就直接按弧长解释，不出现 `(b/4)×0.10472` 这类魔法系数）。最终语义：
  - ①**公式原样搬移**（FollowerAngle.h `followerModeSwitchValues` 返回 `FollowerModeSwitchResult{angle, arcMm, angleFormula, arcFormula}`）：切换只改 rotationMode + 把源公式字段原样赋给目标公式字段（followerAngleFormula ↔ arcLengthFormula 文本不变）；**数值字段仍做几何保持换算**（无公式时 45°↔47.12mm 一致；有公式时 Resolver 按公式求值、公式语义跟随当前模式）。三处入口（属性卡/条带/会话）共用，**条带旧内联换算只读原始存储值、公式驱动时拿到陈旧值——换共享入口后按公式求值**。
  - ②**状态栏模式显示**：根因 = **ElaPushButton::paintEvent 没有 isChecked 分支**（自绘不吃 QSS，选中态完全不渲染）→ °/⌒ 两钮点击后"状态完全一模一样"。修复 = 换原生 QPushButton + `chipButtonStyle`（`:checked` 实底可见）+ QButtonGroup 互斥；徽标追加模式字（"跟随 L2 · 弧长"）。
  - ③**输入锁定**：`ContextStrip` 的 `isBridge = block->isBridge || !seg->lengthFormula.isEmpty()` 把"长度用了变量"当成桥线 → 长度/角度输入框全锁死（用户拍板"错误设计，不需要锁定"）。修复 = 只认 `block->isBridge`（真桥线），applyLength/applyAngle 的守卫同步改 `block->isBridge`。
  - 回归：test_dialog_tabs::followerAngleModeToggleToArc（数值双向 + 公式原样搬移）、test_context_strip::pinnedUnitToggleFlipsFollowerMode（条带同款 + 徽标模式字）、bridgeLineReadOnly 改为真 isBridge 断言 + 对照"长度公式线可编辑"、test_rotate_copy::modeSwitchKeepsFormula 两场景断言公式原样 + 数值字段仍按几何换算。**坑：改"公式不烘焙"语义要连带改 AGENTS.md「连接角度会话」铁律条目与 test_rotate_copy 旧"拒绝"断言**。
- **省道线（2026-08 用户拍板）**：起点 A 挂已有点 → 终点 E = B 沿 B 所在线段方向转 β 角（默认 90）偏移 d；线方向/线长全自动（线是算出来的）。**不用 Attachment**（防角度驱动冲突；省道块无 incoming attachment）。坑：①**θ_B 必须是"B块.rotation + B 线段出口方向"（refWorld 同构）**，不能直接用世界 atan2(B 线段)——B 块自身有 rotation 时世界方向要加回块旋转；②B 的线段必须显式记 `dartRefSegmentId`（exitSegmentAtPoint 自动选第一段）——B 点扩展出多段后方向基准不能静默漂移；③**顺序**：Step 8 必须放 Step 7/7b 之后（A/B 块定位/aim 旋转完成）且自带 4-pass 有界迭代（B 可以是另一条省道线的终点 E）；④省道块终点 Polar distance 每帧写回 |A−E|——写回后必须同步 resolvedPos 并 ++geometryEpoch（否则画布缓存不重建、undo 前 dist 值错）；⑤**吸附到 E 的跟随线要同帧到位**：Step 8 末尾 must `settleAttachments(preserveEndTargetRotation=true)` 重沉降（否则像曲线锚点 follow 一样滞后一拍）；⑥删除善后：A 或 B 块被删 → removeBlock 清空全部 dart 字段降级普通线（几何冻结），deleteImpactReport 已加 `dartLinesDegraded` 计数（第 9 项）+ DeleteImpactConfirm 显示行——新删善后分支必须同步这两处；⑦依赖注册必加 `blockReferences`（dartStartBlockId/dartRefBlockId），漏了 = 拖拽时省道块不进 affected 集、不跟随 B 移动；⑧交互：智能笔 W 空闲档循环 直线↔省道线（画线中 W 仍是 leader 候选循环，勿抢）；第一击/第二击都必须现吸附点（自由起点/空白点击 toast 拒绝）；⑨面板省道态隐藏角度编辑行后，普通线分支恢复时须显式 show（m_editAngle/m_lblAngleCaption/m_lblFollowValue/m_lblFxAngle），漏恢复 = 切回普通线后角度行消失；输入聚焦中（hasFocus）不得被 refresh 覆盖文本；⑩d 输入文本：纯数字 = mm、公式 = cm 域（与 ParamPoint.distanceFormula 同约定，勿混用单位）；⑪**省道线弹窗必须非模态（2026-08 修复）**：原 `QDialog::exec()` 会锁住主窗口，用户无法切到变量/公式面板复制引用；改为 `show()` + `QPointer<QDialog> m_dartDialog`，弹窗打开期间画布点击/键盘事件一律忽略，OK 校验失败保持弹窗打开可修改/复制，Cancel/关闭不创建并保留 Drawing 状态以便另选 B。测试：test_commands `dartLine_computesEndAndFollows`（平移/旋转 B、移动 A、改 d 断言终点）/`dartLine_undoRedo`/`dartLine_degradeOnHostDelete`；test_serializer `blocksRoundTrip` 扩展省道字段 round-trip + isDart()。
- 约束类型分派点登记表 / 删除影响报告计数等规范见 AGENTS.md「开发规范」。
- **曲线算法四连修（2026-08-28，代码审查发现的存量问题，非用户报告；修复后 ctest 22/26 = 既有基线零回归）**：
  - **① 曲线名称/长度标签偏在曲线前段**：`Block::rebuildCurveCache` 的 `labelLocal = evalCurve(spans, 0.5)` 是**首段跨度的参数中点**，不是整条曲线的弧长中点——3+ 锚点的曲线标签明显靠前（CurveItem 注释还写着 arc-length midpoint，与实现不符）。修复：先算 `arcLengthMm` 再 `arcLengthToParam(spans, arcLengthMm/2)` 取中点（Block.cpp rebuildCurveCache）。test_commands `curveRenderCache_filledByResolve` 旧期望锁定的是错误行为，已按弧长中点语义更新（坑：该用例的 CurveAnchor 锚点 offset=0 → 曲线实为直线，弧长中点=(50,0)，勿按 freePos(50,20) 断）。
  - **② arcLengthToParam 注释声称 bisection+Newton 实为纯二分 40 次**（CurveMath.h 注释失真）：改 safeguarded Newton——括号内 Newton（导数 = 速度 |B′(t)|），出括号/速度为零回退二分（CurveMath.cpp）。回归 test_curve `arcLengthToParamPrecision`（µm 级，vs 2000 段折线积分参考）。
  - **③ buildBezierSpans(Hobby) 五处重复构建收口**：Block.cpp 交点/插值点/缓存 + ToolSelect 双击 + BreakCommands 各自现算，手工同步已漂移过一次（曲线打断 C2/Hobby 不同解，本文件 2026-10 条目）。修复：**`Block::spansForSegment(seg, skipUnresolvedPassPoints, tolerateStaleEndpoints)` 统一入口 + 锚点指纹 memo**（`m_spanMemo`：锚点位置+切线+auto+tension 哈希）。**关键设计：memo 有效性键是指纹而非 geometryEpoch**——fixpoint 内点先动、epoch 后 bump（Block::resolve 在 fixpoint 之后才 bump），用 epoch 判断会在 resolve 中途供应陈旧 span。两个容忍标志对应两种既有 mid-fixpoint 行为，勿合并：交点 = pass 点跳过+端点吃缓存位姿；插值点 = pass 严格+端点吃缓存位姿（端点↔查询自环靠缓存位姿收敛，改严格 = 死锁，同 polarEndpointCycleSeed 条目教训）。收益：同一沉降 sweep 内 N 个曲线辅助点共享一次 Hobby 求解（原每点每 pass 一次）；rebuildCurveCache 顺带填充 memo，帧后工具/命令零求解命中。
  - **④ projectPointOnCurve 单起点 Newton 收敛到局部最近点**（深弯曲线 = 袖窿场景最近点跳变）：改 16 采样 + 局部极小多候选（含两端点）逐候选 Newton 取最优。回归 test_curve `projectHairpinGlobalMin`（深 U 曲线 vs 4000 采样暴力参考；**对称轴查询只断距离不断 t**——镜像双极小 t 不唯一，断了必 flaky）。
- **曲线 P3 两项增强（2026-12，CURVE_P3_DESIGN.md，ctest 23/27 = 基线零回归）**：
  - **① P3-1 Alt+拖拽切线手柄 = 持久断开切线锁定（尖角模式，D1-D5 按默认建议）**：原唯一断锁入口是属性面板复选框（SegmentAnchorTab，直写模型无 undo）。现画布手柄拖拽支持 Alt——`ToolCurveEdit::beginHandleDrag(int, Qt::KeyboardModifiers)` 在**拖拽开始时**快照 `m_handleOldLocked` 并置 `pt->tangentLocked=false`（D3：中途 Alt 变化忽略）；`dragHandleTo` 不改——断锁后两个 `if (pt->tangentLocked)` 同步分支自然跳过 = 对侧手柄保持断锁前值。**配套铁律：undo 快照必须含锁定标志**——`SetCurveTangentCommand` 增加 `oldLocked/newLocked`（构造函数/redo/undo 三处，redo 写 `tangentLocked = m_newLocked` 必须放在 `++geometryEpoch` 前），否则 undo 无法恢复锁定态。`cancelHandleDrag` 追加恢复 `pt->tangentLocked = m_handleOldLocked`。视觉（D4）：断锁点手柄改橙色 `0xE68A00`（updateHandleGraphics 由 `tangentLocked==false` 驱动，无新 item）。端点镜像手柄（D5）与非端点一致不特殊处理。测试 tests/test_curve_edit.cpp（新 target，QTEST_MAIN GUI 事件流）`altDragBreaksLock`/`plainDragKeepsLock`/`altDragUndoRestoresLock`。
  - **② P3-2 Hobby 求解极端转角过冲限幅（D6-D9 按默认建议）**：`hobbyVelocity` 分子 `(cosθ−cosφ)` 在相邻弦接近 ±180° 折返（锚点 interpPercent 外推）时放大、分母趋小 → 手柄长度可达弦长数倍 → 曲线打圈/自交。修复：`CurveMath.cpp` 匿名命名空间新增 `kMaxHandleRatio=2.0`（注释注明可调、只拦失控形态；正常服装曲线比 0.3~0.7，领口实测最坏 ~1.4），`solveHobbyTangents` 的 **AUTO 分支**在 velocity 输出后 `std::min(c, chordLen*kMaxHandleRatio)` 限幅（手动切线不动——用户显式意图）；不改 `hobbyVelocity` 本身、不改方向系统。测试 test_curve：`hobbyFoldbackHandlesClamped`（折返点位 `{(0,0),(100,0),(50,2),(150,0)}`，限幅前 chord2 实测 3.505x/5.752x → 限幅后 ≤2x，断言用**切线向量** `3·|ctrl−P|` 而非原始控制点偏移——后者 5.75/3≈1.92x < 2 永不触发、防假阳性；`hobbyNormalShapeUnclamped`（领口点位最坏 1.4015x < 2x = 限幅 no-op，逐位不变）。
- **曲线打断不保持形状 + 断点连接丢失/跳变（2026-10 修复，用户报告「曲线打断不能维持形状」「端点和组件连接打断位置跳变」）**：
  - **① 形状断裂两半成因**：`BreakSegmentCommand`（src/document/commands/BreakCommands.cpp）原实现用 **C2 解冻结切线**，但画布曲线缓存（`Block::rebuildCurveCache`，Block.cpp:579）用 **Hobby 解**（`AutoCurveMode::Hobby`）——冻结值与实际曲线不同解 → 两半画出来就不是原曲线；且断点落在跨度内部时（Interpolated 打断）冻结的是**全量导数**，未按子跨度参数化缩放（t0 / 1−t0）——子跨度控制点应 = 3(L.ctrl1−P)/3(R.ctrl1−B)（de Casteljau）。修复：`gatherBreakGeometry` 改用 `solveHobbyTangents`（与缓存同解、**每点 in/out 分开存**）；`subdivideBezier(spans[spanIdx], t0)` 后按 `3(B−L.ctrl2)` / `3(R.ctrl1−B)` / `3(L.ctrl1−P_j)` / `3(P_{j+1}−R.ctrl2)` 生成 t0 缩放切线并写 `subTanIn/OutOverride`（曲线点打断 = 链点，无子跨度，原切线即精确）。**实测：修复前 broken→orig 偏差 58mm，修复后 0.03mm（=0.1mm 展平精度）**。
  - **①b 惰性缓存陈旧（更隐蔽的成因）**：断点改链点/切线/端点后 `Block::resolve` 的增量几何检测（prevPos 比较）发现**点没动 → 不 bump geometryEpoch → 惰性曲线缓存（2026-09 引入）继续用打断前 span**——前段画出旧曲线、后段按切线对准错位。修复：`modifyFrontBlock` 曲线分支末尾**显式 `++block->geometryEpoch`**（铁律见 AGENTS.md「画布缓存刷新」——凡曲线结构变了点没动必须手动 bump）。这就是「打断后形状完全不对但 d1=0」的诡异来源：旧曲线还画着。
  - **② 断点/原端点附件被删**：redo 快照 `m_removedAttachments`（target = 断点 or 原端点）后**无条件删除且不重建** → 端点/组件连接丢失。修复：`finalizeBreak` 改为 remove 后按规则重建——**目标 = 断点 → 保留**（断点仍是前段端点，同 id 同段；参考方向从 interior 弦向变为端点切线/段向，按 `refDeltaRad` 补偿跟随角，角度模式+followerAngle+弧度模式分别处理，公式角度跳过——世界朝向零跳变，实测 deltaDeg=0.0000）；**目标 = 原端点且端点被删 → 重指后段终点**（st.bpEndId，原线尾部，跟随不丢）；原端点幸存 → 原样保留。undo：删除存活副本（compensated/re-pointed）后 `addAttachmentsRaw` 原样恢复。坑：**组件级附件必须走 `removeAttachments`（批量）而非逐个 `removeAttachment`**——批处理不清 exposedPointId，单删会清暴露端点（旧代码经单删路径已有此隐患）；平局即附件保留，`addAttachmentsRaw` 会经 `addAttachmentRaw` 重新记录暴露端点（ParamDocument.cpp:1971）。
  - **③ 曲线辅助点按 interpPercent 分派前后段是错的**：pass 点（CurveAnchor）是**弦向分数**、打断点（Interpolated）是**弧长分数**，两者不可比——改按 `projectPointOnCurve().s` 弧长位置分派（`gatherBreakGeometry` + `redistributeAuxPoints` 曲线分支）。
  - 回归测试：test_break 新增 `curveBreakKeepsShape`（中段 Interpolated 打断）/`curveBreakNearStartKeepsShape`（前段无 pass 点 = 中点插入路径）/`curveBreakAtAnchorKeepsShape`（链点 CurveAnchor 打断，偏差 0.0000）/`breakFollowerAtBreakpoint`（断点端点连接保留+零跳变+undo/redo）/`breakComponentAtBreakpoint`（组件连接）/`breakFollowerAtOriginalEnd`（原端点重指后段尾部）/`curveBreakFollowerKeepsDirection`（角度补偿零旋转），共 7 个新用例，全部通过；ctest 22/24（2 红 = 既有基线，见第 5 组）。
- **Polar 端点锚在本线段辅助点 → 自环 → 端点不解析 + 跟随线角度跳变（2026-08 诊断，3.gcad「L68 以 P176 断开角度跳变」；同日引擎级修复）**：P176（线段端点，Polar 15mm/−45°）的 `refPointId` 为 P175（**同一条线段上的交点辅助点**）→ 闭环：端点位置由交点定义，交点又是"该线段与射线"的交点。**热启动能收敛**（设计引导 = 缓存位姿：注释 "its cached position is used now and later iterations converge once the endpoint resolves"，Resolver Step 6/6c）；**冷加载（无缓存位姿，端点位姿为零）→ 线段退化 → 交点永不解析 → 端点死锁**，跟随线（followerAngle 180 挂 P176）拿不到领袖段出口方向 → 停在存档旧旋转 = 角度跳变观感。**修复（Resolver.cpp Step 6 + Block.cpp 本地交点分支）**：线段退化且端点无缓存位姿时，用 `Block::polarEndpointCycleSeed` 把端点极角公式**临时锚段起点**做一轮引导，交点算出 → 不动点随后收敛到真锚（仅零长度缓存才引导，热路径零开销；温/热状态保持原引导行为）。验证：`TestBreak::polarEndpointAnchorCycleResolves`（冷启动收敛到理论不动点 (7.5,0)/(22.5,0)）。**教训**：①"从交点沿段 N mm"语义用**插值辅助点**（Interpolated ref=交点）最稳——1.gcad 的 D1 即此；段端点（Polar）引用点原则上勿锚同段辅助点（引擎现已可解，但语义上插值点更直接）。②排"角度跳变"先查 toPoint 的 resolved 标志；③改 Resolver 引导逻辑时**保留"缓存位姿优先"**——Step 6 的设计注释不可违反（曾因无条件种子覆盖好缓存值打挂测试）。
- **工作侧交点的射线起点在辅助层 → 变量修改后交点偏离 1.3mm 不跟随（2026-08-24 修复，E:\3.gcad 复现）**：即"跨图层交点不更新"的镜像方向。Phase 2.5 只覆盖"辅助层交点引用工作层点"；反向"**工作层交点引用辅助层点**"（交点挂在工作块上、射线起点 = 辅助层点）时：Phase 2 解交点用的是**当时的辅助层位姿**，而辅助层若经跨层连接跟随工作链，其位姿要等 **Phase 3（跨层沉降）**才最终落定 — 沉降移动了辅助层射线起点之后，工作侧交点永远不会重解 → 交点停在旧位置漂离射线（实测偏移 1.30556mm；复现 = 3.gcad 在工作块 ba39e737 段 efcc253c 上建交点、起点取辅助块 3fe15b56 的 Free 点（经跨层连接跟随工作链），改任一影响链的变量即偏离）。**修复（Phase 4 工作侧跨层交点重解，ParamDocument.cpp）**：新增结构标志 `m_workingIntersectToAux` + `scanWorkingIntersectionAuxRefs()`（与 Phase 2.5 的标志同点计算、同缓存规则：仅全量 resolve 重扫），Phase 3 之后若为 true 且有工作层求解过 → 有界 4 轮 [工作层重解(runWorkingPhase) → 全量 measure，变了则辅助层重沉降，下一轮再用新辅助位姿解工作侧交点]。回归：test_intersection_update`workingSideIntersectionWithAuxOrigin`（3.gcad 现成结构 + 合成工作侧交点，基线 + 全变量扫描全部 on-ray）。**姊妹方向**（辅助层交点引用工作层点）已由 Phase 2.5 覆盖，回归 `crossLayerIntersectionFollowsVariable`。**经验**：跨层相交的"层向"有两个方向，Phase 2.5 只防了一个；凡"交点依赖的另一侧几何在沉降后才动"都必须有对应的后置重解相位（Phase 4 与 2.5 的"后置"目标相反）。
<!-- [2026-12-15 恢复标注] 原工作副本第 116 行内容缺失：当日会话日志未捕获，未能重建（约 200 字符，大概率空行或短条目）。若需旧组织版本条目请从 git HEAD（154 行旧版）核对。 -->
- **线条属性连接卡「输入框大小不一 / 跟随↔独立无缓存 / 同一模式内容不同」（2026-12 用户三点反馈）**：①**样式**——ElaLineEdit/ElaComboBox 原生固定 35px，但 ElaPushButton 默认 38、PointRefEdit（裸 QLineEdit 自绘 QSS）默认 ~24px，同排混摆即"大小不一"。修复：连接卡统一 `kFieldH=35`（ElaPushButton/PointRefEdit setFixedHeight(35)），行形式统一「固定宽 92 标签 → 150 参考输入 → 子标签 → 150 点输入/90 小输入 → 按钮」，每行首标签 `setFixedWidth(92)` 全行对齐；模式/滑轨下拉换成 ElaComboBox（主题一致 + 35px）；∠/⌒ 切换按钮移到行尾（30×35）。**坑：行布局里的按钮漏 `addWidget` 会停在 (0,0) 盖住标签**——清除按钮曾因此"消失"，用 `card->grab()` 截图探针才抓到（仅几何断言发现不了）。回归 test_dialog_tabs::connectCardUniformHeights（全行内控件 =35px、点引用 =150px）。②**缓存**——`onModeChanged` 切「独立线段」不再裸删：新增 `detachWithCache()` 把完整 Attachment 快照进 `m_modeCache`（host/角度/弧长/公式/角度基准/滑轨/角度独立/焊接）后 RemoveAttachmentCommand；切回「跟随」用 AddAttachmentCommand 原样恢复（redo 走 addAttachmentRaw verbatim，不走 addAttachment 的强制 isLocked=true）；宿主已删/校验失败 → 丢弃缓存。**显式断开（清除按钮 / 位置吸附 ✗）必须 `m_modeCache.reset()`**——否则"断开后再切独立↔跟随"会把旧连接复活；setTarget 换线重置。回归：test_select_wkey::connectionCardNewSemantics 第 7 步。③**同一「跟随」两种表单**——自由线选了「跟随」时标题仍写「独立线段 · 连接」、角度行标题是「独立角度(°)」（那是连接态"角度独立"的名字）、行集也不一样。修复：标题跟随模式（跟随 · 未连接 / 独立线段）；自由态角度行中性标签「角度(°)」+「= 世界角度」；「跟随」下连接/未连接共用同一行（连接线段+连接点，未连接时空栏 + 占位符，在「连接点」输入 P# 即建立连接——原「位置吸附」输入与「连接点」输入合并为一个 PointRefEdit，onConnPointResolved 按有无连接分派 重定向/建立）；独立线段模式隐藏全部连接行。**2026-12-12 用户拍板统一状态模型**（比 ③ 更彻底）：「跟随」严格 = 真的在跟，无连接时下拉恒「独立线段」、条目置灰（仅 ①已连接 ②有缓存可恢复 时可选，QStandardItemModel::item(0)->setEnabled）；自由视图统一为「位置吸附:」行（输入 P# 回车建立连接，连接行标签 per-state 切换 连接线段:/位置吸附:）；引用线段行自由态为**预填**（连入时 onConnectToResolved 自动落库，refresh 不再覆盖预填）。删除的旧代码：m_userMode、跟随·未连接 标题、freeAngleRefMode、onAngleRefPointResolved 的 combo==1/2 死分支、m_userMode==1 连接后置 angleIndependent（onConnectToResolved/onFollowHostToggled）。回归：connectionCardNewSemantics 增加 置灰/可选/位置吸附:标签 断言。
**2026-12-15 用户再拍板：删除「跟随/独立线段」模式下拉（卡片纯状态驱动）**——模式 = 状态投影而非用户选择（无连接线段/无引用线段 = 已经等于独立线段；refreshModeCombo 强制 currentIndex(att?0:1) + 「跟随」置灰只是“不可能状态”宣告），连接开关回归「位置吸附」复选框（重新显示，refreshConnectionToggles），m_modeCache 语义转为「断开记忆快照」：取消勾选/清除时快照完整 Attachment（原 detachWithCache 并入 onFollowHostToggled(false)/onClear），重新勾选优先 AddAttachmentCommand 原样恢复（宿主有效 + checkAttachment 通过），失效退回记忆宿主新建；「拆开」按钮移至连接行尾，仅连接态显示；删净 m_cmbMode/m_modeRow/refreshModeCombo/onModeChanged/detachWithCache。回归改写：connectionCardNewSemantics 步骤 4/5/7 走复选框路径（快照恢复 断言 followerAngle/isLocked/!angleOnly/方向零跳变 < 1e-9）。ctest 22/26（4 红 = 2 既有基线 + test_intersection_update/test_extend::savedDocFormulaStartExtendRenders 依赖 E:/3.gcad 活档内容，与本次改动无关）。当日用户再拍板细化（不做面板状态区分）：同一张卡、同样行集、标题恒定「连接」（refreshUnifiedState，原 refreshConnectedState/refreshFreeState 两视图合并删除），自由线 = 连接字段为空的同一表单——无「独立线段/跟随线段」两个状态，无连接时 清除/拆开/独立角度/滑轨 禁用仍显示；角度行行位恒定，caption 按语义切换。**同日面板整页重设计（取经 m3/HashiCorp Helios/NN-g/8pt 栅格）**：两列四行卡片栅格（Row1 基本信息|几何〔长度+角度行组 SegmentAngleCard 嵌入〕、Row2 连接线段|引用线段〔角度基准+指向点 SegmentRefCard〕、Row3 延长|外观、Row4 P#起点|P#终点〔卡标题=点 ID〕）；全部行内控件 35px 高统一 + 输入定宽（主 150/小 90/注入 70）+ 禁 addWidget(edit,1) 伸缩；连接卡拆分（旧 SegmentConnectionCardAngle.cpp/RefAim.cpp 删除，新 SegmentAngleCard/SegmentRefCard）；「位置吸附」终局改名「连接线段」（无状态区分后该行恒为连接语义，记录在案不再回退）；延长行标签=起点延长(P12)/终点延长(P13)（2026-12 用户拍板，见下条）；起/终点卡去掉「起点/终点」标题。回归：test_select_wkey connectionCardNewSemantics（改名断言「连接线段」）、test_dialog_tabs connectCardUniformHeights、test_rotate_copy propertyDialogShowsFollowValue。ctest 22/26（4 红 = 既有基线 + E:/3.gcad 依赖）。**同日二轮版式修整（视觉截图回归）**：对话框默认宽 620→1060 + 水平滚动兜底（QScrollArea horizontal AsNeeded，防裁切）；连接/引用卡内行拆两行（连接线段|连接点、引用线段|引用点），标签列 92 统一、输入 140/130、按钮 58×35；点卡标题/编号改纯 P#（去掉 Serial::toHtml 红色层码前缀）。**坑：SegmentAngleCard（普通 QWidget）嵌入 ElaScrollPageArea 卡片（几何卡）时出现"卡中卡"圆角白框**——以 setStyleSheet("SegmentAngleCard{background:transparent;border:none;}") 消除（同类嵌入行组可复用此招）。视觉验证 = 临时截图用例 test_dialog_tabs::panelShotGrab（整页 grab 至 panel_shot.png 供人工检查，验证后已移除）。**v3 Apple 窄栏实验（用户 2026-12-16 拍板：完全没法用 → 已回退 v2 两列卡片定稿）**：曾实施 500pt 单列 + 扁平分组 + 三卡改 QWidget 基类 + TabSize(120,36)，用户弃用后逐项回退（三卡基类回 ElaScrollPageArea、500pt/扁平分组/TabSize 作废、滑轨标签回 水平/垂直 自然宽）。**坑（v3 期间发现，保留记录）**：①grab() 的 PNG 尺寸 = 窗口逻辑尺寸 × devicePixelRatio（本机 1.25，625px 截图实为 500pt 窗口）——判断窗口大小务必除以 dpr，勿据此误判"面板太宽/太高"；②行内子标签（如 偏移(°):/水平(cm):）在窄列会按词折行——需要时 setFixedWidth 定宽防换行。
- **连接卡三度简化（2026-xx 用户拍板，修「基准点清除无效 / 独立角度无效」）**：「基准点」旁「清除」实际无效的根因 = ①默认连接（angleRefBlockId 空）时按钮禁用（SegmentRefCard::refreshAngleRefRow 启用条件 !angleRefBlockId.isNull()），②自定义基准被清除后自动回填位置宿主、角度仍跟随——从用户语义看「清除 ≠ 角度不再跟随」；「独立角度」复选框与基准点字段本是同一状态（有连接线·无基准线）的两个重复入口，且受仅角度/滑轨互斥限制、感知弱。修复：删「连接线段」「独立角度」「连接保护」复选框与断开记忆快照（onFollowHostToggled/onLockToggled/onAngleIndependentToggled/m_modeCache 全删，SegmentConnectionCard 系列 + LinePropertyDialog.cpp）；「拆开↔重连」双面按钮（onDetachClicked → SetAttachmentAngleOnlyCommand(!angleOnly)，重连 = 位置回原宿主+重新焊接）；基准点清除改 SetAttachmentAngleIndependentCommand(true)（位置保持吸附、角度不再跟随），恢复 = 基准线/基准点输入 L#/P#（SetAttachmentAngleRefCommand 自动清独立角；独立角态跳过 belongs 校验，防陈旧 angleRefSegmentId 拦截恢复路径）；分区顺序 基本→几何→连接→端点→外观；连接分区标题 hint 追加 ·独立角/·仅角度 后缀。回归：test_select_wkey::connectionCardNewSemantics 全量改写（拆开/重连双面 + 连接点清除/P# 重连 + 基准点清除独立角 + 恢复跟随）。
- **连接卡两维独立（2026-xx 用户再拍板：拆开/清除重叠 → 双 拆开/重连 按钮）**：连接点「拆开」与「清除」功能重叠（都是断开位置关系），删除「清除」；基准点「清除」改为角度维度的「拆开/重连」双面按钮——连接 = 两个正交维度：连接点按钮管位置（拆开 = angleOnly、重连 = SetAttachmentAngleOnlyCommand(false) 位置回宿主+重焊），基准点按钮管角度（拆开 = angleIndependent、重连 = SetAttachmentAngleIndependentCommand(false) 反算回原基准/位置宿主）。**模型解耦（关键）**：①Resolver.cpp applyAttachment 的 angleOnly 分支原为 if (att.angleOnly && !att.angleIndependent) —— 双拆开时位置仍被钉死（角度独立分支把位置约束留在下方代码）→ 改为 if (att.angleOnly) 无条件放行位置（独立分支已保持自身旋转，写同值并 return）；②SetAttachmentAngleIndependentCommand redo 与 ParamDocument::setAttachmentAngleIndependent 不再清 angleOnly（旧互斥设计），setAttachmentAngleOnly 不再清 angleIndependent；③滑轨仍与两者互斥（setAttachmentSlideMode 进滑轨清两者）。旧档双标志附件读出的行为随之改变（由位置钉死 → 位置自由），符合新语义。四态矩阵：全连接 / 独立角（位置跟·角度拆）/ 仅角度（位置拆·角度跟）/ 自由（双拆开，基准线旋转本线不动）。连接分区 hint 与 ContextStrip 徽标补 自由/仅角度 子态。回归：test_select_wkey::connectionCardNewSemantics 再次全量改写（两维独立断言 + 双拆开自由线不跟随基准旋转 + 逐步重连零跳变 + P# 建连）。
- **角度基准两点化「点2 输入框完全无效」（2026-08-31 用户报告：「以点1到点2的方位为角度基准，点2的输入框并不能使用」）**：自动跟随态（默认态，angleRefBlockId 为空）填点2回车后输入直接消失、角度纹丝不动。根因链 = ①`SegmentRefCard::onAngleRefPoint2Resolved` 把 att 当前的 angleRefBlockId（自动态 = null）原样当点1传给六参命令，实际只落了 ref2 字段；②Resolver 两点方向分支入口门控在 `!att.angleRefBlockId.isNull()`（Resolver.cpp applyAttachment，ref2 在门内才被读）→ ref2 被引擎整体忽略；③refresh() 走自动态分支 `m_angleRefPoint2->clearPoint()` → 刚输入的文字被清掉。次级缺口：自由线只预填点2时连接落库只读点1（五参重载 ref2 默认参数 null）静默丢弃；点2 漏设 `setExcludeBlock(m_blockId)`（本线自身点入选后又被 fromBlockId 守卫静默拒绝）；settle 依赖图漏 `addDep(angleRef2BlockId)`（点2 宿主块后结算时两点方向读陈旧 transform）；全仓零测试覆盖。修复：自动态填点2 = 明确的自定义意图，把所连点固化为显式点1（携带出口线段）与点2 六参同写（`SetAttachmentAngleRefCommand`，反算零跳变）；`ParamDocument::setAttachmentAngleRef` 扩六参（ref2 感知 + 两点反算，与 Resolver 两点分支同解；ref2 空参时清字段，与命令行为对齐）；自由线预填点1+点2 连接落库六参同写（只填点2时点1取所连点，同自动态语义）；补 setExcludeBlock、点2==有效点1 拒绝（零向量方向）、addDep(angleRef2BlockId)；补回归 test_commands::angleRefTwoPointBasis_engineAndUndo（doc API 两点方向 + 平移点2宿主跟随转向 + 命令 undo/redo 全还原 ref2）+ test_select_wkey::angleRefPoint2TwoPointBasis（自动态填点2固化/同点拒绝/引擎两点跟随/undo 回自动态/自由线预填落库）。教训：给「门控字段 + 可选扩展字段」型模型加 UI 写入路径时，扩展字段单独落库而门控字段为空 = 静默失效——写入路径必须成对验证（测试里断言引擎真的消费了写入值，而非只断言字段写进去了）。
- **线条属性面板一轮细调（2026-12 用户 5 点截图批注，均已修复）**：①**线段 ID 显示完整编号**——`m_lblSegId` 原用 `Serial::tag`（仅 "L1"）且贴右缘（LinePropertyDialog.cpp:672）；改为完整 serial（随机前缀+类型序号，如 wjqs4L1）并移到「类型」下拉框右侧与输入列对齐（:349-354；窗口标题仍用 tag/名称，未动）。②**角度输入框打开时不显示数值**——`SegmentAngleCard::setTarget` 未调 `populateAngleField`（头注释声称"由 setTarget 内部调用"，实为幽灵契约），自由线打开面板时角度框只有占位符「数值(°)或公式」而读数「= 世界角度 xxx°」有值（用户批注"显示输入数值"）；修复 = setTarget 里 `populateAngleField(); refresh();`（SegmentAngleCard.cpp:84-90）。③**粗细 spin 数值域完全看不见**——`ElaDoubleSpinBox::Inline` 模式两个步进按钮并排占 2×高（70px），`setFixedWidth(76)` 后编辑域仅 ~12px（只剩两个箭头，用户批注"输入框完全看不到了"）；放宽到 110（编辑域 ~46px，LinePropertyDialog.cpp:524-527）。④**延长行标签**——「起 P1/终 P2」简写改为「起点延长(P1)/终点延长(P2)」（补全"延长"文字、去掉简写"起/终"）。⑤**延长读数补全为等式**——「原长 32.76 + 延长 0 + 0 = 实际 32.76」（原「原长 ｜ 延长 ｜ 实际」三栏无运算符号；SegmentExtendCard.cpp:204-209）。验证：test_dialog_tabs 全绿（含 connectCardUniformHeights/probeTabHitArea）+ test_rotate_copy::propertyDialogShowsFollowValue + test_select_wkey::connectionCardNewSemantics 通过；panelShotGrab 临时截图探针用后已移除。
- **线条属性面板二轮细调（2026-12 用户 3 点截图批注 + 参数问答）**：①完整 ID 的关键部分标红——`m_lblSegId` 改用 `Serial::toHtml`（灰前缀 #9a9a9a + 红 #d40000 加粗类型序号 L#，与 SerialDelegate/QuickAuxDialog 同一约定；ElaText 富文本直接可用）。②「发布长度参数」按钮从几何分区标题右侧**移到类型下拉框正下方**（左缘 x=kLabelW+6=70、150×35 与下拉框同宽对齐；用户拍板"放到轮廓下拉框下面对齐好看"，几何分区标题回归纯标题）。③**悬停提示纯黑看不清的根因（2026-12 判因为"漏了 @tooltipFg"，2026-08 复查发现真因更深——见下条）**：Theme::buildStylesheet 的 QToolTip 规则替换列表漏了 @tooltipFg → `color: @tooltipFg` 非法失效，文字取暗色调色板 → 黑底黑字；补 `s.replace("@tooltipFg", ...)`（Theme.cpp）并去掉 QToolTip 的 Consolas 字族（中文提示更清晰）。QToolTip 颜色约定：亮色 = 深底浅字（tooltipBg #0D1117 / tooltipFg #F8FAFC），暗色 = 浅底深字。**发布长度参数语义（用户问"干嘛用/怎么引用/发布到哪/参数 ID"）**：`LinkedVariable::fromSegment` → refName = "L"+完整 serial（如 L2t50wL2）、name = 线段名+"长"；发布后进入文档关联参数表（变量面板「关联」页 LinkedCard 可见、随档保存），值 = 源线段实际长度（cm 域、只读、随每次 resolve 自动刷新），任意公式输入框（长度/角度/延长量/跟随角）直接写该引用名即可引用并联动。
- **全局样式表从未被安装——悬停提示纯黑的真因（2026-08 用户再报"线条属性悬停提示全黑"，上条 2026-12 的 @tooltipFg 修复非完整解）**：`Theme::apply` 末段装全局 QSS 的代码写的是 `if (auto* app = qobject_cast<QWidget*>(QApplication::instance())) app->setStyleSheet(...)` —— **QApplication 继承 QGuiApplication 而非 QWidget，这个 cast 恒返回 nullptr**，`setStyleSheet` 从未执行 → buildStylesheet 生成的全部 app 级规则（QToolTip / QListWidget / dimText / serialBadge / editBand 等）全部未生效。进程内探针实证：修前 `app.styleSheet().size()==0`（QTipLabel 调色板回落原生 #ffffe1/#000000；在半透明 ElaDialog 上原生 tooltip 渲染成纯黑无字）。**修法 = `qobject_cast<QApplication*>(QApplication::instance())`（QApplication 自带 `setStyleSheet` 成员，qapplication.h，非 QWidget 专属）**。修后探针：appSS length 3982、QToolTip 规则在位。诊断手法值得复用：`GCAD_TIPPROBE=1` 环境门控 + `QToolTip::showText` 强制弹提示 + 遍历 `topLevelWidgets()` 找 `QTipLabel` dump 其 palette/ownStyleSheet（widget 级样式表会显示为 `"/* */"`）——纯 Qt 独立复现程序**无法**复现本坑（最小案例全部正常），必须进真进程查 `qApp->styleSheet()`。教训：**应用级 setStyleSheet 一直静默失败却没人发现，是因为各 widget 自身的实例级 setStyleSheet 兜住了大部分视觉；只有 QToolTip 这类"只存在于 app 级规则"的选择器坏相外显**。回归：test_dialog_tabs 12/12、test_select_wkey 24/24（serializer 1 红为既有基线）。
  - **后续（同日，用户拍板终版）**：全局 tail 苏醒后 `QWidget { font-size:13px }` 兜底规则把 ContextStrip 无实例样式控件的字号放大（用户「状态栏好丑」）；先试行"收缩到只剩 QToolTip"，用户再拍板改为：**恢复完整全局 tail（程序其余部分走全局设计样式），仅 ContextStrip 条带豁免**。豁免手法 = 条带内部 objectName 改 **strip 前缀**（editBand→stripBand、serialBadge→stripSerial、mutedText→stripField、dimText→stripNote —— 全局 ID/对象名规则不再命中）+ 无实例样式的 ElaText 标签补 `setStyleSheet("font-size:11px")` 实例钉死（widget 级样式优先于应用级，压过 QWidget 兜底字号）。**新増全局规则时禁止使用 strip 前缀以外的这些旧名**；test_context_strip 的 serialBadge 查找已同步改 stripSerial（只断言文本）。editBand 的黄底自始至终没显示过（MainWindow 只设 objectName+WA_StyledBackground，无实例样式）。回归：test_context_strip 27/27、test_dialog_tabs 12/12；test_select_wkey 整批偶发 1 红（拖拽手势时序抖动，AGENTS 已登记特征）单跑连续 24/24。
- **变量面板与控件文字悬停提示纯黑底黑字看不清（2026-09 用户报告「变量面板的有一部分页面的悬浮标签是纯黑色，连文字也是黑色看不清楚。以及部分文字悬停的标签也是纯黑色，这个是不是主题的影响？」）**：用户在测试时发现变量面板的序号、代号名称胶囊、来源说明以及组件页操作按钮悬停时，部分标签仍然呈现纯黑底甚至黑字。**真因链**：①历史遗留墨面反转规范与原生渲染分流：先前的重构已将工具栏和上下文条升级为带显式 CSS容器的 TooltipFormatter 富文本，但变量面板（VariableCard/LinkedCard/MeasureCard/AngleMeasureCard）、组件页（ComponentTab）、复制胶囊（CopyChip/CompoundChip）以及图层面板行内控件仍大量使用 `setToolTip("纯文本")`；②原生 `QTipLabel` 在 Windows DWM / 半透明 ElaDialog 复合体系下的黑底合成 bug：当 `QToolTip` QSS 中包含 `border-radius: 4px` 时，Qt 会自动为 `QTipLabel` 开启 `Qt::WA_TranslucentBackground`，而在无 HTML 容器保护的纯文本绘制上下文中，部分宿主层级未能正确绘制不透明背景，导致回退透出底层默认黑底；叠加浅色模式下 QPalette 的深碳墨文字色（#1A202C / #0D1117），形成致命的“黑底黑字完全隐形”；③暗色模式下的 `ThemeTokens::dark()` 中遗漏了 `tooltipBorder`（未初始化导致 QColor().name() 默认返回 `#000000` 纯黑边框）。**修复收口**：①全量消灭悬停裸文本：将 `CardBase`（序号/只读锁/删除/来源信息）、`CopyChip`（名称代号提示）、`CompoundChip`（代号/名称双击编辑提示）、`FormulaCard`（实际覆盖/条件分支）、`FormulaGroupHeader`（分组重命名/解散）、`VariablePanel`（添加/分组按钮）、`ComponentTab`（回正/断开/解散/角度输入）、`MainWindow`（页面标签切换）、`LayerPanel`（线段行序号/操作/折叠/计数）等所有遗漏点全面接入 `TooltipFormatter` 结构化纸黄色技术卡片；②底座样式加固：在 `Theme.cpp` 的 `buildStylesheet` 中为 `QToolTip` 显式写入 `background: @tooltipBg` 与 `QLabel#qtooltip_label` 规则，并在 `ThemeTokens::dark()` 中补全 `tooltipBorder`（#6B5E38），在 `QApplication::setPalette` 中向 Active/Inactive/Disabled 全组同步固化 `ToolTipBase` 与 `ToolTipText`。回归：全量 ctest 33/33 绿灯通过，3 组守卫 check_layering/check_hardcoded_colors/check_test_fixtures 0 警告。
- **线条属性端点连接摘要（跟随/挂载/指向）上下截断（2026-09 用户报告「线条属性的线段挂载信息会被上下截断」，已彻底修复）**：
  - **根因分析**：`LinePropertyDialog::buildEndpoint`（LinePropertyDialog.cpp:832）中端点连接摘要标签 `connSummary`（`m_lblStartConn` / `m_lblEndConn`）被硬编码了 `connSummary->setFixedHeight(16)`。然而其样式 `dimMono`（来自 `Theme::dimValueStyle()`）指定了 `font-size: 11px`，在 Windows 环境下中文字体（如微软雅黑）由于行间距与基线度量，11px 字号的实际行高通常在 18~22px（若开启 125% 或 150% 等高 DPI 缩放则更大）。由于固定高度仅 16px 且 QLabel 默认垂直居中对齐，导致字符顶部笔画（如汉字顶端）与底部笔画（如汉字撇捺勾或小写字母下延）被视口上下边界直接裁切（上下截断）。此外，使用 `v->addWidget(connSummary, 0, Qt::AlignLeft)` 限制了宽度展开，导致多项挂载时可能触发不合理的折行。
  - **修复**：①移除 `connSummary->setFixedHeight(16)`，将构造字号同步为 11px，让标签依据字体度量与 DPI 自动计算高度（`sizeHint()`）；②将 `v->addWidget(connSummary, 0, Qt::AlignLeft)` 调整为 `v->addWidget(connSummary)`，使其占满面板可用宽度，多条挂载自然横排或折行；③在 `test_dialog_tabs` 的 `endConnectionRowAndBadge` 测试中追加宿主线打开属性对话框验证 `ldrEndConn` 显示「挂载」摘要且 `height() >= fontMetrics().height()`，杜绝截断回归。
- **端点延长量从 0 变到非 0 画布渲染未更新（2026-09 修复，用户报告「有时候触发延长量，画布线条正确移动但形状没变」）**：
  - **根因分析**：在 `Block::applyEffectivePositions`（Block.cpp:1167）中，用于检测可视几何变动并触发重绘的逻辑写成了 `if (!m_effectiveLocal.isEmpty()) { ... if (moved) touchGeometry(); }`。当一条线段初始无延长量（`m_effectiveLocal` 为空，如初建线段、新读档或延长公式变量由 0 改为非 0）时，该分支直接为假，漏掉了 `touchGeometry()` 调用！导致被延长的线段所在 Block 的 `geometryEpoch` 未自增，画布 `BlockItem::syncFromBlock` 判定“几何未改变”而跳过了 `updateFromBlock()`（旧渲染缓存未重建，形状没变）；而挂在该端点上的跟随线（Follower）因为在求解时通过 `worldPos` 读到了新的有效端点坐标，导致跟随线所在图元在画布上正确发生了位移，视觉呈现为“连线走了，本体形状没变”。
  - **修复**：完善 `applyEffectivePositions` 的几何变化检测：当 `m_effectiveLocal.isEmpty()` 时，只要本帧 `eff` 中存在任意端点偏离本体 `resolvedPos`（有有效延长外移），即判定 `moved = true` 并显式调用 `touchGeometry()`；与非空时的增量逐点比对互为完整闭环。
  - 回归测试：`tests/test_extend.cpp` 新增 `variableChangeFromZeroToExtendRefreshesCanvas`（测试变量驱动延长量从 0 到 50mm 时 `geometryEpoch` 自增且画布 `BlockItem` 包围盒增宽 ~50mm，100% 通过）。
- **滑轨面板流三坑（2026-12 用户反馈"滑轨功能好像是坏的，没有生效的痕迹"）**：①**只填一轴被静默拒绝**——`SegmentConnectionCard::onSlideOffsetEdited` 原要求两轴都能解析（`if (!okA || !okP) { refreshCard(); return; }`），只填「水平」留空「垂直」（tooltip 承诺"留空/0 表示不偏移"）时直接 return + refreshCard 清空输入 → 输入被悄悄丢弃、无任何生效痕迹；修复 = 空串按 0 解析（SegmentConnectionCardConn.cpp:474-481；留空/0 + 另一轴非 0 自动选 AlongLeader/PerpLeader，双轴 0 或空 = 退回全连接）。②**任意 ElaLineEdit 按回车会悄悄关闭整个属性对话框**——ElaLineEdit 未覆盖 keyPressEvent，回车事件沿父链传播到 QDialog 的 default-button/accept 路径 → 对话框瞬间关闭（输入内容看似被丢弃）；修复 = `LinePropertyDialog::keyPressEvent` 吞掉 Return/Enter（提交语义由 editingFinished 承担；Esc 照旧走 reject()/撤销全部，LinePropertyDialog.cpp）。③**滑轨输入回车后关闭对话框触发 Qt 6.11 析构断言**——`emit changed(SlideModeChanged)` 的信号发放本身（空槽也复现，几经二分定位）令对话框析构期 `QtPrivate::assertObjectType` 断言（"class destructor may have already run"，qobjectdefs_impl.h:107，QTest qFatal）；修复 = 滑轨路径不再 emit changed（画布刷新经 `m_doc->resolveAll()` → `ParamDocument::resolved` → CanvasScene 观察者链自动完成；onConnCardChanged 对 SlideModeChanged 本就 default no-op）。验证：test_dialog_tabs 9/9 + test_rotate_copy::propertyDialogShowsFollowValue + test_select_wkey::connectionCardNewSemantics + test_commands::slideMode_alongAndPerpConstraints/slideMode_dragOffsetsUndoRedo 全绿；probeSlideFlow 临时探针（面板滑轨流 = 只填水平 2 → AlongLeader/along=20mm/字段保留/对话框不关/析构无断言）用后已移除。提示：滑轨"沿线"方向 = 基准线在吸附点的出口方向（起点锚点 = end→start、终点锚点 = start→end，与折叠角参考系一致）。
- **独立角度（angleIndependent）三处编辑失效（2026-12 用户报告「勾选独立角度后，角度就无法输入调整了，选择工具拖动改变角度也无效。输入角度会不断跳动」）**：独立角 = 位置仍焊在宿主、角度自管（Resolver.cpp applyAttachment 对 angleIndependent 保留 from.transform.rotation、**忽略 followerAngle**），因此**所有角度编辑路径都不得把它当普通跟随线写 followerAngle**。三处修复：
  ①**条带（ContextStrip）**——applyAngle/refreshFields 原按 findEditAttachment() 命中即写/读 followerAngle（静默无效 / 显示陈旧值）。修复 = att && !att->angleIndependent 才走跟随线分支，独立角落入自由线分支（写端点 Polar 角 ep->angle = targetDeg − rotDeg、读世界角 ep->angle + rotDeg），并 st.attId = QUuid() 不碰附件；refreshChrome 的 °/⌒ 单位切换与「跟随」徽标对独立角禁用/改写（弧长模式对独立角无意义）。
  ②**属性面板（SegmentAngleCard）**——applyAngle 末尾原调 populateAngleField()：写入 ep->angle 后几何**尚未重解**（resolveAll 由对话框 refreshScene 延后），按旧 resolvedPos 读回世界角 → 覆盖用户刚输入的值 → 「不断跳动」。修复 = 去掉该调用，保留用户输入，世界角读数由 onDocResolved（resolved 信号）在重解后刷新（与 2026-08「= 绝对角度不实时刷新」条目的"不调 populateAngleField"同规）。
  ③**旋转工具（ToolRotate）**——rebuildAnchorState 原对锚点附件一律进 Connected 模式写 followerAngle（Resolver 忽略 → 拖动无效）。修复 = att->angleIndependent 走自由线旋转路径（写块 transform.rotation，Resolver 保留它且拖帧内把 from-point 重新吸回宿主点 = 位置焊点不动）；**独立角附件绝不进「旋转=放弃跟随」的释放快照**（if (!fa->angleIndependent) 守卫），否则拖一下位置焊点就没了。
  回归：test_context_strip independentAngleEditApplies（条带输入 45 → 世界角 45 / 附件保留 / 块旋转不动）、test_rotate_copy independentAngleLineRotatesBlockKeepsPin（旋转 −90° / 附件保留 / 位置焊点 300,0 不动 / 一步 undo 回 0°）、test_dialog_tabs independentAngleInputDoesNotJump（面板输入 45 不回跳 / 世界角 45 / 附件保留）。ctest 31 例 = 2 既有基线红（test_serializer::bridgeAuxPointSnappableAndAttachable + test_component::dragComponentLeaderCurveFollowStable）。
<!-- [2026-12-15 恢复标注] 原工作副本第 118 行之后约 1.4KB 内容缺失：当日会话日志未捕获，未能重建；git HEAD 为旧组织结构（154 行），需要旧条目可用 git cat-file HEAD:TROUBLESHOOTING.md 取回。 -->
- **线段换向三个 UI 联动 bug（2026-08 用户报告「换向会把点的名称覆盖，且世界角度显示消失」）**：①**端点名称被覆盖（数据损坏级）**——`LinePropertyDialog::populateFromModel` 逐字段 setText 无 QSignalBlocker，端点名称/备注输入框的 textChanged 均接 `onLiveUpdate → applyToModel`（把输入框内容写回**当前角色**端点）；换向后端点角色互换，回填起点卡 → 回波把"终点输入框里的旧起点名"写进新终点 → 回填终点卡时读到已污染值 → 原名永久丢失。修复 = populateFromModel 全字段 QSignalBlocker（名称/类型/长度/显示/端点 13 个控件）。**教训：任何"角色互换"的模型变更后做表单回填，必须整段静音信号——回波写入用的是旧角色映射**。回归 test_commands `reverseSegment_namesSurviveUndoRoundTrip`。②**世界角度标签消失**——`SegmentAngleCard::onDocResolved` 的自由线分支直接 `setVisible(false)`（原语义"非 follower 无绝对角读数"），但自由线的世界角是纯几何读数；换向命令 push 内含 resolveAll → resolved 信号到达 → 标签被藏。修复 = 自由线/独立角分支重算 start→end 世界方向刷新标签（同值短路）。③**延长卡滞留旧值（潜在误编辑）**——换向把 extendStart↔extendEnd 随端点角色互换，但 SegmentExtendCard 不监听 resolved，两输入框仍显示旧角色数值，用户拿错基线编辑。修复 = 延长卡 connect resolved 刷新（hasFocus 跳过，refresh 内已有 QSignalBlocker）。排查确认无缺口：SegmentEditBar（创建态工具条，showForLine 显式驱动）、SegmentAnchorTab（已监听 resolved 且直线禁用）、角度卡输入框（换向后 populateAngleField 重填，编辑即接管语义不变）。回归：test_commands 13 个 reverse 用例全绿；ctest 22/26（4 红 = 既有基线）。
- **换向/旋转锚心反馈缺失（2026-12 用户报告「上下文属性条换向部分一直显示 p1、p2，和点名称完全无关，点击切换也不调转；旋转工具选旋转端点，条带没有任何变化；应该点击换向画布就切到另一头」）**：三个独立问题一起收口——
  ①**条带基准读数硬编码 P1/P2 兜底**（ContextStrip.cpp refreshChrome，旧 `sp->name.isEmpty()? "P1" : sp->name`）：普通画线端点**无名**（LineFactory 不设 pt.name，只有辅助点/交点表单命名）→ 兜底恒命中 → 按钮永远显示「P1 → P2」、与端点真实串号无关、换向后 start/end 互换但兜底不变 → 读数纹丝不动。修复 = 显示真实 `Serial::tag(serial)`（与属性对话框 `SegmentAngleCard::refreshBasisRow` 同口径），换向后随端点身份互换翻转；按钮宽 96→110（三位数串号防裁切）。
  ②**换向几何零跳变 = 画布无可见变化**（设计如此，非 bug）：ReverseSegmentCommand 物理换身、点标签按身份绑定不移动、画布无方向指示 → 用户感知"点了没反应"。修复 = 画布加**方向指示箭头**（新头文件 `src/canvas/DirectionMarker.h` `drawDirectionChevron`：中点旁垂向偏移 5px、两臂 ±25° 回开 4px 的 chevron；直线 = atan2(p2−p1)、曲线 = CurveItem::labelAngle 弧长中点切线，均 scene 坐标 Y 向下；灰显层不画、ghost 降 alpha）。换向后 start/end 互换 → BlockItem rebuildCache 重算 p1/p2、曲线 spans 反向 + 切线镜像 → 箭头自动翻转。
  ③**旋转会话内条带换向 ≠ 切锚心**（用户拍板：旋转工具的换向意思是换选择的锚点）：原条带换向在旋转会话内也 push ReverseSegmentCommand（持久换驱动端），旋转工具自己的锚心（m_anchorIsEnd，X 键/点端点切）是工具临时态，条带无感知。修复 = 双通道（仿连接角度会话四通道）：工具→条带 = `ToolRotate::reportRotateAnchorState` → `ToolHost::setRotateAnchorState(active, anchorIsEnd, canToggle, reason)` → ToolManager emit `rotateAnchorStateChanged` → MainWindow → `ContextStrip::setRotateAnchorState`（基准读数锚心端在前 + 换向按钮资格 = Ready 且两端无连接，禁用原因 tooltip）；条带→工具 = `ContextStrip::reverseRequested(blockId, segId)`（仅旋转会话内发）→ `ToolManager::forwardReverseRequest` → `Tool::onReverseRequested`（默认 no-op）→ ToolRotate override = `toggleAnchor()`（gizmo pivot 环移动，自带全部守卫，**不 push 命令**）；普通（非旋转）会话换向仍由条带直接 push ReverseSegmentCommand。上报点 = selectTarget/toggleAnchor/点端点切换/clearTarget/onDeactivate 五处；hideBar 清会话标志（工具切换两路幂等）。坑：条带 basis/tooltip 措辞改「起点/终点」「锚心」；showStrokePreview 占位改「起点 → 终点」。回归：test_context_strip +2（`basisShowsSerialTagsAndReverseFlips` / `rotateAnchorStateFlipsBasisAndRoutesReverse`，前者断言串号 tag + 换向点击翻转 + undo 复原，后者断言锚心端在前 + reverseRequested 转发不 push + 会话结束恢复普通换向可 push）、test_rotate_copy +1（`stripReverseTogglesAnchor`：自由线换向 = 锚心 start↔end 翻转、连接线换向 = no-op 且挂接不释放）。
- **换向/锚心第二轮（2026-12 用户复核报告「旋转工具换向逻辑不对：换向不改变环形显示；点端点角度不随基准变；缺确定提示」）**：第一轮落地后真实链路上还有三个洞，逐一修复——
  ①**`selectTarget` 上报时序 bug（换向走错路径，环形/gizmo 不动的根因）**：`reportRotateAnchorState()` 原放在 `m_state = RotateState::Ready` **之前**调用，而它的会话激活判定是 `m_state != Idle` → 首次选中上报 `active=false` → 条带 m_rotateSession 恒 false → 点「换向」走 `ReverseSegmentCommand`（几何零跳变、gizmo 纹丝不动）而非切锚心。修复 = 上报移到 m_state=Ready 之后（ToolRotate.cpp selectTarget）。**同批修 onDeactivate 时序**：`reportRotateAnchorState()` 原在 m_blockId/m_state 清空**之前**调用 → 上报 active=true 的陈旧会话 → 条带残留旋转会话标志（换向按钮错走切锚心语义、基准读数错位）。修复 = 状态清空后再上报。
  ②**条带角度字段不随锚心基准（点端点/换向切锚心后角度不 ±180°）**：条带角度字段（自由线）= `ep->angle + rotDeg`（模型 P1→P2 世界角），而旋转工具的显示语义（currentAngleDeg）= 锚心基准角（锚=终点时 +180°，v3 定稿「线总是指向光标」）——锚心切换后字段不翻。修复 = 旋转会话内显示带锚心偏移（ContextStrip.cpp refreshFields 自由线分支 `+180 if m_rotateSession && m_rotateAnchorIsEnd`），**输入回写同步减回偏移**（applyAngle 自由线分支 `st.endAngle = (targetDeg − anchorOffset) − rotDeg`，否则用户按条带读数输入线会偏 180°）；`setRotateAnchorState` 追加 refreshFields（锚心切换也要刷角度字段，原只刷 chrome）。
  ③**缺确定提示**：锚心切换（换向/点端点）后状态栏提示不刷新，pivot 环移动是唯一反馈。修复 = `updateStatusHint` 的 Ready 分支附带锚心串号 tag（「旋转：锚心 P2 · 右键或回车确认后拖动」/ 确定态「拖动旋转」），toggleAnchor + mousePress 点端点切换路径补 `updateStatusHint()` 调用。
  回归：test_rotate_copy `anchorSwitchSyncsStrip`（新，走真实接线：StripBridge 补 wire rotateAnchorStateChanged → strip + strip.reverseRequested → tm.forwardReverseRequest；断言 选中即进旋转会话（基准锚心端在前 + 角度 0° + 换向可点）→ 点条带换向锚切终点（基准翻转 + 角度 180° + 提示带锚心 + 不 push 命令）→ 点起点端点锚切回（条带全跟随））；test_context_strip `rotateAnchorStateFlipsBasisAndRoutesReverse` 补角度字段断言（锚=终点 180° / 锚=起点 0°）。**坑：`selectTarget`/`onDeactivate` 这类「先清状态再上报」的顺序坑——凡上报内容依赖自身状态字段，上报必须放在状态翻转之后，测试要用真实接线（StripBridge 全量 wire）而非直驱工具/直调条带**。
- **undo 双栈分裂 + 命令 ID 撞号 + 绕过 undo 的直写路径（2026-12 P0 修复，AGENTS.md「架构决策记录」 三 P0 全落地）**：
  - **①双栈收口单栈（跨文档污染根除）**：`ParamDocument`（ParamDocument.cpp:26）与 `MainWindow`（MainWindow.cpp:97）各建一只 QUndoStack——CanvasView/ToolSelect/cards 往**文档栈**推命令而菜单 Ctrl+Z、isClean 脏标记只绑 **MainWindow 栈**：文档栈命令撤不掉、不参与脏标记；且 `ParamDocument::clear()` 不清栈 → 打开/新建文档后旧档命令快照在新文档 undo 复活。修复：MainWindow 删除自建栈、`m_undoStack = m_paramDoc->undoStack()`（非拥有别名，析构 disconnect 顺序不变）；`ParamDocument::clear()` 末尾 `m_undoStack->clear(); m_undoStack->setClean();`（ParamDocumentLifecycle.cpp:51-57，清栈在 emit 之上防信号竞态）。**教训：undo 栈生命周期必须绑定模型，UI 只做绑定不做拥有；clear() 不清 undo = 跨文档时间旅行**。
  - **②mergeWith 命令 ID 撞号 = UB**：`RotateBlockCommand::id()=1003`（BlockCommands.h）与 `SetVariableValueCommand::id()=1003`（VariableCommands.h）撞号——QUndoStack push 时按 id 相同判定调 `top->mergeWith(other)`，static_cast 成异类读字段 = 未定义行为。修复：新建集中枚举 `src/document/commands/CommandIds.h`（`cad::cmd::CommandId`：MoveBlock=1001/SetFollowerAngle=1002/RotateBlock=1003/**SetVariableValue=1004**——1003 撞号者重编号），四处 `id()` 改 `static_cast<int>(CommandId::X)`；四个 mergeWith（MoveBlock/RotateBlock/SetFollowerAngle/SetVariableValue）的 `static_cast` 改 `dynamic_cast` + 判空返 false。**教训：QUndoStack 的 merge 判定只看 id，命令 id 必须全局唯一（集中枚举单一定义点），mergeWith 永远 dynamic_cast 兜底**。
  - **③绕过 undo 的模型直写路径收口**：SegmentAnchorTab.cpp 四 lambda 直写 `pt->tangentIn/Out/autoTangent/tangentLocked` + `++geometryEpoch` + `resolveAll()`（切线编辑完全不可撤销）+「释放跟随」按钮直写 follow 三字段；LinePropertyDialog `applyToModel` live-apply 直写 seg 字段（确认后 Ctrl+Z 撤不掉）；SegmentExtendCard/LayerPanel/CanvasView 存在 "undoStack 为 null 时静默降级直改" 双写 else 分支。修复：①SegmentAnchorTab 全部改走 `SetCurveTangentCommand`（统一 `pushTangent` lambda：读模型旧值→push→redo 写新值，no-op 短路防污染撤销链；resolved→refreshFields 自动同步 UI）+"释放跟随"改走新建 `ReleaseCurveFollowCommand`；②LinePropertyDialog `onAccepted` 把「打开快照→确认状态」差异推成一步 `SetLinePropertiesCommand`（新建，oldProps=newProps 才 push；快照字段 = Seg 属性+端点字段，与取消时恢复同一份 m_snapshot）；③p0-1 后文档栈恒非空 → CanvasView/SegmentExtendCard/LayerPanel 删 else 直改分支统一走 `m_doc->undoStack()->push`（LayerPanel 的注入 m_undoStack 成员随注入点删除）。**教训：模型写路径三选一——命令 / *Raw 静默恢复 / 编译期封死可变引用，禁止第四条"else 直改"**；继承 QUndoCommand 的对话框如果 live-apply，必须在确认时把会话推成一步命令（同 SegmentEditBarCommand 契约），否则 undo 栈 clean 标记不感知修改（保存后关闭悄悄丢）。回归：ctest 23/27（4 红 = 既有基线，无新增回归；test_commands/test_rotate_copy/test_dialog_tabs/test_select_wkey 等 undo 敏感用例全绿）。

- **求解收敛「魔法 4 轮」收口 + 未收敛可观测化（2026-12 P1-4，AGENTS.md「架构决策记录」 P1 首项；ctest 23/27 = 基线零回归）**：
  - **问题**：有界不动点循环的预算 `4` 硬编码散落 **10 处**（Resolver.cpp 交点不动点 / Step 7 aim / Step 8 dart；ParamDocumentResolver.cpp Phase 1 aux、Phase 2 working、Phase 2.5、Phase 3、Phase 4、组件跟随内外层），且除 Phase 3 外**耗尽预算一律静默**——最后一轮还在动的"半收敛"姿态被当成正常结果交付（诊断徽章不亮、日志无痕）。
  - **修复**：①`Resolver.h` 新增 `constexpr int kMaxSettleRounds = 4`（单一定义点，注释禁止在循环里重打字面量）；②**10 处**全部改引用（组件跟随外层 `kMaxFollowSettleRounds = kMaxSettleRounds`）；③每个循环加收敛标志（`converged`/`aimConverged`/`dartConverged`/`auxSettled`/`workSettled`/`compConverged`/`followConverged`），**耗尽即报 `ResolveDiagnostic::Kind::NotConverged`**；④`runIntersectionFixpoint` 加 `bool* budgetExhausted` 出参（Step 6d / 7b 两个调用点据此上报）；⑤ParamDocument 侧新增匿名命名空间 `reportNotConverged(diags)`（同 pass 内去重——否则 Phase 2.5/3/4 各报一条把徽章计数灌水），Phase 3 原有的裸 push 一并改走 helper。
  - **语义约定（勿反）**：所有循环都是「本轮无进展即 break」，所以**预算耗尽 ≡ 最后一轮仍有进展 ≡ 没到不动点**；"跑满 4 轮"不是强度证明，是未收敛信号。
  - **测试**：test_resolver `aimRingExhaustingBudgetReportsNotConverged`（两块互指 endTarget + 非对称偏移 30°/20°：几何级数收敛但第 4 轮仍差 ~0.15°/轮 → 预算耗尽 → 恰好 1 条 NotConverged）+ 对照 `oneWayAimReportsNoDiagnostics`（单向指向一轮到位、零诊断）。**教训：验证"未收敛上报"必须配一条健康对照，否则分不清真信号与误报**。
  - **坑**：①诊断被 test_rotate_copy（21 处）/ test_aux_layer（3 处）的 `QVERIFY(doc.diagnostics().empty())` 直接断言——新增任何上报点必须先跑这两个 target；另须用真实活档验证误报（临时探针跑 e:/1.gcad、e:/2.gcad、e:/3.gcad 全量 resolve，NotConverged 全 0；1.gcad 的 1 条 Dangling 是既有问题、与本次无关），探针用完即删；②**盘点同类循环时 grep 模式要放宽**——首轮用 `< 4; ++` 只抓到 7 处，漏掉 Phase 1/2 写成 `for (int i = 0; i < 4 && (measure...); ++i)` 的两处（条件式而非 break 式），收口类改动务必按语义再扫一遍（`i < 4`、`round < 4`、`< 4 &&`）。

- **表达式编译缓存实例化（2026-12 P1-5，AGENTS.md「架构决策记录」 P1 第 5 项；ctest 23/27 = 基线零回归）**：
  - **问题**：`ExpressionEvaluator::compiled()` 的缓存是函数级 `static QHash + static deque`（ExpressionEvaluator.cpp 旧 67-88 行）：①进程级共享可变状态，未来多线程求解必然踩数据竞争；②满 8192 条时 `index.clear(); store.clear();` **整体清空**——此前发出的所有 `const Compiled&` 瞬间悬空，只能靠头文件注释"callers must re-fetch"兜底（真实 UB 隐患）。
  - **修复**：新增 `ExpressionCache` 类（`ExpressionEvaluator.h`），把缓存变成显式对象。①`ParamDocument` 持有 `m_exprCache`；`Resolver::resolveAll` 新增末位参数 `ExpressionCache* exprCache`（默认 nullptr、不破坏既有 45 处调用），内部 `ctx.cache = exprCache` → `ConditionEngine::evaluate` 经唯一决策点 `cacheFor(ctx)` 取用；ParamDocumentResolver 11 处 Phase 调用点全部注入 `&m_exprCache`；`ParamDocument::clear()` 同步 `m_exprCache.clear()`（字节码随文档释放）。②无文档上下文的调用方（UI 校验 / 一次性命令求值 / 测试）走 `ExpressionEvaluator::defaultCache()`，改为 **thread_local**——不再是共享可变全局。③`BreakCommands` 打断求值也设 `ctx.cache = &doc.expressionCache()`。
  - **引用稳定性（关键设计，勿退化）**：存储为 `std::deque<Generation>` 分代；活代满 `kMaxLiveEntries=8192` 就 `emplace_back()` 开新一代，旧代**只退休不释放**（不再被检索，但内存保留）→ 已发出的引用永不悬空。可用是因为 `std::deque::push_back` 不移动既有元素。**"分代退休"替代"整体 clear"是这次的核心：既保留有界活集（防 live-typing 每键一次编译撑爆单代），又不牺牲引用稳定性。**
  - **测试**：test_expression 32/32（新增 `cacheIsInstanceScoped` 两缓存互相独立且 clear 互不影响 / `cacheReferencesSurviveGrowthGuard` 越过代后旧引用仍可读且算对（8/4+2=4）/ `documentOwnsCompileCache` 求解写入文档缓存、`clear()` 归零）。
  - **坑**：①`ExpressionCache::compiled` 内必须写 `ExpressionEvaluator::Compiled`（非成员类没有注入作用域，`Compiled` 裸写 = C2065）；②`ExpressionEvaluator` 的构造函数与 `compile()` 是 private，需 `friend class ExpressionCache;`；③CMake AUTOMOC 偶发 `Cannot open SRC:/build/out/<target>_autogen/deps for writing`（瞬时错误，原样重跑 `tools\build.bat` 即过，勿去改 CMake）；④**test_expression 目标未链接 gcad_document**，需要 `DocumentFile::load` 的临时探针只能放进 test_intersection_update（它链接了）。

- **P1：架构评估后续六项（2026-12 全量收口，AGENTS.md「架构决策记录」；每改一项跑一次全量，ctest 始终 23/27 = 同一组 4 红）**：
  - **① geometryEpoch 收口为 `Block::touchGeometry()`（P1-1）**：`Block::geometryEpoch` 由裸公有字段改私有 `m_geometryEpoch`，对外只留 `geometryEpoch()`（只读）+ `touchGeometry()`（唯一 bump 入口）；**30 处** `++geometryEpoch` 改走入口（外部 26 处跨 engine/commands/tools/tests 四层 + Block 内部 4 处，内部也统一走入口，`m_geometryEpoch` 只在 `touchGeometry()` 体内被写）。**收益核心：刷新点可 grep**（`grep -rn touchGeometry src/`），漏 bump 从"靠记忆"变"可审计"。批量改法（可复用）：**只改写表达式尾部**——`++<pre>{.|->}geometryEpoch;` → `<pre>{.|->}touchGeometry();`、`<pre>{.|->}geometryEpoch` → `<pre>{.|->}geometryEpoch()`，这样 `const_cast<Block*>(x)->`、`doc.findBlock(id)->` 等复杂前缀无需解析；改完 Block.cpp 内裸成员名用 `re.sub(r'(?<![A-Za-z0-9_.\->])geometryEpoch\b(?!\s*\()', 'm_geometryEpoch', ln)`。坑：注释行要先跳过，否则把注释里的 `geometryEpoch` 也改了（无害但脏）；**脚本动源码前先 `shutil.copyfile(p, p+'.bak')`**（这些文件带未提交改动，`git checkout` 不是保险）。
  - **② `*Raw` 静默恢复通道收进 `RawModelAccess`（P1-2）**：ParamDocument 的 14 个 `*Raw` API 全部**私有化**，唯一 friend = `cad::param::RawModelAccess`（新头 `src/parametric/ParamDocumentRaw.h`，14 个静态转发定义在 ParamDocumentStores.cpp）。调用从 `m_doc->addAttachmentsRaw(x)` 改为 `cad::param::RawModelAccess::addAttachmentsRaw(*m_doc, x)`，**不 include 该头就编译不过**，信任在调用点可见可 grep。35 处调用点：**DocumentSerializer 12 / commands 17 / ToolRotate 5 / CopyDragController 1 / VariableStore 1** —— 注意调用方比"仅 serializer/commands"多两层，**tools 层的拖拽取消快照还原**（verbatim 恢复拖拽前现场）是真实需求，通道必须对它开放。**切记 CRLF**：CMakeLists.txt / 部分头文件是 CRLF，python 替换 `\n` 模式会静默失配（本次踩到：CMake 源列表没改成、`assert` 才暴露）。
  - **③ 可变裸指针的短期护栏（P1-3）**：`findBlock()/blockById()` 返回的是 vector 内部指针，**结构性变更即失效**。新增 `bumpStructureEpoch()`（addBlock/removeBlock/addBlockRaw/clear 四处）+ 可观察 `structureEpoch()` + debug 版 `blockPointerInRange(p)`（校验是否仍落在当前 `m_blocks` 存储区；release 编译为 `return true`）。约定写进访问器注释与 AGENTS.md：**一次一取，禁止跨结构性变更持有**。勘察结论：ToolRotate/BreakCommands 等热点其实已经"变更后重新 findBlock"，所以**真句柄化（id+generation）属未做的中期项**，别在评审里说成已完成。测试 `structureEpochTracksBlockMutations`（add/remove/clear 递增；resolve 与纯属性编辑**不**递增）+ `blockPointerMayLeaveStorageAfterGrowth`（断言写成 `!inRange(p) || p == doc.findBlock(id)`，规避"新缓冲区恰好落在同一地址"的 flaky）。
  - **④ 分层设防（P1-6）**：三件事。a) **app 层逻辑上移**：CanvasView 只做命中测试 → `emit segmentContextMenuRequested(cad::canvas::SegmentHit{blockId, segId, scenePos, paramT, globalPos})`，菜单/QuickAuxDialog/三个命令/切工具+选中+toast 全搬进 `MainWindow::onSegmentContextMenu`；旋转工具"右键=确认反悔"的抑制改为 app 安装的 `ContextMenuGuard` 谓词（canvas 不再 `dynamic_cast<ToolRotate*>`）。b) **依赖倒置切断 canvas→tools**：新增 `canvas/InputDispatcher.h`（6 个 dispatch 纯虚函数），`ToolManager : public QObject, public cad::canvas::InputDispatcher`，`setToolManager(ToolManager*)` → `setInputDispatcher(InputDispatcher*)`，**64 处调用点靠隐式上转型零改动**（这是能安全重命名 64 处的关键）。c) **静态检查替代 include 收窄**：新增 `tools/check_layering.py`（扫 `src/<module>/**` 的 include，凡是更上层模块就报错 exit 1；canvas/document 同级互访放行、tests 不检查）。**为什么不做"每库 include dir 收窄"**：全仓用 `#include "parametric/X.h"` 这种 src 根相对写法，收窄 include dir 必然大面积打断构建，收益/风险不划算，用 lint 把关即可。脚本首跑报出唯一违规 `document/DeleteImpactConfirm.cpp → ui/ElaMsgBox.h`：**该对话框本来就编在 `gcad_ui`**（CMake 注释写明），用 `git mv` 把它迁到 `src/ui/` 让目录与库归属对齐（同步改 ToolSelect/LayerPanel 的 include 路径与 CMake 源列表）→ 现在全仓干净。

- **P2 开工：测试去活档化 + undo 上限 + GUI 条件等待（2026-12；ctest 基线 23/27 → 25/27）**：
  - **① 测试不许依赖仓库外文档（P2-2，用户拍板："3.gcad 是正常的存档文件不是测试文档"）**。历史债：test_intersection_update 5 个用例加载 `e:/3.gcad`（用户一改就红：交点偏移 29.17mm / 缺变量 后长补正）、test_extend 1 例、test_serializer 1 例加载 `e:/存档/1.gcad`、test_p612_colinearity 加载 `build/out/Debug/1.gcad`（未随仓库提交 → **常年 QSKIP 空跑**）。**没有把活档复制进 `tests/fixtures/`**（那只是把漂移搬进仓库、还要把用户纸样入库），而是**用模型 API 合成等价拓扑**：
    - `test_intersection_update.cpp` 新增 `CrossLayerDoc` + `buildCrossLayerDoc(fx, originOnAux)`（~40 行）：工作层块（anchor + Polar 端点，距离公式 `L`）+ 目标段块（承载 Intersection 与 15mm 插值辅助点）+ 一条**跨层连接**（aux follower → working leader，挂在 leader 的 ANCHOR 上，这样改 L 只动射线起点、不动目标段，Phase 2.5/3 才有活干）；`originOnAux=true` 是镜像拓扑（目标在工作层、起点在辅助层 → Phase 4）。5 个用例全部改挂合成档 → **10/10**。
    - `test_extend.cpp`：删除 `realDocExtensionMovesEffectiveEnd`（行为已被同文件的合成用例覆盖）；`savedDocFormulaStartExtendRenders` 改写为 `formulaStartExtendEvaluatesAndMovesStart`（变量 + `extendStartFormula` → 求值 50mm / 有效起点外移 / 包围盒变宽）→ **17/17**。
    - `test_p612_colinearity.cpp` 整文件重写：借用点交点（`interAimPointId`）的起点/借用点都由参数 `H` 驱动，三个 H 值验证共线性 + "交点确实在动"（防空过）→ **3/3**（此前是空跑）。
    - `test_serializer.cpp` 删除 `diagL54ConnectionOffset`（写死 4 个 UUID 的用户档诊断）。
    - `test_nav_smoke.cpp` 的调试截图从硬编码 `e:/garment-cad/build/*.png` 改到 `QDir::temp()/gcad_nav_smoke/`。
    - **守卫脚本 `tools/check_test_fixtures.py`**：扫 tests/ 的绝对路径 / build 产物 / 家目录引用，发现即 exit 1；一次性探针可加 `// fixture-allow: 原因`；`test_realdoc_*`（GCAD_DOC 手动跑）豁免。当前干净。
  - **② undo 上限（P2-5）**：`ParamDocument::kUndoStackLimit = 150` + 构造时 `setUndoLimit()`。命令带全量模型快照（删除命令还带级联子图），无上限 = 长会话无界内存。测试 `test_commands::undoStackLimitIsBounded`（超限后 count 封顶、undo/redo 仍可用）→ test_commands 47/47。
  - **③ GUI 固定等待（P2-3，部分）**：`tests/TestHelpers.h` 新增 `cad::test::waitUntil(pred, 5000)`（轮询事件循环至条件成立）与 `grabStable(widget)`（抓帧等到连续两帧一致，替代 `qWait(30); grab()` 那种"半帧/空白"竞态）；已改造 11 处最高风险点（10 处 `qWait → waitUntil(findChild)` + hold_show 抓帧）。**全仓还有 ~239 处 `QTest::qWait`**，需逐处识别条件后迁移。
  - **抖动实测结论（重要，别再误判回归）**：整批 ctest 下 GUI 测试的红集合**每次都不同**（本次 aux_layer+curve_edit+rotate_copy，下次 select_wkey+hold_show，再下次全绿），**单跑全部 100% 通过**。确定性基线现在只有 2 个红：test_serializer::bridgeAuxPointSnappableAndAttachable、test_component::dragComponentLeaderCurveFollowStable。看到别的红 → 先单跑复现再说。
  - **踩坑**：①`QVERIFY/QVERIFY2` 不能用在**非 void** 的辅助函数里（宏失败时 `return;`，MSVC 报 C2561/C2280）——把断言挪到调用方，或改成返回 `bool` 让调用方断言（本次 `buildCrossLayerDoc` / `renderNonWhitePixels` 都踩到）；②**测试文件是 CRLF**，python 读字节后 decode 不会做换行转换，`'a\nb'` 模式匹配不上——用 `chr(13)+chr(10)` 或先 `.replace('\r\n','\n')`；③ Qt 文本模式下 `open(encoding='utf-8-sig')` 会做通用换行翻译，与二进制读到的内容不一致，排查时别混用两种读法。


- **角度基准消费方与 Resolver 语义漂移（2026-09 审核 F0-F6）**：`effectiveAngleRefWorld` 抽取前，三处消费方（SegmentAngleCard 绝对角度读数 / ConnectGesture::reattachAngleOnly 重挂反算 / ToolRotate Gizmo 基准可视化）与两条角度命令（SetAttachmentAngleRef / SetAttachmentAngleIndependent 的 redo 反算）各自实现 refWorld，且都漏了 ①自定义角度基准（angleRefBlockId 优先、点1→点2 连线方向最优先）②影子偏转 baselineOffsetDeg 叠加 → 设了自定义基准/影子后读数错、重挂跳线、Gizmo 画错基准。修复：共享函数 `cad::param::effectiveAngleRefWorld(doc, att)`（声明 FollowerAngle.h，实现 ParamDocumentAttachments.cpp，与 Resolver::applyAttachment 的 refWorld 逐位同构）——**坑：FollowerAngle.h 是纯 header 且被 parametric 各 TU 广泛 include，函数若内联在头里需要 ParamDocument 完整类型 → 编译报 C2027 未定义类型**，必须声明/实现分离（实现放已 include FollowerAngle.h 的 ParamDocumentAttachments.cpp，无需动 CMake）。另收口：SegmentRefCard::refresh 隐藏判定（Dart+hasEnd）从 populateFromModel 补写收进卡内（此前 onRefCardChanged/onConnCardChanged 路径可把省道线句式行重新置真）；lengthAuto 进 SetLinePropertiesCommand::Props + 对话框快照（此前对话框内切换直写模型，撤销全部/Ctrl+Z 都回不滚）；滑轨两轴偏移编辑改推新命令 SetAttachmentSlideOffsetsCommand（此前直改附件不进 undo）。回归：test_serializer::attachmentsRoundTrip 补 angleRef2 往返断言（影子用例已随功能删除）。ctest 31 例 2 红 = 既有基线（bridgeAuxPointSnappableAndAttachable / dragComponentLeaderCurveFollowStable），无新增。

- **角度基准点2「无法填入」/ 静默拒绝假象（2026-09，E:.gcad L2 报告：「对齐点的方向只有基准点1输入框有内容，点2没有也无法填入，但角度正常生效」）**：存档附件 `angleRef*` 全空 + `angleIndependent=false` = 自动态（角度基准 = 位置宿主 L1 在吸附点 P2 的出口方向）——点1 灰显回显所连点、点2 留空是**设计如此**（点2 可选：两点连线方向），角度正常是因为 Resolver 自动态直接用宿主出方向驱动、根本不读点1/点2。点2 填不进的真因（首轮修复未覆盖, 用户实测仍复现后二挖, 2026-09） = **`PointRefEdit::focusOutEvent` 失焦一律 `revertDisplay()`**（PointRefEdit.cpp:269-275, 原注释"只有显式 Enter 才提交"的防误触设计）——真实用户操作是"点进输入框→输入→点走/Tab"，**根本没按回车**, 输入在失焦瞬间被无声清空, 连 `onAngleRefPoint2Resolved` 都到不了, 合法内容与非法内容无一幸免 → 点2"永远填不进"而点1（自动灰显回显, 不走该控件提交）恒有内容, 与本次修复的拒绝路径 toast 无关。修复: `focusOutEvent` 改为 **合法单解自动提交**（输入非空且 ≠ 显示值 && findMatches 恰 1 解 → applyMatch, 与 Enter 同语义）; 多解**不弹选择框**（失焦事件里弹模态框有焦点死锁风险, 还原让用户重来）; 未改动/非法输入照旧还原。**坑: 该控件是共享控件（连接卡「连接到」+ 角度基准点1/点2 + 端点连接全用它）**——改 focusOut 语义影响所有调用点, 回归跑 test_select_wkey/test_dialog_tabs/test_commands 全绿。回归补: test_select_wkey::angleRefPoint2TwoPointBasis 第 6 步（setText 后发 QFocusEvent(FocusOut) 不按回车 → 断言自动态点1 固化 + 点2 落库）。次要修复（前置批, 保留）: `SegmentRefCard::rejectRefInput(reason)` toast 明示拒绝理由 + 锚点 tag 红闪, 替代拒绝分支的静默 refresh()。点2 填不进的真因 = **拒绝路径静默刷回**：`SegmentRefCard::onAngleRefPoint2Resolved` 两条拒绝分支（①blockId==att->fromBlockId 本线自身点 ②点2==有效点1 零向量方向）拒绝后直接 `refresh()` 把输入清掉，零反馈；且**「本线自身点」实际在输入层就被 `PointRefEdit::setExcludeBlock` 挡下**（findMatches 排除本块 → flashError 红框 900ms 后 revertDisplay），用户真正撞上的是 ②（点2 填 L1·P2 或自动态有效点1）——闪一下就消失且无任何解释。修复（两批）：①主修 `PointRefEdit::focusOutEvent` 失焦合法单解自动提交（见上）；②`SegmentRefCard::rejectRefInput(reason)` —— **toast 明示拒绝理由**（画布底部，`m_scene` 由构造签名 unused 启用）+ 对齐锚点 tag 红闪 900ms（对话框内 toast 未必醒目），替代两条分支的静默 `refresh()`；`CanvasScene::showToast` 在 `views().isEmpty()` 时早退（无头单测不创建 toast），测试需挂 `CanvasView view(&scene)` + `show()`。坑：**HudItem 不是 QObject**（无 Q_OBJECT），`scene.findChild<HudItem*>()` 编译报 `qt_metacall 不是 HudItem 的成员` → 用 `scene.items()` 遍历 + `dynamic_cast` 定位；findChild 对非 QObject 模板参数直接 static_assert。回归：test_select_wkey::angleRefPoint2TwoPointBasis 第 1 步补 toast 断言（拒绝后场景存在可见且含「零长度」的 HudItem，模型仍自动态）。ctest 全量 2 红 = 既有基线。

- **角度基准「对齐点」设计缺陷（2026-09 用户拍板修正：「对齐点和调换进出应该是完全无关的」）**：旧设计对齐锚点 = 绑 `seg->startPointId` 的**只读 tag**、随换向箭头翻转 —— ①用户无法选择"本线段的哪个端点对齐"（P3/P4 固定 = 朝向起点）；②换向后 start/end 身份互换、tag 乱跳，与连接语义脱节；③自动态点2 恒空，用户看不到"P3 对齐 P2、以 P2→P1 为基准"的方向全貌（E:.gcad L2 报告："对齐点的方向只有角度基准1输入框有内容，点2没有也无法填入"——除 focusOut 静默清空外，**自动态设计本来就不回填点2**）。修正（用户拍板三项）：①对齐点 → 可输入 PointRefEdit（`alignPointEdit`，objectName 契约），`setRestrictToBlock` 只接受本线端点（P3/P4 互选），写 `Attachment::fromPointId` + 新命令 `SetAlignPointCommand`（undo 快照 fromPointId/followerAngle/公式/模式）；**只改吸附端：本线方向（start→end）与角度基准均不变，followerAngle/公式原样存活，不反算不清公式**（directionAtPoint 对两端返回同向）；②自动态点2 回填宿主线段另一端 P1（autoEcho 灰显），点1 回填目标点 P2 —— 三点全可见；③对齐点与换向解耦：fromPointId 由连接语义决定，ReverseSegmentCommand 从不改附件 fromPointId（仅 followerAngle +180·k 补偿旧档空 angleRefPoint 回填）。**坑：autoEcho 回填必须不算用户意图**——refresh 预填落库分支须跳过 `isAutoEcho()` 的字段（原 has1/has2 只看点非空，自动回填后会把自动态误固化）；`PointRefEdit::applyMatch` 成功解析即清 autoEcho（任何成功解析 = 用户意图）。坑：autoEcho 是动态属性（QSS 灰显），`setAutoEcho(false)` 需 unpolish/polish 才刷新样式（既有实现已处理）。回归：test_select_wkey::alignPointEditableAndAutoStateTwoPointBackfill（自动态三点回填+幂等不落库 / P4 换对齐点零跳变 / 宿主点输入拒绝 / 换向后 fromPointId 不动 / SetAlignPointCommand undo）。ctest 全量 2 红 = 既有基线，无新增。

- **进点/对齐点规则表落地（2026-09 用户拍板：「对齐点基本=进点」「一个端点只能跟随一个点」）**：进点 = 本线段被钉住的那端（`Attachment::fromPointId`），出点 = 对端。规则表：①自由线（创建）→ 对齐点灰显默认进点（start 端 = 创建序先建点），连接后变真实钉点；②拖拽建连接 → 进点 = 按住端（`m_connectFromPoint`，既有）；③面板「连接到」→ 进点 = 输入端（既有）；④桥接（pin+pin）→ **无进点语义**：对齐点显示默认 + 禁用（长度由两点距离决定）；⑤起点连接 + 终点指向 + 指定长度 → 进点生效、对端指向目标位置、**允许超出延伸**（Step 7 只驱动旋转，长度由 Polar 距离独立决定，终点沿指向角越过目标点不截断）；⑥换向 → 进点不动（钉点不变，箭头只是身份标签）；⑦**出端被钉（倒挂配置，2026-09 用户拍板转正）**：拖拽把出端钉到宿主上而进出未换向时，改长度 = 钉住的出端世界位置不动、长度变化全部表现为自由进端沿本线方向伸缩（长度编辑恒写终点 Polar 距离，Resolver 位置约束把 fromPointId 钉回宿主点——与旋转工具 "start swings, end stays pinned" 同源，**设计行为勿当 bug 修**；正规解法 = 换向按钮把进出调正）。**实现要点**：`SegmentRefCard::refresh` 的隐藏判定从「endTarget/桥接整卡隐藏」改为「**方向段**（点1/点2/[独立]）在角度由约束决定时隐藏（endTarget 或桥接），**对齐点段恒显示**」——省道仍整卡隐藏；对齐点 tooltip 明示「箭头=身份标签，对齐点=实际钉点」。**坑：旧断言 `refCard->isHidden()`（桥接线整卡隐藏）与规则表冲突**——test_select_wkey::connectionCardEndConnection ③④ 与 test_dialog_tabs::endConnectionRowAndBadge ② 均改为「方向段隐藏 + 对齐点段保留禁用」断言。回归：test_select_wkey::endTargetOvershootExtendsAlongAim（指定长度 200mm > 目标距离 ~146mm：终点越过目标点 + 沿 起点→目标 射线共线同向 + 有 endTarget 时对齐点锁定 start 端禁用）+ test_commands::endPinnedLengthEditMovesFreeStart（出端钉宿主 + 改长度 60→100mm：钉端零位移、自由进端反向伸缩 40mm）。ctest 全量 2 红 = 既有基线，无新增。

### 第 9 组：P2 收口（UI 归位 / 版本迁移 / 求解量化 / 条件等待）

  - **① UI 组件归位 ui/（P2-4）**：33 个文件 `tools/` → `ui/`，命名空间同步 `cad::tools` → `cad::ui`。**踩坑**：
    - **先跑一遍 `grep -rn 'cad::tools::<名>' src/ tests/` 再改命名空间** —— 本次迁移文件内部 0 处限定引用（都在 `namespace cad::tools{}` 里用短名），所以只改 `namespace` 开合两行即可；真正要手改的是**留在 tools/ 里的前置声明**（`class AngleHud;` 在 `namespace cad::tools{}` 内 → 必须提到全局 `namespace cad::ui { class AngleHud; }`，成员声明改 `QPointer<cad::ui::AngleHud>`，否则编译能过但拿到的是 `cad::tools::AngleHud` 这个不存在的类型）。
    - **`SegmentConnectionCard{Build,Refresh,Conn,Dart}.cpp` 是切分 TU**，文件名不匹配主类名，改命名空间时按 `stem.startswith('SegmentConnectionCard')` 匹配，别漏。
    - **给 `gcad_ui` 补 `gcad_canvas` 链接**：这些控件要访问 CanvasScene/BlockItem 做实时刷新。include 是全仓 PUBLIC，编译照样过，**链接才报未定义符号**。
    - **`git mv` 对未跟踪文件会失败**（SegmentAngleCard/SegmentRefCard 当时是新增未提交文件）→ 报 `fatal: not under version control` 后改用普通 `mv`，不影响结果。
  - **② CMakeLists 块替换（通用踩坑，高危）**：用 python 按行切片重排 `add_library(...)` 时，**`lines[i:j]` 的 `j` 必须是闭合 `)` 的索引 +1**。本次写成 `lines[ui_s:ui_e]`（不含 `)`）后又自己补了 `)`，导致两个块之间的 `target_include_directories` + `target_link_libraries` 整段被丢掉，`grep target_link_libraries(gcad_ui` 直接查不到。**防御：改完立刻 `grep` 校验关键行还在**（本次靠 `grep -n "gcad_ui"` 只有 5 行命中才发现）。备份先落到 `build/`（已 gitignore）。
  - **③ 版本迁移框架（P2-1）**：新增 `src/document/FormatMigration.*`。**最重要的判断：区分「历史」与「模型不变量」** —— 我一开始把「文件缺辅助层就插一个」也当成历史搬进迁移，结果 `test_serializer::degradedValuesProduceWarnings` 立刻红。它是 `Layer.h` 声明的模型约束（每文档恰好一个辅助层、index 0、不可删除），对所有输入都成立，**必须留在序列化器**；真正属于历史的是「v0 的整数下标要 +1 位移」。判据：**这条规则对「今天刚写出来的 vN 文件」还成立吗？成立 = 不变量/兜底，不成立 = 历史。**
    - 迁移函数签名 `QJsonObject (*)(QJsonObject, QStringList*)` 纯 JSON→JSON，可脱离文档/画布/ZIP 单测（`test_migration` 11 例，一次全绿）。
    - **块没有 `layer` 字段时绝不能替它填 0**：序列化器的「缺字段 → 落到第一个工作层」默认逻辑还要继续生效，迁移里补 0 会静默改变落点。
  - **④ 求解开销量化（P2-6）**：`test_resolve_scale` 分档实测。**结论：不做 worker 线程**（拖拽路径线性、800 块仍只占 60fps 预算 24%；超线性的全量 resolve 只由离散动作触发）。**踩坑：同一份代码空闲 vs 构建刚结束跑，800 块差 63%**（4039µs vs 6597µs）→ 这类 bench **只做正确性断言、绝不断言耗时**，否则必 flaky。
  - **⑤ GUI 条件等待续（P2-3）**：
    - **新助手 `cad::test::settle()`**：断言「什么都没发生」时用（如「编辑 B 线不得影响 A 线的 attachment」）——没有可等的状态，`waitUntil` 拿不到谓词。本次把这类负向断言误写成 `waitUntil(某值 == 新值)`，直接超时失败。**迁移前先分清：断言"值变了"→ waitUntil；断言"值没变"→ settle。**
    - `test_dialog_tabs::switchBackAfterTyping` 抖动的真根因：输入值经 **debounce 定时器**应用，断言却在 `mouseClick()` 返回后立刻跑。改成 `waitUntil(interpPercent == 0.6)` 后红集合收敛。
    - **不要机械迁移 `qWaitForWindowExposed` 后的 `qWait(80)`**：`QTest::qWait` 不提前返回，80ms 一定等满，负载下并不会变短——它是**过度等待**不是「等不够」，换成瞬时成立的条件只会暴露被掩盖的竞态。
  - **⑥ 源码换行是 `\r\r\n`（40 个文件）**：MSVC 因此报 `C4335 检测到 Mac 文件格式`。python 里 `raw.replace('\r\n','\n')` 会把 `\r\r\n` 变成 `\r\n`（看似成功但内容没对齐），**必须先 `.replace('\r\r\n','\n')` 再 `.replace('\r\n','\n')`**。`Edit` 工具对这类文件也会匹配失败。**修不修是独立决策，本次只顺手规范了改动到的 test_dialog_tabs.cpp。**
  - **⑦ A1 批次（test_rotate_copy 前段 qWait 条件化，2026-08-29）**：
    - **判定结论（后续批次直接沿用，不要重新考证）**：sendMouse 式 `sendEvent(view.viewport())`+`qWait(20)` 对是**同步链路**——Qt 6.11 下 `QTEST_QPA_MOUSE_HANDLING` 未定义（全 Qt 头仅 qtestmouse.h:159 引用、无定义点），`QTest::mouseClick(QWidget)`/`QApplication::sendEvent` 均走 `qApp->notify` 同步投递；ToolRotate/AngleHud/CanvasScene 链路无定时器、无排队连接（CanvasScene 仅 toast 1500ms 与 GL 探测 singleShot(0)，不在测试鼠标路径上）→ 断言不依赖异步工作，**该模式统一保留 qWait+一条判定注释**；独立点位（如 Esc 取消）才逐个判 waitUntil/settle。
    - **Edit `replace_all` 会跨批次边界**：sendMouse lambda 全文件同构（每个 UI 测试一份），一次 replace_all 命中 35 处、其中 27 处落在任务边界（850 行）之外。**批量替换前先 `grep -c` 数命中数**，与预期不符就改用 python 按行区间改（本次从备份恢复后重做）。
    - **逐行替换脚本不消费下游行**：把「sendEvent+qWait 对」换成「sendEvent+settle」时，逐行处理只改了 sendEvent 行，原 qWait(20) 行照常流过 → 变成 settle+qWait 双重等待。**改完必须复核**（`settle` 的下一行不允许还是 qWait）。
    - **test exe 直跑 stdout 捕获不到**（GUI 子系统进程）：bash 重定向得到空文件但 rc=0，测试照样跑了 13 秒。用 `ctest -R x -V`（Pass/耗时）或 QtTest 自带 `-o 文件,txt`（函数级 PASS 明细 + Totals）。
    - **python 原地改源文件后必须补 BOM 再走构建**；本次 Edit 工具实测**保留**了 BOM（与 AGENTS.md「Edit 剥 BOM」的记载不符，但保险起见每次改完仍用脚本核一遍）。

- **线条属性「连接到」拆开/重连两处 UI 缺陷（2026-09 用户报告：「点击拆开后，连接到[]的输入框应该是空的，当前里面还是有原内容，虽然可以拆开了。且输入点id没有触发跟随，只是在线段上有特效」）**：①**拆开后输入框不清空**——`LinePropertyDialog::refreshEndpointConnRows` 的起点行回显条件只判 `att && m_topIsStart`，未排除 `att->angleOnly`（拆开 = 位置已自由），框里仍回显原目标点，与按钮已翻面「重连」矛盾（误导用户以为还连着）。修复 = 回显条件加 `!att->angleOnly`，拆开态 `clearPoint()` + 按钮「重连」可用（与隐藏辅助卡 SegmentConnectionCard::refreshUnifiedState 的既有语义对齐）。②**拆开后输入点 id 重定向不恢复位置连接**——`onStartConnectResolved` 重定向分支只改 `toBlockId/toPointId/toSegmentId` + 反算 followerAngle，`angleOnly` 保持 true → Resolver 只写旋转、跳过位置钉点（Resolver.cpp applyAttachment angleOnly 分支），画布上只有线段特效（角度跟随生效）而位置不吸附。修复 = 重定向分支先清 `angleOnly=false + isLocked=true + slideMode=None`（与「重连」按钮 SetAttachmentAngleOnlyCommand(false) 同语义：位置回宿主 + 重新焊接），再改目标点。**教训：拆开（angleOnly）是"位置维度已断开"的持久状态，任何"输入目标点 = 重连意图"的路径都必须显式恢复位置维度，只改目标点 = 静默半连接**。回归：test_dialog_tabs 新增 `detachClearsConnectEditAndRetargetReconnects`（拆开 → 框清空 + 按钮「重连」+ angleOnly；输入新宿主 P# 回车 → angleOnly=false / isLocked=true / toPoint=新目标 / from-point 吸附回新宿主点 / 框回显新目标）。

- **重连覆盖方向基准（2026-09 用户拍板：「连接拆开的时候角度基准保持不变。再次连接的时候也应该不变。当前重新连接任意点，会覆盖掉方向基准。而且只覆盖方向点1，点2没有内容」）**：自动态（`angleRefBlockId` 空）下重连，各路径只把旧目标点固化为点1、点2 留空 → 两点连线方向退化为单点出口方向；面板「连接到」重定向后基准跟随新宿主（方向基准被覆盖）。修复 = 新增共享 helper `cad::param::preserveAngleRefOnReattach`（FollowerAngle.h 声明 / ParamDocumentAttachments.cpp 实现）：自动态下把旧所连线段固化为**两点基准**（点1 = 旧目标点、点2 = 旧线段另一端），已自定义的基准原样保留，独立角时 ref 字段是还原缓存不动；**必须在改写 toBlockId/toPointId 之前调用**（旧宿主信息仍在 att 上）。接入 4 条重连路径：`ConnectGesture::reattachAngleOnly` / `SegmentConnectionCard::onTargetResolved` / `ReattachAttachmentCommand::redo` / `LinePropertyDialog::onStartConnectResolved`。配套：①反算 followerAngle 改用 `effectiveAngleRefWorld`（与 Resolver 同构，含两点基准与影子偏转）——此前恒用新宿主方向反算，固化基准后 Resolver 按旧基准驱动 → 重连瞬间跳线；②方向行新增 **[链接当前线]** 按钮（`SegmentRefCard::onLinkCurrentLineClicked`，objectName `linkCurrentLineBtn`）：清空自定义基准回自动态（走 `SetAttachmentAngleRefCommand`，undo 可撤），**必须先清空点1/点2 编辑框再 refresh()**——refresh 的"预填自动落库"分支（angleRefBlockId 为空时）会把编辑框残留的旧自定义值当用户预填重新提交，刚清空的基准会被写回；③独立角时点1/点2 清空但**不禁用**（用户拍板：可直接填点 = 退出独立角并建立自定义基准）；**独立角也不锁「链接当前线」按钮**（2026-09 修正：独立只是清空，不锁按钮——独立角 + 自定义基准时点击 = 清空基准 + 退出独立角回自动态，`SetAttachmentAngleRefCommand::redo` 会清 `angleIndependent`）。回归：test_dialog_tabs 新增 `reattachPreservesAngleRef`（自动态重连 → 点1=旧目标点/点2=旧线段另一端；自定义基准再重连 → 原样保留）/ `linkCurrentLineButtonClearsRef`（点击 → angleRef 全清 + 自动态回显当前线段两点 + 按钮禁用）/ `independentAngleKeepsRefEditsEnabled`（独立角 → 点1/点2 空且可编辑；独立角 + 自定义基准 → 按钮可用，点击 → 清基准 + 退出独立角）。

- **影子偏转锚点推导已删除（2026-09 用户拍板删除；本条为原「影子偏转累计角度偏差 → 锚点推导重设计」历史档案的收尾）**：shadowAnchorRotDeg 锚点推导（2026-09）与其前身 baselineOffsetDeg 累计账本均已随功能移除 —— 有效基准 = 真基准方向，不再叠加影子；`migrateV1ToV2` 从「锚点换算」改为「纯清理 baselineOffsetDeg 残留键」；test_migration 用例 `v1ShadowOffsetMigratesToAnchor` 改写为 `v1ShadowOffsetKeyIsDropped`（断言旧键丢弃且不写锚点键）。

## 5. 测试 / 基线（判"是不是我引入的红"先看这里）

- **拆开影子基准落地的三个新坑（2026-09-03，DETACH_SHADOW_DESIGN.md）**：
  ①**测试裸指针跨 addBlock 悬垂**：测试里 `const Block* leaderBlk = doc.findBlock(...)` 长持后，
  拆开影子换代 `addBlock(影子)` 扩容 blocks vector → 指针悬垂 → 读到垃圾 id → 断言
  `shadowMasterBlockId == leaderBlk->id` 假红（dt6 实测 leaderBlk 读出 dddddddd-dddd-dddd-dddd-dddddddddddd）。
  规则照旧「一次一取，禁止跨变更持有」——测试也要遵守：提前把 id 复制成值
  （`const QUuid leaderId = doc.blocks().at(0).id;`），断言用 `doc.blockById(leaderId)`。
  ②**PointRefEdit 名称解析必须排除影子块**：影子点有自己的 serial（克隆产物），P# 输入会解析到
  不可交互的影子点（R4 违规 + 面板路由错宿主）。findMatches 两个循环（点/线段）都要跳过
  `block.isShadow`（PointRefEdit.cpp）。
  ③**克隆影子点 Free 约束必须双写 freePos/resolvedPos**：Block::resolve 的 Free 分支取
  `freePos` 覆盖 resolvedPos —— cloneShadowOf 若只拷 resolvedPos，下次 resolve 后影子方向塌回
  零位（拆开瞬间姿态丢失）。
- **既有基线红 = 0 条（2026-09-02 修复，两条基线红已根治）**：
  ①`test_serializer::bridgeAuxPointSnappableAndAttachable` —— 根因：Resolver Step 5（桥跟随者重解）在
  `205a229`（旧组件铰链系统让路）被删除后未恢复，`bridgesMoved` 成为死变量；桥在 Step 4 拉伸后其
  follower 停在 Step 3 的旧桥姿态上，桥 aux 点跟随者落点错误。修复：恢复
  `if (bridgesMoved && settleAttachments())`（Resolver.cpp Step 5）。
  ②`test_component::dragComponentLeaderCurveFollowStable`（31.4798mm）—— 根因：锚点后处理改写宿主
  曲线切线 → followResettle 重解把以该切线为基准的内部连接重新定向 → 组件对齐被打破（bEnd 离开
  xEnd），外层收敛循环预算耗尽时停在偏离状态。修复：followResettle 后补一次组件沉降 + 锚点后处理，
  每轮结束在"组件对齐"状态（ParamDocumentResolver.cpp followResettle 分支）。
  **全量 ctest 34/34 全绿（2026-09-02 实测两次）**。
- **环境漂移红**：test_intersection_update（5 用例）+ test_extend::savedDocFormulaStartExtendRenders 依赖
  活档 `E:/3.gcad`，用户一改该文件就红（交点偏移 29.17mm / 缺变量「后长补正」），非代码回归；缺该文件则 QSKIP。
- **test_hold_show 空输出 exit 1（2026-09-04 根因确诊并修复）**：根因 = **`renderNonWhitePixels` 的隔行/隔列采样
  在 100% DPI 下把 10px 长度标签「10.00 cm」数量被测小**：标签实际渲染约 161 个设备像素（绿色小字，diff
  bbox 41×6），但步进 2 采样只数到 36，低于按 125% DPI 校准的 50px 阈值（`forced - baseline > 50`）→
  `forceLengthRevealsLengthLabels` 断言假红。**「空输出」是全环境 QtTest 现象**：该机 Qt 6.11.1 debug
  版 stdout/stderr 整体为空（连纯数学的 test_curve / 通过的 test_aux_layer / test_smartpen_aux 都 0 字节），
  与失败无关；判据：`-functions` 正常（moc/插件加载 OK）→ 必在用例执行期；用 `-o <file>,txt` 把结果写
  文件绕过 console 即见完整 `Totals: 8 passed, 1 failed`。**修复**：`renderNonWhitePixels` 去掉 step-2
  采样、改全像素扫描（差异只有 161 个物理像素，按步进 2 采样会系统性漏算小标签，阈值按 125% DPI 校准，
  100% DPI 下正好跌破）。修复后 9 用例全绿，全量 ctest 41/41 通过（详见 CONVENTIONS.md「test_hold_show 修复」）。
- **GUI 时序抖动**：test_dialog_tabs::switchBackAfterTyping / test_aux_layer / test_rotate_copy 在整批 ctest
  压力下偶发失败，单跑即过 —— 先单跑复现再判回归。**test_select_wkey 也在此列（2026-08-29 实测：
  整批 3 次里挂 1 次，单跑连续通过）**，它有 50 处 `qWait`（AGENTS.md P2-3），本就是抖动候选。
  **test_component::junctionComponentConnectGesture / componentConnectOverlapSwitchTarget 也在此列
  （2026-09-02 实测：整批 5 次挂 2 次，单跑连续通过；失败断言 = 拖拽后状态停在 Selecting 而非
  AngleInput，连接未建立，与 Resolver 修改无关——该阶段组件尚无组件级连接，settleComponents 不进入）**。
  判法：**同一个 exe 连跑两次 + 整批再跑一次**，只在整批挂、单跑稳 = 抖动，不是回归。
- **测试里不要用 CanvasView 造视口（2026-08-29 实测，test_mode_indicator）**：`CanvasView` 的 viewport 是
  `QOpenGLWidget`（CanvasView.cpp:50），Fixture 里放一个 → 每个用例一个 GL 上下文，5 个用例 = 5 个；
  **单跑 9 用例全绿，整批 ctest 直接 SEGFAULT**。改用**普通 `QGraphicsView`** 即愈 —— 角标锚定只用
  `mapToScene`，键盘走 `ToolManager::dispatchKeyPress` 根本不经视口，两者都不需要 CanvasView。
  代价：`CanvasView::zoomFactorChanged` 那条重定位接线不在自动测试覆盖内（滚动那条覆盖了同一机制）。
  同类旁证：AGENTS.md 记的 test_canvas_perf singleCurveFrame 段错误（疑似环境/软渲染）也是 GL 上下文问题。
- **构建偶发 `fatal error C1083 ... .obj: Permission denied`（2026-08-29 实测）**：ElaWidgetTools 的 7 个
  `.obj` 同时报无法写入，但 `Get-Process` 查不到任何占用进程，**重跑一次 build.bat 就全过**。
  判据：错误全落在 `_deps/elawidgettools-build/` 下 + 无任何编译语义错误 + 重跑即愈 = 文件锁抖动，
  不要去改 patch 脚本。**改完源码一定要核 exe 时间戳**（`stat -c '%y' build/out/xxx.exe`）：
  构建失败时 exe 是旧的，跑的也可能是旧版 —— 本次就差点把旧 exe 的"通过"当成新代码已验证。
- **GUI 测试不要在同一进程里反复 new CanvasScene（2026-08-29 实测，test_mode_indicator）**：为了"每个工具
  一套干净环境"在循环里造 4 个 Fixture（各含 `CanvasScene` + `ToolManager`），结果 5 个用例全 PASS 却在
  `cleanupTestCase` **之后进程退出期崩溃**（栈里 QLineEdit 构造 + LdrShutdownProcess，单次耗时从 3ms 涨到
  33 秒）。改成**一个 Fixture 反复 `switchTool`**（场景只 new 一次）即愈。
  **判据：用例全绿但进程非零退出 → 先查是不是反复创建场景级对象，别急着判产品代码回归。**
  与第 1 组"陈旧 AUTOMOC 段错误"的区分：**本条是用例全绿后退出期崩；那条连 `-functions` 都出不来文本。**
- **角度归一化函数 while 循环死循环隐患（2026-09 修复，P1 改进）**：`src/geometry/Angle.h` 中的 `normalizeDeg180` 与 `normalizeRad` 原使用 `while (deg > 180.0) deg -= 360.0;`。当外部输入极端多周期大数（`1e12`）或由于未初始化/除零产生 `Inf`、`NaN` 时，`while` 循环空转乃至无限死循环导致主线程卡死。**修复**：改用 `std::fmod` + `std::isfinite`（非有限数秒级安全返回 0.0，保持左开右闭 `(-180, 180]` 和 `(-π, π]` 区间契约，O(1) 复杂度）。验证：`tests/test_curve.cpp` 新增 `angleNormalizationRobustness` 覆盖常规角、开闭边界、多周期大数与非有限数，测试全绿。
- **MSVC 全局 `/utf-8` 与 Qt6 预编译库 ABI 冲突引发 SegFault（2026-09 关键排错经验）**：切勿在根目录 `CMakeLists.txt` 对 MSVC 盲目施加全局 `add_compile_options(/utf-8)`。MSVC 的 `/utf-8` 会同时开启 `/source-charset:utf-8` 和 `/execution-charset:utf-8`，导致全仓窄字符串字面量均按 UTF-8 编码编译进二进制；而 Windows 下 Qt 6 官方预编译二进制的局部 API/内部私有模块按系统 ANSI（CP936）代码页预期解码，导致 `test_resolver` 与 `test_reverse_segment_commands` 在启动或内部符号解析时报 `0xC0000005 (STATUS_ACCESS_VIOLATION)` 崩溃。**架构定则**：严格遵守 AGENTS.md 既有规范「含中文源文件必须 UTF-8 with BOM」，让 MSVC 自动识别源码 UTF-8 而不干扰全局执行字符集；`/utf-8` 仅针对纯独立自编译第三方库（`ElaWidgetTools`）保持局部 `target_compile_options`。
